# How many falsification recipes may still assert only `"nonzero"`.
#
# A `nonzero` recipe proves "something exited non-zero". It cannot tell a control that FIRED from a
# recipe that CRASHED, a tool that was ABSENT, or a plant that never ran -- the Increment 17
# measurement of the `secrets` probe is the worked example: it advertised "a credential reaching the
# history" and proved "a generic entropy rule fires on a random string", at a 5.3% flake.
#
# The council refused a 25-recipe sweep and adopted a RATCHET instead (18-F): commit the number,
# fail when it rises, lower it each cut. Same mechanism as docs/skip-budget.txt, second population.
# A sweep is the thing that reports EVAPORATED for seventeen days; a number is either met or not.
#
# Lower this. Never raise it without saying why in the cut's ratification.
#
# Ratchet log -- each step names what came off the list, so a future cut can see whether the
# number moved because recipes got stronger or because controls were deleted.
#   25 -> 20  Increment 19, condition 18-7. Security first, as the condition says: the two
#             remaining legs of the gate's security core (`deps` asserts the advisory id
#             PYSEC-2021-66 that pip-audit resolves the planted jinja2 2.11.2 to; `slopsquat`
#             asserts `FAIL [F.8] langchain-quantum-helper-xyz`, naming the planted package and
#             the rule) and all three hooks, which AGENTS.md calls the most catastrophic rules
#             enforced deterministically. `slopsquat` was additionally scoring off exit 2 --
#             GP's reserved NOT-EVALUABLE code -- which is TB-088 surviving inside the `refuse:`
#             branch that TB-087 repaired.
20
