# Backticked `/token`s in shipped documents that are NOT skill invocations.
# Read by conformance/test-documented-skills.py. Format: `token<TAB>reason`.
#
# WHY A DECLARATION AND NOT A HEURISTIC. A shipped document writes `/health` and
# `/repo-review` identically, and no amount of pattern-matching separates an HTTP
# route from a slash-command reliably. So the exemptions are NAMED, each with a
# written reason -- a hand-kept list is allowed only in that form -- rather than guessed at.
#
# THE FAIL DIRECTION: forgetting to declare a route here produces a FALSE POSITIVE,
# which is loud and takes one line to fix. A heuristic that guessed wrong would
# produce SILENCE, which is how ten dangling skill references once shipped unnoticed.
# The check reads names of three or more characters, so a shorter token needs no row.
health	the L.7 version-stamped liveness/readiness contract; the single most-cited endpoint in this package
ready	a readiness probe route, paired with /health in the go-live checks
version	a build-identity route named beside /health
deploy	a directory in the repository layout (`/deploy/**`), a DevOps-owned contract surface per K.10
tmp	a filesystem path in shell examples
# Names a reader types that are not this project's skills: Claude Code's own commands,
# common HTTP routes, and filesystem roots written as paths.
review	a Claude Code built-in command, not a skill this project ships
security-review	a Claude Code built-in command, not a skill this project ships
rewind	a Claude Code built-in command, not a skill this project ships
init	a Claude Code built-in command, not a skill this project ships
compact	a Claude Code built-in command, not a skill this project ships
clear	a Claude Code built-in command, not a skill this project ships
help	a Claude Code built-in command, not a skill this project ships
model	a Claude Code built-in command, not a skill this project ships
config	a Claude Code built-in command, not a skill this project ships
agents	a Claude Code built-in command, not a skill this project ships
mcp	a Claude Code built-in command, not a skill this project ships
schedule	a Claude Code built-in command, not a skill this project ships
memory	a Claude Code built-in command, not a skill this project ships
permissions	a Claude Code built-in command, not a skill this project ships
hooks	a Claude Code built-in command, not a skill this project ships
doctor	a Claude Code built-in command, not a skill this project ships
cost	a Claude Code built-in command, not a skill this project ships
status	a Claude Code built-in command, not a skill this project ships
resume	a Claude Code built-in command, not a skill this project ships
context	a Claude Code built-in command, not a skill this project ships
export	a Claude Code built-in command, not a skill this project ships
login	a Claude Code built-in command, not a skill this project ships
logout	a Claude Code built-in command, not a skill this project ships
docs	FastAPI's interactive API docs route
redoc	FastAPI's ReDoc route
metrics	a conventional metrics endpoint
etc	a filesystem root written as a path
usr	a filesystem root written as a path
var	a filesystem root written as a path
opt	a filesystem root written as a path
home	a filesystem root written as a path
bin	a filesystem root written as a path
dev	a filesystem root written as a path
proc	a filesystem root written as a path
v2	an example public API contract version, used to show what may not change without an ADR
skill	the literal placeholder in the control roster's own description of `documented-skills` -- "a document telling a reader to type a `/skill` that does not exist". HARVEST-CONTEXT.md is GENERATED from that description, so the control quoting itself tripped itself. Not a route and not an invocation: no skill will ever be named `skill`

# ── model_ranking (DevFlow v6.0 adoption, D-155) ────────────────────────────────────────────────
# This engine's own HTTP routes, named in its PRD and plans.
recommend	the engine's /v1 recommendation route, named in docs/deliverables-plan.md
rankings	the engine's ranking route, named in docs/deliverables-plan.md
# Upstream HTTP APIs this project reads, quoted in records as the URLs it fetched.
rows	Hugging Face datasets-server endpoint, quoted in closure reports, plans and decisions
first-rows	Hugging Face datasets-server endpoint, quoted as fetched
splits	Hugging Face datasets-server endpoint, quoted as fetched
is-valid	Hugging Face datasets-server endpoint, quoted as fetched
filter	Hugging Face datasets-server endpoint, quoted in docs/fixpack-1.md
# Claude Code's BUILT-IN commands. They are not shipped skills and never will be; a record that says
# a seat ran `/security-review` or that a session was `/rewind`-ed is naming the harness.
# Filesystem paths and FastAPI's own routes, quoted in security reviews as attack surface.
app	a container filesystem path (/app) in the M7 security review's path-traversal probes
data	a container filesystem path (/data) in the M7 security review
nonexistent	a deliberately absent path in the M11 security review's negative test
