---
record_type: warnings
id: warnings-ledger
status: draft
process_version: v6.6
date: 2026-09-23
---
# Warning ledger (V4C-77)

> **Copy of `warnings.ledger.template.md`, instantiated so a fresh install starts with a report rather
> than a silence.** Read the template for why `C2b` stops at three.

**The rule in one line: a warning may not survive the close it was raised in.** It is FIXED, ACCEPTED
with a reason AND an owning milestone, or ESCALATED (this project keeps its own validator, D-161: an issue as the owner is not read here). `check_records.py` rules `C2a`/`C2b`/`C2c` enforce it.
A skipped or bypassed control is not a warning: it is a row in `docs/control-events.csv`.

| id | rule that warned | first seen | path | status | reason + owning milestone |
|---|---|---|---|---|---|
| W-012 | `conformance/test-governed-paths.py`, first run after the v5.0 install | 2026-08-16 (D-113 migration) | `.governed-records:14` | FIXED | The glob `docs/wave-checklist-*.md` matches **no file in this repository** — this project names its wave records `docs/plans/m{N}-wave-{W}-close.md`. The glob was written from memory and never verified, which means **every wave-close record this project has ever produced was ungoverned**: the frontmatter, id-uniqueness and cross-reference rules (R1–R3, X1–X3) never ran on any of them. Nothing is known to be wrong inside those records; what is established is that nothing checked. The remedy is a one-line glob correction, but `.governed-records` decides what the gate reads, so it is a **gate-definition change and the owner's call** (AGENTS.md §3 escalate-now). Owning milestone: **M6, before W1.** **RESOLVED 2026-08-16 — owner ruled APPLY.** The glob is now `docs/plans/m*-wave-*-close.md`. It matches **20** records, not the 4 first estimated, and governing them surfaced `R1` on **every one**: none had frontmatter, because all were written from the v4.3.1 template that shipped without it — the exact defect v5.0's template comment names. A frontmatter block was ADDED to all 20 (`record_type: wave`, `id`, `status: ratified`, `date` = each file's first-commit date from git). **No line of any record's content was altered**, and `process_version` was deliberately omitted rather than stamped `v5.0`, which would be a false statement about when they were written. Validator now scans 28 records (was 8) and passes. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** The glob `docs/wave-checklist-*.md` is no longer in `.governed-records`; the manifest names `docs/plans/m*-wave-*-close.md`, which matches 19 real records. Verified by grep against the file. |
| W-013 | `conformance/test-documented-commands.py`, first run after the v5.0 install | 2026-08-16 (D-113 migration) | `docs/closure-report-m3.md:69`, `docs/reviews/m4-security-review.md:80`, `docs/reviews/m5-security-review.md:101` | ACCEPTED | v5.0 removed the `pin-check` Make target — action pinning moved into `conformance/test-action-pins.py`, which passes (16 references, 0 unpinned). Three **historical records** still instruct a reader to invoke that removed target. The records are append-only (B.2) and one of them is a governed record, so correcting the text rewrites history to match a tool. **This is a migration hazard in GP v5.0 itself, not only here:** removing a Make target retroactively invalidates the records of every project that documented it. Under V4C-71 it is a finding to hand BACK to GP. Owner's call on both halves. Owning milestone: **M6, before W1.** **RESOLVED 2026-08-16 — owner ruled HAND BACK.** The three records are untouched. The finding is written up as **GPF-001** in `docs/gp-field-findings.md` with three ranked remedies, for the owner to carry to the pipeline repository. A second finding made during the same migration ships with it (**GPF-002**: the shipped record schema declares 7 record types while the validator enforces 14, and nothing validates against it). The conformance leg stays RED on these three lines until GP rules; that is the honest state, not a suppression. **RECLASSIFIED 2026-08-22 (M11-W3 ledger audit): ACCEPTED as history.** Three historical closure reports still instruct a reader to run `pin-check`, a target v5.0 removed. They are records of what was true when written, and GPF-001's rule cuts both ways: a tool may not retroactively invalidate an old record, and neither may a tidy-up. Anyone reaching those instructions finds the target missing immediately; the equivalent check, `conformance/test-action-pins.py`, passes. |
| W-016 | `docs/plans/m6-wave-1-close.md` rows 3 and 4 | 2026-08-16 (M6-W1) | K.7 / V3C-78 / F15 | ACCEPTED | **M6-W1 has had NO fresh-eyes review and no pulled-forward security pass.** The lead agent wrote every line of the wave, so it cannot supply fresh eyes, and it has no second reviewing agent available without the owner's authorization. Declared **NO-ENVIRONMENT**, not waived, and **the wave is held OPEN rather than closed** — W2 does not start until this runs. This matters more than usual here: the one M5 wave that skipped its review shipped three BLOCKING defects, one of which printed a success message over a database it had not repaired, and the retrospective scored that control's value by exactly that wave. Row 4 is recorded separately because F15 is the failure mode where a **plan-tagged** security pass disappears quietly. First occurrence for both controls; under `C2b` a third sends the control itself for review. Owner action: authorize a reviewing agent, or review it himself. Owning milestone: **M6-W1.** **RESOLVED 2026-08-17 — the owner authorized a reviewing agent and both controls ran.** They did not merely tick: the code review returned **BLOCKING** (2 BLOCKING / 10 MINOR) and the security pass returned **BLOCKING** (1 BLOCKING / 6 MINOR); both re-issued BLOCKING on the first fix, and the wave closed only after a third round. The most serious finding was in the guard protecting this milestone's central contract — a nine-word denylist that one rename walked past — and the second was a disclosure that failed OPEN. **This row is the measured answer to the question of what a skipped review costs: everything found here was found by someone who had not written the code, in a wave whose gates were already green.** Records: `docs/reviews/m6-wave-1-review.md`, `m6-wave-1-rereview.md`, `m6-wave-1-security.md`. **RECLASSIFIED 2026-08-22 (M11-W3 ledger audit): ACCEPTED as history, superseded by D-133.** M6-W1 had no fresh-eyes review and cannot retroactively acquire one. What was missing was not a reviewer but a RULE that made a self-review unable to close a wave green, and that now exists — K.7 in the single-agent lane means a separate session leaving a file, enforced by `scripts/wave_check.py::review_seat_problems`. **C2b-reviewed: D-133 @3** (2026-08-25, M12-W5) — K.7 reached three acceptances and the control was reviewed; D-133 is the outcome. |
| W-020 | `docs/plans/m6-wave-3-close.md` rows 3 and 4 | 2026-08-17 (M6-W3) | K.7 / V3C-78 / F15 | ACCEPTED | **M6-W3 is HIGH tier and none of its three reviews has returned.** The wave carries a schema migration, untrusted-input parsing and a network security config — three independent auto-HIGH triggers — so V3C-78 requires a separate Code-Reviewer AND Tester plus the plan-tagged pulled-forward security pass, not the single combined reviewer W2 had. All three are dispatched and the wave is held OPEN. Third consecutive wave with a review pending at the record's writing; under `C2b` this is the count that would send the CONTROL for review, and the honest reading is that it is the CADENCE under review rather than the seat — reviews are running every time, they are simply slower than the code. Owning milestone: **M6-W3.** **RESOLVED 2026-08-17.** All three seats ran and all three returned BLOCKING: 4 + 3 + 2, plus a tenth the Code-Reviewer found inside the fix delta (an undeclared CLI exit code on a K.8 frozen contract, now D-120). **The HIGH tier paid for itself measurably:** the Tester ran 52 mutants across three rounds against the author's 11, and 16 of them stayed green on code whose gates were already passing. One combined reviewer would have shipped an unwired startup validator, an unguarded remote-fed YAML input, a broken exit-code contract, and a denial of service the author introduced while fixing a denial of service. **RECLASSIFIED 2026-08-22 (M11-W3 ledger audit): ACCEPTED as history, superseded by D-133.** M6-W3 was HIGH tier with three auto-escalating triggers and none of its three reviews returned. Recorded, unrepeatable, and now structurally prevented. |
| W-026 | M7-W1 security review MINOR-5 | 2026-08-18 (M7-W1) | `Makefile` lint target vs `scripts/` | FIXED | **The linter has never seen `scripts/`, including its security rules.** `make lint` is `ruff check src tests`; `ruff check scripts` reports **18 pre-existing errors** (24 when first counted; the agent's own drive-by `ruff --fix` runs during this wave reduced it, which made the row stale the day it was written — MR8), and ruff's `S` (flake8-bandit) ruleset has therefore never run on that directory — which now holds the L.8 deploy gate (`smoke_deps.py`), the governance validator (`check_records.py`), the bootstrap gate and the wave-check tool. This wave removed a `# noqa` from `smoke_deps.py` and nothing checked the result. **Not fixed by the agent because widening `make lint` is a GATE-DEFINITION change** (AGENTS.md §3 escalate-now), and it would turn the build red on 24 findings the owner has not seen. Two honest paths: fix the 24 and widen the gate in one change, or widen it with an explicit per-rule ignore list that names what is being deferred. Owning milestone: **M7.** **CLOSED 2026-08-18 at the owner's instruction.** `make lint` now reads
`ruff check src tests scripts`. Widening it surfaced **12** findings, and the shape of them is the reassuring part: only
ONE was a security rule. Three were real and fixed — an `except Exception: continue` in `check_records.py` that silently
skipped any record the English-only rule could not READ (so the gate reported clean on a file it never opened), plus a
semicolon statement and an unused variable in `wave_check.py`. Nine were `C901` complexity in one file and are EXEMPTED
in `pyproject.toml` with the reason written beside them: `check_records.py` is a rules engine where each governance rule
is a branch, so the number describes the file rather than diagnosing it, and splitting nine functions would touch the
validator every governed record depends on. **Only the complexity heuristic is exempt; every security, correctness and
import rule now applies.** Suppressing C901 then revealed five more findings it had been masking — a `glob.glob`, a
loop that should be `any()`, a list concatenation, and an ambiguous-dash warning on a line where the dashes are DATA
(the rule accepts four spellings of "no artifact") — four fixed, one annotated. Every edited tool was then RUN, not
merely linted: `check_records` scans 38 records and its self-test still fires L1; `arena_calibration` still produces
output. |
| W-027 | M7-W1 code review B6 | 2026-08-18 (M7-W1) | `.github/workflows/contract-tests.yml:14-17` | FIXED | **A promise in the signed plan was not delivered, and saying so is cheaper than letting it pass.** `docs/plans/m7-plan.md` §5 assigned "coverage / roster-staleness CI legs, never run" to W1; the triggers are **byte-identical to `9f4471d`**. The workflow still runs only on `workflow_dispatch` and a Monday cron that has never fired, so both jobs remain unexecuted in this repository's history — the exact class this milestone exists to end, one layer above the code. **What W1 DID deliver is that the workflow would now survive a run:** its build step invokes real product code, accepts exit 3 as the degraded-but-built case, and its arguments are checked against the code by `test_ci_argument_drift.py`. What is missing is the run itself. Not done by the agent: `workflow_dispatch` already exists, so the remedy is to TRIGGER it rather than to change a trigger, and starting a run on the owner's repository spends his CI minutes and hits five third-party APIs. **Owner action, one line:** `gh workflow run contract-tests.yml` — then read the result, because a first-ever run is evidence and this wave has no substitute for it. Owning milestone: **M7-W1 / owner.** **Owner ruled 2026-08-21 to leave it open while nothing deploys.** State measured that day: `gh` is authenticated and `origin` exists, but **15 commits are unpushed** and `origin/main` sits at `9359251`, so triggering the workflow now would test a tree three waves old. The order is push, then `gh workflow run contract-tests.yml`. Expect it to come back RED on arena for the reason in W-024, which is a correct and informative result rather than a reason not to run it. Owning milestone: **M9**. **RESOLVED 2026-08-22 — and the row's own premise was wrong.** The claim was that the workflow runs only on a Monday cron *that has never fired*. Checked against GitHub after the M9 push: it fired on **2026-08-17** and FAILED. So the workflow HAS run; what had never happened is anyone looking at the result. **The push revealed something worse: `CI` itself has been RED since at least M8**, failing on the conformance leg, because `make check` did not run `conformance/run-all.py` at all — so the gate the owner types was green while the gate that runs was red, and a permanently red CI had taught everyone to stop reading it. Of the five conformance findings, four were known GP hand-backs (GPF-001, GPF-004) and **one was ours**: this project's own field-experience report proposed a Make target in backticks and the checker read the proposal as an instruction. Fixed. `scripts/conformance_gate.py` now runs the suite inside `make check` with per-FINDING exemptions — never per-leg, because our defect lived inside a leg that was already failing for somebody else's reason — and a stale exemption fails too. Proven to catch a new dangling command. The `py3.14` CI leg added at W-025 passed on this same run, which is the first confirmation that the interpreter the authoritative run uses is now covered. **FIXED 2026-08-22 (M11-W3) by owner ruling, and the row's claim was too kind.** It said the triggers were never delivered. Measured: the workflow had run **twice in its entire life and failed both times**, and it was never a source-drift failure — `pyproject.toml:128` sets `fail_under = 85` for the FULL suite while this step runs only `tests/integration`, 14 tests whose job is to hit five live endpoints, so coverage came out at 18% and pytest exited 1 with every test passing. It was **structurally unpassable from the day it was written**, and because step 1 failed, the three steps after it — which build the evidence database against real sources and smoke the CLI — had never executed once. That is why the 2026-08-17 cron going red for a REAL arena timeout reached nobody: a badge that has always been red carries no information. Fixed in three parts: `--no-cov` on the live step; `scripts/ci_coverage_gate.py` so the coverage step tolerates a category the build DECLARED absent (seven of nine surfaces are Epoch-backed and D-101 makes that bundle owner-placed) while still failing on any other empty category and on a wholly empty artifact; and `push`/`pull_request` triggers **scoped by path** to the client, source-registry, ingest/build, integration-test and workflow files. Scoped deliberately: these jobs hit other people's services, and a gate that fails for reasons nobody caused is how `continue-on-error` gets added later. **The workflow is now GREEN for the first time**, and `test_real_arena_latest_satisfies_parser_contract` PASSED — M9's arena fix verified against the live endpoint. |
| W-028 | M7-W1 fix round | 2026-08-18 (M7-W1) | `src/app/workflows/build.py` workspace files | ACCEPTED | **A SIGKILLed build leaves a `<target>.<random>.building` file behind.** The workspace name became unique per run to close a corruption bug (security BLOCKING-4: with a deterministic `<target>.building`, two overlapping builds shared one path and build A published build B's truncated database while reporting `"built": true` with 73 models read back). Unique names close that, at the cost that a run cannot tell another run's litter from a LIVE sibling's workspace — so it deletes neither. Every normal and abnormal exit this process can observe already cleans up; only SIGKILL and power loss escape. **Accepted rather than fixed because the available fixes are worse:** deleting files matching the pattern reintroduces exactly the race that was closed, and an age heuristic reintroduces it on a slow build. Reason: an operator-visible stray file is cheaper than a corrupted artifact that reports success. Owning milestone: **M8**, if `.building` files are ever observed accumulating in practice. **RESOLVED 2026-08-21.** `_sweep_abandoned_workspaces` in `src/app/workflows/build.py` deletes `.building` files untouched for six hours, which is far beyond any observed build (~60 s) and far below any plausible pause. Age is what distinguishes litter from a LIVE sibling's workspace, which the unique-per-run naming deliberately cannot — and the direction of a wrong guess is the safe one: a running build's workspace is seconds old and is never touched, so the worst case is litter surviving one more cycle rather than a live build losing its file. |
| W-029 | M7-W1 code review MR6 | 2026-08-18 (M7-W1) | `tests/unit/test_api_config.py` W-023 guard | ACCEPTED | **W-023's citing test skips in every clean checkout**, because `advisor.db` is gitignored and shipped as a mounted artifact rather than baked into the image (D-116, deliberate — baking it would make every data refresh an image rebuild and put a stale answer behind a fresh build stamp). So the guard covers the owner's machine and nothing else: CI cannot verify an artifact CI does not have. **What actually protects a deploy is a different control that DOES run there:** the startup probe in `adapter.main` opens the database, checks the tables and the `effort` column, and refuses to boot on an unusable one. Recorded rather than papered over, and the skip message now names this instead of being silent — a silent skip reads as coverage. Owning milestone: **M8 / deploy.** **RESOLVED 2026-08-21 — closed on the control that DOES run.** The skip is correct and stays: `advisor.db` is gitignored and shipped as a mounted artifact (D-116, deliberate — baking it in would put a stale answer behind a fresh build stamp), so CI genuinely cannot verify an artifact CI does not have. What protects a deploy is the startup probe in `adapter.main`, which opens the database, checks the schema and refuses to boot on an unbuilt one — and since 2026-08-20 `ios/app.sh` runs that same validator as a preflight (W-042), so the control now fires on the one command anybody runs. A guard that skips where it cannot see, beside a guard that runs where it can, is the right arrangement rather than a gap. |
| W-030 | M7 Stage-4.0 security review, stated as unverified | 2026-08-18 (M7 closure) | `scripts/journey.py`, REQ-API-009 | ESCALATED | **REQ-API-009's over-the-network half is NOT verified, and the criterion reads as if it were.** The journey passes 4/4 — cold entry, the N/A credential step, a content-asserting round trip and a cross-surface sequence — but every one of those runs was against a container on `127.0.0.1`. What no run has exercised: TLS and `force_https`, a real DNS name, Fly's proxy in front of the process, and the latency and connection behaviour that only appear over a network. The security seat flagged this itself rather than letting the criterion's PASS stand unqualified, which is the honest form. **Not closable without a deploy** (D-123 moved that to M8), and it must not be reported as covered by the local run. Owning milestone: **M8 go-live.** |
| W-031 | M7 Stage-4.0 security review, stated as unverified | 2026-08-18 (M7 closure) | `fly.toml`, `Dockerfile`, the mounted-artifact shape | ESCALATED | **No Fly.io deployment exists, so the deploy platform's own behaviour is entirely untested.** Specifically unverified: volume permissions against a container running as uid 10001 (the image is non-root by design, and a volume it cannot read would fail at exactly the wrong moment), the machine's OOM and restart behaviour under the 256 MB VM `fly.toml` declares, and `force_https`. The Stage-4.0 pass measured memory under a local `--memory=256m` container, which is a good proxy and not the platform. `fly launch` reached the payment step and created nothing, so there is no half-provisioned state to clean up. Owning milestone: **M8 go-live.** |
| W-032 | M7 closure, running `wave_check.py` by hand | 2026-08-18 (M7 closure) | `Makefile` `check` target vs `wave-check` | FIXED | **`make check` does not run `make wave-check`, and all four of M7's wave-close records were failing it.** The V3C-69 checker exists, works, and is invoked by nobody: it wanted a `Touched:` line, a `K.8 contracts:` line, a signed footer with a commit range, a recognised status word in every verdict cell, and a backticked referent in every row. M7's four records had none of the first three and were missing evidence on 13 rows between them. **M6's four records pass it cleanly**, which makes this a regression in the agent's own discipline rather than a defect in the tool — the gate was run then and not now. All four M7 records are now filled from the real diffs (`git diff --name-only` per wave range) and pass. **Not wired into `make check` by the agent: adding a gate to the canonical target is a gate-definition change** (AGENTS.md §3), and it would turn the build red on any historical record that does not conform. Owner action: `make wave-check FILE=<record>` exists today; wiring it in is one line in the `check` target. Owning milestone: **M8.** **RESOLVED 2026-08-20.** `make check` now runs `scripts/wave_check_all.py`. Scope is declared rather than assumed: records are in scope when they carry `process_version: v5.0`, so the tool cannot retroactively invalidate the twenty pre-migration records — this project's own GPF-001 finding applied to itself — and omitting the stamp on a newer record is a failure rather than an exemption. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** `make check` runs `wave-check-all`, which validates all 19 v5.0 wave records. The gate that was invoked by nobody is now in the command people type — which is the remedy this row proposed. |
| W-033 | M8-W1 owner review of the running app | 2026-08-18 (M8-W1) | `/v1/recommendations` vs `docs/plans/m8-plan.md` | FIXED | **The client needs the full ranking and `/v1` publishes three picks.** The owner's design has each category showing its three labelled recommendations plus the top 5 by score, and opening a category to reveal every ranked model. The engine ALREADY computes all of it — `category_ranking` returns **44 models for `coding` and 13 for `agentic-coding`** — and `/v1` serves 3. **This is exactly the case D-124 opened the window for:** a value the client cannot render because the API does not send it, filed as a finding against `/v1` BEFORE any client-side workaround, and the workaround is not written while this row is open. Note the two are not the same thing dressed differently: the three picks answer three DIFFERENT questions (best quality, best value, budget) and a top-5 is one ordering, so the payload gains a list rather than a longer version of what it has. Owning milestone: **M8-W2.** **RESOLVED 2026-08-19 — closed by D-125**, which adds the `ranking` array to `/v1` and spends the single contract revision D-124 permitted during M8. The row stayed `ESCALATED` for a day after its own ADR closed it, and was found by the M8 fresh-eyes code review rather than by the agent that wrote both. A ledger that outlives its resolution is the same defect as a record that contradicts the code, in the other direction. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** D-125 spent D-124's revision window adding the `ranking` array, which is the full ranking the client needed. The screen opens a category and shows every ranked model. |
| W-034 | M8-W1 owner review | 2026-08-18 (M8-W1) | product requirement, not yet a REQ-ID | FIXED | **The product needs a scheduled refresh the architecture does not yet have.** The owner stated the shipped product includes an agent that checks the sources every 12 hours and rebuilds when something changed. M7 built the machinery for this (`app.workflows.build` is runnable, fails loud on a partial build, and never leaves a half-written artifact) but nothing SCHEDULES it, and **D-116 forbids ingestion on the serving host**, so a refresh must run somewhere else and hand the artifact over. Two consequences worth deciding before it is built: what a refresh does when a source is down (today the build stops loudly, which is right — silently serving stale prices is the failure this product cannot afford), and how a new artifact reaches a running service without the swap being visible to a user mid-request. Not scoped into M8, which is the client. Owning milestone: **M9.** **RESOLVED by M9.** The refresh exists: one command performs one cycle, `launchd` runs it every twelve hours once the owner loads the plist, and both consequences this row asked to be decided before it was built were decided as ADRs before W2 — what a refresh does when a source is down (**D-128**: it refuses to publish something worse, and an optional source going blind is disclosed rather than hidden) and how a new artifact reaches a running service (**measured**, not designed: the adapter opens a read-only connection per request, so an atomic rename lands on the next request and a request in flight finishes on the inode it started on). REQ-REF-001..007 in `docs/prd.md` carry it. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** M9 built the refresh: `src/app/workflows/refresh.py`, one cycle per invocation, five exit codes, `deploy/com.hcs.modelranking.refresh.plist` for the twelve-hour schedule. The remaining half — that nothing has ever RUN it unattended — is W-054 and is tracked there rather than here. |
| W-024 | `make smoke-deps`, owner's out-of-sandbox closure run | 2026-08-17 (M6 closure) | `src/app/clients/arena.py`, the HF datasets-server endpoint | FIXED | **The arena dependency is DOWN and the closure report claimed the opposite.** `make smoke-deps` now exits 1: four of five remote dependencies parse (litellm 2194 rows, openrouter 389 models, swebench 173, aider 68) and **arena fails with an upstream `500 Internal Server Error` from the HF datasets-server on page 0**, reproduced on two consecutive runs minutes apart, so it is not a momentary blip. **The lead agent had recorded `make smoke-deps` PASS in `docs/closure-report-m6.md` §7 and in `note.txt`; that claim was true when measured and false at the closing tree, and it was the owner's own run that caught it** — which is the L.8 doctrine landing on the agent that wrote it: *configured is not working, and neither is measured-once*. Impact is bounded and stated rather than assumed: arena is an INGESTION source, not a serving path, so `/v1` is unaffected and no test regressed (365/12 local, 372/5 with the bundle). What it does block is re-ingesting arena scores, and **Stage 4.3 explicitly does not pass while any dependency is unusable**. Not fixed by the agent: the failure is entirely upstream, and adding a retry or a skip here would convert a loud external outage into a quiet one — the exact inverse of why this gate was repaired in v4.3.2. **Owner action before any deploy: re-run `make smoke-deps` and require exit 0**, or, if the endpoint stays down, rule explicitly on whether a stale arena source is acceptable for a first deploy. Owning milestone: **M7 / deploy gate.** **DIAGNOSED 2026-08-21, and the diagnosis overturns this row's premise.** arena was never simply down. Measured: `/is-valid` reports filter support, `/splits` lists `text/latest`, `/first-rows` returns rows carrying `category='overall'`, and `/rows` serves the data — 10,359 rows in the split, with `overall` occupying a contiguous prefix of roughly the first 400. **Only the `filter` endpoint fails, and it fails with NO `where` clause at all**, so the query was never the problem. A remedy exists and is bounded: read `/rows`, stop at the first row outside the board, four or five requests, with `parse_arena`'s category filter and the source's `minimum_rows` floor behind it. NOT APPLIED — it requires rewriting `test_filter_page_cap_exhaustion_fails_loudly_without_rows_fallback`, the citing test of security finding W-007, and modifying a security-invariant test is an escalate-now item. Prepared, verified working, then reverted so the tree stays clean. **Owner ruled 2026-08-21 to leave it open while nothing deploys.** Owning milestone: **M9**. Note for whoever takes it: `minimum_rows` for arena is currently **1**, which cannot catch a short read, and should move with the fix. **RESOLVED 2026-08-22 — the source was restored and it was never down.** The owner authorised closing the leftovers. The client now reads the board as the ordered PREFIX of `text/latest` from `/rows`, stopping at the first row outside `overall`: **394 rows fetched live, all `overall`, 394 models parsed.** The build exits **0** with no operator actions, and **all NINE surfaces answer for the first time** — `assistant` had been blind for the whole of M8. **W-007's invariant is preserved, not weakened.** W-007 removed an AUTOMATIC fallback: a filter failure silently began paginating everything until the client rate-limited itself. This is a deliberate read of a different endpoint, under the same page valve, that stops at the board's own end — four or five requests. The three tests citing W-007 were RE-EXPRESSED rather than deleted: each still asserts that a failure of the primary read aborts loudly and touches no other endpoint, with the forbidden endpoint now being the other one. `minimum_rows` moved from **1** — which could not catch anything — to **250**, because the stop condition trusts an ordering upstream does not owe us, and that floor is what turns a wrong guess into a failed dependency instead of a silently thin surface. The tests also caught a real defect on the first run: the API wraps each record as `{"row_idx": N, "row": {...}}` and the category was being read off the outer object, so the prefix ended at row zero and every board came back empty. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** M9 established arena had never actually been down — only its `filter` endpoint fails, and with no `where` clause at all. `/rows` restored all nine surfaces and `runner`'s surfaces section passes. This row stood for three milestones describing an outage that did not exist. |
| W-025 | Owner's out-of-sandbox closure run | 2026-08-17 (M6 closure) | `.venv` vs `.github/workflows/*.yml` | FIXED | **The milestone's out-of-sandbox verification runs on a Python version no CI leg covers.** The local `.venv` is **Python 3.14.0**; CI runs 3.12 on five legs and 3.11 on one; `pyproject.toml` declares `requires-python = ">=3.11"` (a range on purpose, seed C.2) and mypy pins `python_version = "3.11"`. Nothing is violated — 3.14 satisfies the range and the suite is green on it — so this is recorded as a GAP, not a defect: **A0.5 makes the owner's run the authoritative one, and that run is the only one on 3.14, so a 3.14-only regression would be caught by nobody but him, while a 3.14-only PASS would be caught by nobody at all.** Two honest options for M7: add a 3.14 leg to `ci.yml` so the owner's environment is represented, or pin the local venv to a CI-covered version. Not decided by the agent: which way this goes is a toolchain-policy call (V3C-65 pins the toolchain in CI, and the range in `pyproject.toml` was a deliberate seed decision). Owning milestone: **M7.** **RESOLVED 2026-08-21.** `.github/workflows/ci.yml` runs the suite on a matrix of 3.12 and **3.14** with `fail-fast: false`; lint and mypy stay on 3.12 because mypy is pinned to 3.11 semantics and the type leg belongs on a stable pair. The gap was real and specific: under A0.5 the owner's out-of-sandbox run is the AUTHORITATIVE one, and it happens on the single interpreter no leg covered. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** CI now runs `['3.12', '3.14']`, so the local `.venv` at 3.14 is covered by a leg. Verified against `.github/workflows/*.yml`. |
| W-023 | M6 Stage-4.0 re-review | 2026-08-17 (M6 closure) | `advisor.db`, `owner_advisor.db` | FIXED | **The repository's own evidence artifacts are pre-M5 schema and cannot be served.** Neither has the `effort` column M5 added, and the `/v1` path opens the database READ-ONLY by design, so it cannot migrate them. Before the startup probe added at this gate, the process booted, answered `/health` with 200 and a build stamp, and returned 200 with zero picks for every real query — and **Stage 4.3 verifies deploys with `/health`**, so it would have reported that deploy healthy. The probe now refuses to boot and names the command. **Owner action, one line, before any deploy:** `python -m app.workflows.schema migrate --db advisor.db` — expect exit 3 with `required_operator_actions` if it carries roster links (D-120), then re-ingest the rosters and expect exit 0. Owning milestone: **M6 closure / M7 deploy.** **CLOSED 2026-08-18 at M7-W1, and NOT by the migration this row proposed.** The one-line
`schema migrate` remedy recorded here was necessary and insufficient: it can add a column, but it cannot populate
`px_median`, and an artifact with an empty `px_median` answers every query 200-with-no-picks — the same silent
failure by a different route. The root cause was that **no production code built the artifact at all**; the pipeline
lived only as a heredoc in a CI cron that had never fired. M7-W1 lifted it into `src/app/workflows/build.py`, and
`advisor.db` was rebuilt through that entry point: **73 models, 72 price medians, six sources, `effort` column
present**, read back from the file rather than reported by the writer. Serving it returns 3 picks on `coding` and 3
on `agentic-coding`, where the shipped artifact previously returned 0 on both. Arena remains absent (**W-024**) and
`assistant` discloses that it has no evidence, per **D-121**. The citing test is
`test_api_config.py::test_the_repositorys_own_artifact_is_checked_not_assumed`, which was **inverted** at this wave:
it used to assert the artifact was broken, went red the moment it was fixed, and now asserts it is servable. |
| W-022 | Self-audit during the M6 Stage-4.0 fix round | 2026-08-17 (M6 closure) | the author's fault-injection harness (scratchpad, not in-repo) | FIXED | **Some fault-injection results this milestone measured STALE BYTECODE, not the mutant.** The harness writes a mutant, runs pytest, then restores the original; Python caches `.pyc` by mtime and size, so a restore inside the same mtime granularity could leave the MUTANT's bytecode cached — or the ORIGINAL's, masking a live mutant. Found while debugging a test that failed against source visibly containing the fix: `inspect.getsource` showed the guard and the running code did not have it. **Measured consequence: with `python -B` (no bytecode written), W3's re-run went from 18/18 to 17/18** — one result previously reported as killed was not. All harness scripts now pass `-B`, and the milestone's key invariants were re-verified clean: **10 of 10 killed with caching disabled**, covering Ruling A, the publication allowlist, the environment and database fail-closed checks, the startup validator's wiring, the YAML guard on both the local and remote inputs, the roster window, and D-120's exit code. **This is the same defect class the milestone's own lesson names:** a control that appears to run and does not — this time the control was the author's own verification. Owning milestone: **M6, fixed here.** |
| W-021 | M6-W4 deploy-readiness walk | 2026-08-17 (M6-W4) | `.github/CODEOWNERS` | FIXED | **The K.10 ownership boundary assigns nobody.** Every rule in the file owns its path to `<DEVOPS_HANDLE>`, which is not a GitHub handle, so the pattern matches and the review request goes to no one. The file's own Stage-0 note says a solo repo should DELETE those lines rather than leave a fake owner, *"because an unmatched/invalid owner assigns no one"* — so this has been documented and untrue since bootstrap. **Same class as W-016, GPF-003 and the wiring defects this milestone found four times: a control that reads as installed and does not run.** It matters now rather than before because W4 just created the first two files those rules point at (`Dockerfile`, `fly.toml`). Not deleted by the agent: removing a K.10 boundary is the owner's call. Two honest options, both recorded in the file — delete the rules for a solo repo, or set a real handle AND enable "Require review from Code Owners", without which the file is advisory whatever it says. Owning milestone: **M6 closure.** **RULED AND CLOSED 2026-08-17 by the owner at the closure gate: DELETE the rules.** This is a solo repository with no DevOps team, which is the case the file's own Stage-0 SETUP note already prescribes deletion for. The alternative was rejected for naming a review no second person exists to perform, and a handle without "Require review from Code Owners" would have replaced an inert control with an inert control that looked better. **K.10's boundary is relocated, not abandoned:** AGENTS.md §5 forbids agents overwriting DevOps-owned build/deploy files and `conformance/test-governed-paths.py` checks the governed globs — both of which RUN, which this file never did. The deleted rules and the reasoning are preserved as a comment in `.github/CODEOWNERS` so restoring them needs no archaeology. Status: **FIXED.** |
| W-019 | M6-W2 fresh-eyes review | 2026-08-17 (M6-W2) | `scripts/check_records.py` rule `L1` | ACCEPTED | **The English-only gate detects an alphabet, not a language.** `L1` fires on Turkish-specific letters, so Turkish written in pure ASCII passes it silently. Measured: after D-118's migration was declared complete and `make check` was green, four ASCII Turkish fragments were still shipping in live user-facing strings, one of them rendering a single sentence in two languages. **The migration had followed the gate's signal and stopped exactly where the gate stops** — and three tests were left pinning the survivors in a file whose exemption D-118 had just removed on the grounds it carried no Turkish. The four are now translated and the tests updated; what is ACCEPTED is the residual detection gap, not the strings. No mechanical fix is proposed here: a word-list would be the denylist class this milestone has already been caught by twice, and widening `L1` is a gate-definition change and therefore the owner's. The honest guard today is the tests asserting exact English sentences, plus review. Owning milestone: **M6 closure**, where the owner rules on whether a stronger check is worth its false positives. **RESOLVED 2026-08-21 — mitigated locally and handed back generally.** `tests/unit/test_language_of_shipped_strings.py` parses every string literal under `src/` and fails on whole-word ASCII Turkish, with a conservative list chosen so a hit is a hit (ambiguous words like `bir`/`de`/`ile` deliberately excluded — a guard that cries wolf gets deleted). Proven to fire on `"Bu model secim icin uygun degil"`, which `L1` reads as clean. Scope stated in the module: a tripwire on the likely forms, not a language detector. The general defect is GP's shipped rule and is filed as **GPF-006**. |
| W-018 | `docs/plans/m6-wave-2-close.md` row 3 | 2026-08-17 (M6-W2) | K.7 / V3C-78 | ACCEPTED | **M6-W2 has not had its fresh-eyes review yet.** Dispatched to the seat that reviewed W1, which authored none of this code either; the wave is held OPEN rather than closed while it runs. Recorded rather than left implicit because W1 measured what this control is worth: two BLOCKING findings, one of them in the guard protecting the milestone's central contract, in a wave whose gates were already green. Second occurrence of a review pending at wave end; under `C2b` a third sends the control itself for review, which here would mean questioning the cadence rather than the seat. Owning milestone: **M6-W2.** **RESOLVED 2026-08-17.** The review ran and returned **BLOCKING** (2 BLOCKING / 8 MINOR); the re-review closed both and the wave. Neither BLOCKING was findable by the author: one was a migration that had followed the GATE's signal rather than the policy, the other was this wave's own thesis holding for two renderings and failing for the third. **RECLASSIFIED 2026-08-22 (M11-W3 ledger audit): ACCEPTED as history, superseded by D-133.** Same shape as W-016: an M6 wave whose review is a fact about a conversation. The remedy is the rule, not a review written four milestones later from a diff nobody can reconstruct. **C2b-reviewed: D-137 @3** (2026-08-25, M12-W5) — V3C-78 reached three acceptances riding the same rows as K.7 and had NOT been reviewed: D-133 settled who reviews, never when. D-137 is V3C-78's own outcome. |
| W-017 | M6-W1 implementation | 2026-08-16 (M6-W1) | `src/app/workflows/rank.py:143-170` | FIXED | **The recommendation engine writes while serving a read.** `build_price_medians` runs `DELETE FROM px_median` + `INSERT` on every `recommend()` call, so the engine cannot be driven from a read-only handle. M5's closure security review recorded this as NOTE-7/MINOR-3 and correctly judged the CLI's read-write handle appropriate; an HTTP surface changes that judgement, because serving read-write would let an anonymous GET rewrite the operator's table while `schema.connect()` migrates the file underneath it (W-009). W1 **contained** it without touching the engine, as the signed plan requires: the file is opened read-only and copied into an in-memory database per request (`adapter/main.py:serving_snapshot`), so the operator's bytes are never written — asserted by `test_the_api_never_writes_to_the_database`. Accepted for W1 because the real fix is engine work the plan forbids this wave, and the containment is proven rather than assumed. **AMENDED 2026-08-17 after the W1 security pass: this is a DENIAL-OF-SERVICE VECTOR, not a cost line.** The containment copies the entire operator database into memory on **every unauthenticated GET**, with no auth, no rate limit and no size cap; measured at 20 concurrent requests against a 51 MB database → **~1.1 GB RSS from ~120-byte requests, roughly 450,000× amplification**. Not exploitable at today's ~1 MB, and rate limiting is the fairness class so no fail-direction rule is broken — but the original wording ("its cost grows with the database size") would have let M6-W3 fix the engine write, close this row, and leave the amplification standing. **It becomes BLOCKING at Stage 4.3 deploy readiness** and may not be closed by the engine fix alone: closing it requires either a bound on the snapshot or a control in front of the surface. Owning milestone: **M6-W3**, alongside W-009's two migration entry points. **NOT FIXED IN W3, and the W3 security pass ruled the deferral DEFENSIBLE with three conditions — all three adopted here.** Its reasoning: the remedy is outside the wave's permission envelope (bounding it means changing `rank.py`, which the signed plan forbids W1-W3), rate limiting is the FAIRNESS class which fails OPEN under V3C-33/45 so no fail-direction rule is broken, and W3 did not widen the surface. **Condition (a): the status is no longer ACCEPTED.** That is the word W-005 carried for two milestones, and what it produced was a guard installed on three theoretical inputs and not on the one real one. **Condition (b): the Stage-4.0 closure pass must RE-DERIVE the amplification independently** rather than cite W1 or W3 — the honest answer to "who decides this severity" is a second measurement, not a stronger adjective. The W3 pass already did this once and got ~9,100x at today's 761 KB, the same line as W1's 450,000x at 51 MB. **Condition (c): no go-live before Stage 4.3.** **ALL THREE CONDITIONS CLOSED 2026-08-17, and the Stage-4.0 pass re-derived the number itself: ~47,000x**, measured three ways, with the 50x spread against W1 and W3 explained as methodological — they divided the FILE size, it measured resident memory at 2.09x the file. **Its ruling turned on a different quantity, and that is the part worth keeping:** the per-byte ratio is dominated by database size, an operator variable an attacker cannot move, which is exactly why two honest passes derived numbers 50x apart. The quantity that decides survival is the CEILING — `concurrency x RSS factor x file size` — and every term of it was previously undeclared: concurrency was AnyIO's default 40 that nobody chose, the VM size was absent so Fly's 256 MB default applied, and nothing measured the file. **(a)** `validate_startup_config` now stats the database and refuses to boot past a budget DERIVED from the other two terms (`max_database_bytes()`); at today's 8 requests / 256 MiB that is 15.25 MB against a 1.02 MB database, 15x headroom, and a data refresh that outgrows the machine now fails at DEPLOY rather than under load. **(b)** `fly.toml` declares `[[vm]] memory` and ties the three numbers into one arithmetic, with a test that fails if the edge cap and the process cap drift. **(c)** the thread limiter is set in the app's lifespan, read back inside a running loop by its citing test — `fly.toml` had been asserting `hard_limit = 8` as "W-017's containment" while the process ran 40. Six mutants, six killed. **This row stays ESCALATED rather than FIXED: the snapshot itself is unchanged and the engine still writes while it reads.** What closed is the unboundedness, not the copy. **CLOSED 2026-08-18 at M7-W2/W3, by DELETION rather than by a bound.** M7-W2 moved
`build_price_medians` out of `recommend()` into `app.workflows.build`, so the engine no longer writes while it reads —
proven value-for-value: 72 model medians identical and all nine recommendation shapes byte-identical against a
pre-change baseline, now computed over a READ-ONLY connection, which was impossible before. M7-W3 then deleted
`serving_snapshot` and every constant that existed only to size the copy (`RSS_FACTOR`, `MEMORY_BUDGET_MB`,
`PROCESS_BASELINE_MB`, `max_database_bytes()`), plus the boot-time database-size ceiling. **Measured after the change:
30 additional requests grew peak RSS by 0.64 MiB against a 0.93 MiB database, where a copy per request would have added
roughly 28 MiB**, and the file is byte-identical after serving. The amplification is not bounded, it is gone, so there
is no ceiling left to tune. Citing test:
`test_api_config.py::test_w017_is_closed_by_deletion_not_by_a_bounded_copy`, which asserts the mechanism rather than
the name — it parses the adapter and fails on any `backup()` call or `:memory:` connection, so a snapshot reintroduced
under a different name is RED. **One self-inflicted lesson recorded with the close:** deleting the three budget tests
also deleted a guard on the concurrency/edge agreement, which is a LIVE control unrelated to snapshots, and two
mutants walked through the gap until it was restored. |
| W-015 | `make bootstrap-check`, first run after the v5.0 install | 2026-08-16 (D-113 migration) | `scripts/bootstrap-check.sh` (GP-owned) | FIXED | v5.0's placeholder repair reports **5 failures on a tree the v4.3.1 checker passes** — isolated by running both scripts against the identical unchanged tree, so the regression is in the checker, not in this project's files. Every match is notation inside prose: `<pkg>` in the body of **D-001, a UNIVERSAL ADR that GP ships and forbids editing**, in GP's own `main.py` seed docstring, in a README directory diagram, and in a commented-out lint rule. A project can therefore only go green by editing files it is not allowed to edit. **Not blocking here** — Stage 0 closed at M1, and `bootstrap-check` is in neither `make check` nor `make gate` — but it blocks any project bootstrapping on v5.0 today. Written up as **GPF-003** in `docs/gp-field-findings.md` with three ranked remedies. Owning milestone: **M6 (carried as a GP finding, not project work).** **RESOLVED 2026-08-21 — HANDED BACK, not fixable here.** Filed as **GPF-003** in `docs/gp-field-findings.md`: the placeholder repair cannot tell a placeholder from notation, and every match is inside prose in files GP ships and forbids editing. A project can only go green by editing files it is not allowed to edit, so the remedy is GP's. Carried in the field-experience report as well. **FIXED 2026-08-22 (M11-W3).** `make bootstrap-check` now PASSES (0 fail / 1 warn). The count had grown from 5 to 7, and all 7 were legitimate: `` `<artifact>.refresh.json` `` naming a record's filename pattern, `` `src/<pkg>/clients/` `` in a UNIVERSAL ADR that must stay generic, `--db <path>` inside a command an error message tells an operator to run, a directory tree in a fenced block, and a commented-out example in `pyproject.toml`. **Rewriting those to suit the checker was considered and rejected** — a universal ADR is generic on purpose and an error message naming `<path>` is telling the reader to substitute one. The v4.3.2 widening that caused this was itself a correct repair (lowercase template stubs were being reported filled), so the fix keeps the widening and narrows the SCOPE: a new `prose_only` helper strips fenced blocks, whole-line comments and inline code spans before both C1 and C3 scan, because the question those checks ask is whether a template stub was left in the PROSE. Applied to BOTH scans deliberately — two scans asking one question in two places is how a fix lands on only one. Proven in both directions: a bare `<one-paragraph summary>` appended to `docs/prd.md` still BLOCKS; the same text inside backticks passes. Handed back to General_Pipeline as GPF-007, since `scripts/bootstrap-check.sh` is GP-owned and every project running v5.0 has this. |
| W-014 | `conformance/test-git-authority.py`, first run after the v5.0 install | 2026-08-16 (D-113 migration) | `docs/HANDOVER-model-ranking.md:180,186` | FIXED | The Cowork-era handover instructs an agent to set a git identity, amend commits, and hand the owner a pull-and-push pair — doctrine that **D-114 superseded on the same day**. Line 186 is prose about workflow files and is a false positive; the surrounding instructions are not. Correct remedy is a superseded-banner at the top of the document rather than a rewrite, because the document is a record of how that environment actually worked. Owner's call. Owning milestone: **M6, before W1.** **RESOLVED 2026-08-16 — owner ruled BANNER.** Two banners added, no content rewritten: one at the document head naming D-113/D-114, and a section banner under §7 stating that an agent must not follow it. The section banner is also what clears the check — `test-git-authority.py` is section-aware by design (it reads the nearest heading so that a rule may name the command it bans), so the ban now sits in the heading above the instructions it supersedes. **FIXED 2026-08-22 (M11-W3) by owner ruling: DELETED.** `docs/HANDOVER-model-ranking.md` is removed from the working tree. It already carried a SUPERSEDED banner and an explicit *"do not act on it"* note at the two flagged lines, so the hazard was mitigated in place — the owner's call was that a document instructing a future agent to set a git identity, amend commits and hand over a pull-and-push pair should not be in the repository at all, mitigated or not. **Four records still link to it** — `docs/handovers/handover_m5-start.md`, `docs/handovers/handover_q1.txt`, `docs/process-log.md` and this ledger — and those links are now dead. They are NOT rewritten: they are records of what was true when written (GPF-001), and this row is where a reader following a dead link finds out what happened. The file remains in git history and is recoverable with `git show 73edfa7:docs/HANDOVER-model-ranking.md`. Verified before deleting that `check_records` still passes across 60 records. |
| W-001 | gitleaks `generic-api-key` | 2026-08-15 (M3-W0) | docs/reviews/m2-security-review.md:8 | FIXED | False positive: the match is the ADR compliance label (D-101) directly following the word "APIs" in prose — zero entropy, present since the M2 commit. Escalated to the owner same-day per AGENTS.md §3 (agents never waive scanner findings). Proposed remedy for the owner's M3 closure session: a scoped `.gitleaks.toml` allowlist for the ADR-label pattern — owner decision, owning milestone M3. (Closure security review found this ledger row's own earlier wording re-tripped the same rule by quoting the literal; reworded 2026-08-15 — description, not quotation.) | **M4 closure update:** this warning SURVIVED its owning milestone's close (M3) — the rule says it may not, and the honest reason is that the remedy is an owner decision an agent may not take (AGENTS.md §3: agents never waive scanner findings). Re-escalated at the M4 gate with the exact allowlist stanza written out in the closure report; owning milestone reassigned to **M4 closure (owner action)**. M4 added no new gitleaks finding and the milestone's scan is otherwise clean. **RESOLVED 2026-08-17 — the owner ratified the allowlist and the scan is clean: `no leaks found`.** The stanza is scoped to the LABEL SHAPE (`D-\d{3}-compliant`), not to a path: allowing `docs/reviews/**` wholesale would have blinded the scanner to the files most likely to carry a real leak, since they quote tooling output verbatim. **Why it took five closes is the part worth keeping:** the finding was diagnosed correctly on day one and the remedy never changed. What blocked it was that the remedy is a WAIVER, agents may never waive a scanner finding, and every closure session filled up before the owner reached a two-line decision. **A control whose only remedy requires the owner is a control that will survive every close until the owner is asked for a decision rather than shown a status** — and it self-propagated meanwhile, because each record documenting it re-triggered the rule **RESOLVED 2026-08-22 (M11-W3 ledger audit).** `.gitleaks.toml` exists in the repository root and `make secrets` reports `no leaks found` at this tree. The owner's scoped allowlist landed; the row was never closed. Verified by running the scanner, not by reading the config. |
| W-002 | M4-W4 re-review (fix-delta pass) | 2026-08-15 (M4-W4) | src/app/workflows/subscribe.py:286 | ACCEPTED | `equivalent_plans` flattens to a name list, so with two or more groups a machine consumer cannot tell which pick each plan is equivalent to, or at what price. Prose carries the structure; the field does not. Accepted for M4 because the fix is a CONTRACT shape that should be decided once, together with the HTTP API surface. Owning milestone: **M6** (API contract wave). **Re-assigned 2026-08-15:** this row was written assuming M5 would be the API milestone; the owner then set M5 = coding-category rescue, so it moves with the API. Re-assignment, not a second deferral — the reason it is not fixed is unchanged. **RESOLVED — and it was resolved at M6, not today.** `src/app/workflows/subscribe.py:108` carries REQ-REC-014 with the structured shape, and `tests/unit/test_subscribe.py` and `test_serializer_parity.py` assert it. **The work shipped two milestones ago and this row was never closed.** That is the ledger failing in the opposite direction to its usual one: not a record claiming a control that is absent, but a record denying a control that is present. Four rows in this ledger were in that state simultaneously (W-002, W-005, W-008, W-010), so for three milestones the project has been over-reporting its own open risk. |
| W-003 | M4 security review MINOR-2 | 2026-08-15 (M4 closure) | src/app/workflows/subscribe.py | FIXED | M5-W4 carries `last_verified` from the exact selected plan-model row into `PlanRank`/`PlanPick` and emits a separate stale-roster notice with plan, date, and URL. Unselected roster links stay silent; the data-owned 30/31-day boundary is tested in `tests/unit/test_rosters.py`. |
| W-004 | M4 security review MINOR-3 | 2026-08-15 (M4 closure) | src/app/workflows/schema.py | FIXED | M5-W4 exposes only the explicit operator command `python -m app.workflows.schema migrate --db PATH`; missing/unusable files exit 2, legacy rows survive, and reruns are idempotent. Read-only recommendation/coverage commands still never migrate. Unit and real module-entry tests cite the command. |
| W-005 | M4 security review MINOR-5 | 2026-08-15 (M4 closure) | src/app/workflows/plans.py:134 | ACCEPTED | `yaml.safe_load` blocks code execution but not alias expansion; a hostile curated file can exhaust memory (measured: MemoryError in ~10 s under 1 GiB). Accepted because both YAML inputs are repo-committed data with no untrusted producer today. Owning milestone: **M6** — the API surface is what changes this boundary, and the remedy (size/expansion guard) ships with it. **Re-assigned 2026-08-15** for the same reason as W-002: M5's scope became the coding rescue, and this row's trigger is the API. **RESOLVED at M6, closed 2026-08-21.** `src/app/workflows/yaml_guard.py` provides `safe_load_bounded` and BOTH YAML entry points call it — `plans.py:135` and `rosters.py:100` — with `tests/unit/test_yaml_guard.py` covering it at 100%. Row left open by oversight; see W-002 for the pattern. |
| W-006 | M4 quality gate / M4-W4 record §5 L-3 | 2026-08-15 (M4 closure) | src/app/workflows/subscribe.py | FIXED | M5-W4 adds REQ-REC-013 and proposed D-111: `excluded_by_budget` counts scoreable pre-cap rows removed by price and `budget_notice` narrates that count without mixing unscored plans or D-110 equivalence. A six-scoreable fixture and real Epoch/DeepSWE CLI path both assert five under `dusuk`. |
| W-007 | Owner verification run, M4 closure session | 2026-08-15 (M4 closure) | src/app/clients/arena.py | FIXED | M5-W4 removes the full `/rows` fallback. Filter 500, exhausted 429 retries, and page-cap exhaustion abort only the Arena source loudly and tests prove the `/rows` endpoint is never touched, preventing the self-rate-limit chain. |
| W-008 | M5-W4 fresh-eyes review (MINOR-2) | 2026-08-16 (M5-W4) | src/app/workflows/subscribe.py:256 | ACCEPTED | The roster-link staleness sentence reads its window from `plan_config.staleness_days`, i.e. the CURATED PLAN table's clock, while `data/rosters.yaml` declares its own `staleness_days` and `rosters.py` ages roster files against it. Both are 30 today, so nothing is wrong on shipped data; the moment they diverge the product will call a roster link stale that the roster's own policy calls fresh, and every gate stays green. Accepted for M5 because the remedy is a persisted roster-window config (a schema change) and this wave was already a HIGH-risk migration wave. Owning milestone: **M6**. **RESOLVED at M6, closed 2026-08-21.** `src/app/workflows/subscribe.py:291` reads the ROSTER's own window and names W-008 in place; `tests/unit/test_roster_window.py` proves the two windows can diverge and that the correct one is used. Row left open by oversight; see W-002. |
| W-009 | M5-W4 fresh-eyes review (MINOR-4/MINOR-6) | 2026-08-16 (M5-W4) | src/app/workflows/schema.py:240 | ACCEPTED | Two migration entry points: the public `migrate()` (SAVEPOINT) is now called by no production path, because `connect()` and `main()` both call `_migrate` inside their own `BEGIN IMMEDIATE` — so the atomicity the tests exercise is not the transaction production runs. Related: the `roster ⇒ last_verified NOT NULL` invariant is enforced at READ time in the recommendation rather than at the write boundary. Both fail safely today (loud `ValueError`, exit 2). Accepted rather than patched at close because `migrate` is a K.8 frozen contract name. Owning milestone: **M6**. **RESOLVED at M6-W3, closed 2026-08-21.** `src/app/workflows/schema.py:253` — `migrate()` is now the ONLY migration entry point and the one production runs, so the atomicity the suite proves is the transaction that ships. The second entry point is gone rather than documented. Row left open by oversight; see W-002. |
| W-010 | M5 closure, live-data probe | 2026-08-16 (M5 closure) | src/app/workflows/registry.py:156 | ACCEPTED | `resolve_effort` infers a suffix effort only when the base name canonicalizes to the same model as the full name — correct, and the reason `qwen3.7-max` is not read as an effort. The consequence is that a row whose model has NO registry rule loses its effort too: four live Epoch rows carrying a literal `_high`/`_medium` suffix were stored as `unspecified` while the ingest reported `effort_unknown=0`. REQ-CAN-005 says an undeterminable effort is counted and disclosed, never defaulted — so the counter under-reports. No shipped answer is affected (those rows are also registry-dropped and never reach a ranking). Owning milestone: **M6**, with the counter fixed to count suffix-bearing rows it could not classify. **RESOLVED, closed 2026-08-21.** `src/app/workflows/ingest.py:121` counts a row whose name carries an effort-looking suffix nobody could confirm, so `effort_unknown: 0` now means "none found" rather than "nothing looked" — REQ-CAN-005 satisfied. Row left open by oversight; see W-002. |
| W-011 | V4C-64 attribution check, M5 closure | 2026-08-16 (M5 closure) | git history 57de98e..bc55bd4 | ACCEPTED | **Twelve of the sixteen M5 wave commits are authored as the OWNER'S OWN NAME with `the address you want on commits` as the email** — an agent committed under the OWNER's name, with an unset-git placeholder as the email. AGENTS.md section 5 states the rule this breaks in as many words: an agent commit indistinguishable from the owner's destroys the evidence V4C-06 stands on. Eleven of them do carry a `GP-Agent: Codex` trailer, so the trailer half of V4C-64 was honoured and the identity half was not; the remaining four commits are correctly `Codex <noreply@openai.com>`. NOT rewritten by the closing agent: rewriting another agent's commits to the closing agent's identity would replace one false attribution with a different false attribution, and a 16-commit history rewrite is catastrophe-class under AGENTS.md section 5 regardless of who benefits. Escalated to the owner while it is still cheap — **nothing is pushed yet**, so a rewrite to the correct `Codex` identity costs one command today and a force-push after. Owner decision; owning milestone: **M5 gate**. **RESOLVED 2026-08-16:** the owner ruled RE-AUTHOR — the twelve placeholder-email commits are rewritten to `Codex <noreply@openai.com>` by the owner, before the first push, scoped so that the closing agent's own commits keep their own identity. **RECLASSIFIED 2026-08-22 (M11-W3 ledger audit): ACCEPTED as history, not escalated.** Twelve M5 commits carry the owner's name for an agent's work. That is a fact about git history and it cannot be undone — rewriting published history is forbidden by standing owner ruling, and doing it to tidy a ledger would be the worse error. It stops being an open question and becomes the ORIGIN of V4C-64: agent commits since M11-W1 carry a distinct identity and `GP-Agent`/`GP-Task` trailers, which is the fix this row actually earned. |

| W-035 | M8 category calibration, measured on the ranked population | 2026-08-19 (M8-W1) | `src/app/workflows/categories.py` (`expert`, `mathematics`) | ACCEPTED | **Two of the nine categories rank on benchmarks that can no longer separate their top models.** On GPQA the twelve highest-scoring PRICED models span 1.8 points against the board's own 2.52 standard error; on AIME three models tie at exactly 100.0 and the top twelve are all at or above 97.7. The engine handles this honestly today — `close_call` fires, the surface says the models are level with the leader, and Best Value picks the cheapest among genuinely tied candidates, which is the useful answer. What is NOT handled is the trend: a benchmark at its ceiling cannot record next quarter's improvement, so these two surfaces will get less informative on their own, with every gate green and no signal anywhere. This is the owner's criterion (*"data that cannot be updated is garbage"* — owner, translated from Turkish) arriving from an unexpected direction: the data updates fine, it just stops discriminating. Accepted for M8 because the remedy is a SECOND benchmark per category and a rule for when a board is retired — a category-layer design change, not a threshold edit. Owning milestone: **M9.** **RE-ASSIGNED to M10 on 2026-08-22.** This row named **M9** as its owning milestone and M9 closed without addressing it. Recorded rather than quietly renumbered: the ledger's own rule is that a warning may not survive the close it was raised in, and six rows survived this one — which is the same failure this ledger already recorded in the opposite direction at M8, when five rows stayed open after their work had shipped. The owner is the milestone that will do it, not the milestone that hoped to. |
| W-036 | M8-W1, found while fixing eight red build tests | 2026-08-19 (M8-W1) | `src/app/workflows/build.py`, `tests/unit/test_build*.py` | ACCEPTED | **The build's source set is injected by monkeypatching module globals, and each new source KIND silently invalidates every existing test.** Adding `EPOCH_BOARDS` turned eight passing tests red — correctly, because they had been asserting against a source set they did not control. The fix was one more `monkeypatch.setattr` line per test, which works and does not scale: a fourth source kind will do the same thing again, and the failure mode in between is the dangerous one — tests that believe they control the inputs while the real registry runs underneath them. That exact shape has now produced three defects in this project (the `sources` default argument, its repeat in `_ingest_bundles`, and this wave's `boards` parameter that never reached the caller). The structural remedy is a single injectable registry object rather than three module names. NOT done at this wave: under D-122 review depth follows blast radius, and this is a test-plumbing refactor across two files during a wave whose purpose was the category layer. Owning milestone: **M9.** **RE-ASSIGNED to M10 on 2026-08-22.** This row named **M9** as its owning milestone and M9 closed without addressing it. Recorded rather than quietly renumbered: the ledger's own rule is that a warning may not survive the close it was raised in, and six rows survived this one — which is the same failure this ledger already recorded in the opposite direction at M8, when five rows stayed open after their work had shipped. The owner is the milestone that will do it, not the milestone that hoped to. |
| W-037 | M8-W1 calibration, third occurrence of the same question | 2026-08-19 (M8-W1) | `docs/reviews/m8-category-calibration.md` | FIXED | **Thresholds have now been calibrated against the wrong population three times, each time a different wrong one:** CSV rows (204 rows / 59 models on terminalbench), then `parse_board` output, and now the full board where the engine only ever ranks the reconciled AND priced subset — 58 of 521 models on ECI. Each correction was found by measuring rather than by review, and each produced materially different numbers: `expert` and `mathematics` were admitting two thirds of everything above the floor into the value window. The engine's ranked population is not written down anywhere as a definition, which is why the question keeps being answered from whatever data is nearest. Accepted rather than fixed because the remedy is a named helper the calibration work is required to call — the definition made executable rather than remembered. Owning milestone: **M9.** **RE-ASSIGNED to M10 on 2026-08-22.** This row named **M9** as its owning milestone and M9 closed without addressing it. Recorded rather than quietly renumbered: the ledger's own rule is that a warning may not survive the close it was raised in, and six rows survived this one — which is the same failure this ledger already recorded in the opposite direction at M8, when five rows stayed open after their work had shipped. The owner is the milestone that will do it, not the milestone that hoped to. **FIXED 2026-08-22 (M10-W3).** `rank.py::ranked_population` is the name, and the half that mattered is the second one: `scripts/arena_calibration.py` was still computing its cut table and value-window sizing from the raw board — this warning's own defect, alive in the script written to recompute the record. It now reaches the accessor and REFUSES those sections without `--db` rather than falling back. `tests/unit/test_ranked_population.py` carries a gate that fails any future script sizing a threshold without importing it, because the rule as prose is what failed three times. |
| W-038 | M8-W2, iOS client review | 2026-08-19 (M8-W2) | `ios/ModelRankingTests/` (empty), `ios/ModelRanking.xcodeproj` | FIXED | **There is no iOS test target.** The directory exists, is empty, and is not referenced by the project file, so every line of Swift in this product is unexecuted by any gate — including `EngineClient`'s decoding, the error mapping that carries the engine's own words, and now a category strip that drives every request the app makes. The exposure is partly covered from the other side: `tests/unit/test_ios_payload_contract.py` parses `Models.swift` for its `CodingKeys` and asserts the endpoint serves those exact fields, non-null where Swift demands non-null, so a server-side rename can no longer break the app silently. What remains uncovered is the Swift itself — a decoding bug, a state bug in the strip, or a view that crashes on an empty surface would be found by the owner, on a device, and by nothing before that. Accepted for M8 because adding an XCTest target means editing `project.pbxproj` by hand, which is the file that decides whether the app builds at all, and this milestone's remaining scope is the category layer. Owning milestone: **M9**, with the target added before any further Swift is written. **RE-ASSIGNED to M10 on 2026-08-22.** This row named **M9** as its owning milestone and M9 closed without addressing it. Recorded rather than quietly renumbered: the ledger's own rule is that a warning may not survive the close it was raised in, and six rows survived this one — which is the same failure this ledger already recorded in the opposite direction at M8, when five rows stayed open after their work had shipped. The owner is the milestone that will do it, not the milestone that hoped to. **FIXED 2026-08-22 (M11-W2).** `ios/Package.swift` compiles `ModelRanking/Engine` — the sources the `.xcodeproj` builds, not a copy — and `ios/EngineTests/` holds 18 tests wired into `make check` and `runner`. Two findings came out of writing them, and both were in the wave's own new work rather than in the Swift: (1) `TieredRouter` injected its similarity tier and hard-coded `ModelRouter()`, so the one property REQ-RTR-003 guarantees — any tier may be absent and the screen still works — was the one property no test could express on a machine carrying FoundationModels; (2) the `make swift-test` recipe piped into `tail`, so `make` received `tail`'s status and returned **0 with a deliberately broken assertion, with the failure not even visible in the output**. Both proven by execution, fixed, and proven again in both directions. **SCOPE, stated rather than implied:** `ContentView.swift` and `ModelRankingApp.swift` are SwiftUI and remain unexecuted. |
| W-039 | M8-W3, verifying REQ-APP-004 against the running app | 2026-08-19 (M8-W3) | `src/app/adapter/main.py` (`/v1` 503 branch), `docs/plans/m8-plan.md` §1 | FIXED | **The 503 the client is required to render honestly cannot be produced.** `/v1` returns 503 on `UnbuiltEvidenceError`, and REQ-APP-004 names that as one of four failure states the app must state rather than blank. It has no live path: M7 added a startup probe that refuses to boot the process on an unbuilt or pre-M5 artifact (`_database_unusable`), so by the time anything can answer `/v1`, the condition that produces 503 has already stopped the process. The two controls are individually correct and were built two milestones apart; together they make one of them unreachable. Not a defect in either — failing at BOOT is strictly better than failing per request, and that was the deliberate M7 ruling. What is wrong is a requirement claiming a verified failure state that cannot be entered, and a 503 branch in the API that no live request can reach. Either the branch is dead code to be deleted with its test, or the probe should permit boot on an unbuilt artifact so the request-level disclosure is the one that fires — a fail-direction decision (V3C-33/45) and therefore not the agent's. Owning milestone: **M9.** **RE-ASSIGNED to M10 on 2026-08-22.** This row named **M9** as its owning milestone and M9 closed without addressing it. Recorded rather than quietly renumbered: the ledger's own rule is that a warning may not survive the close it was raised in, and six rows survived this one — which is the same failure this ledger already recorded in the opposite direction at M8, when five rows stayed open after their work had shipped. The owner is the milestone that will do it, not the milestone that hoped to. **FIXED 2026-08-22 (M11-W3), and the row's PREMISE was wrong.** This warning said the 503 branch has no live path because the startup probe stops the process before anything can answer. The probe guards BOOT; the request path re-opens the artifact per request, so an artifact that stops being servable AFTER boot answers 503 on the very next request — measured. And it is not hypothetical: **M9 shipped a refresh that replaces that exact file every twelve hours**, so the state this row called unreachable is one the product now enters by design on every publish. `tests/unit/test_unavailable_after_boot.py`. Recorded this way rather than silently closed, because the interesting part is not that the branch is reachable — it is that a warning about an impossible state survived the milestone that made the state routine. |
| W-040 | M8 independent security review, MAJOR-1 | 2026-08-19 (M8-W5) | `src/app/adapter/main.py:_ranking_json` | ESCALATED | **The `ranking` array D-125 added is unbounded, and fixing it properly needs a contract move this milestone can no longer make.** `category_ranking` has no `LIMIT` and no floor filter, so `/v1/recommendations` grew from ~1-2 KB to ~20 KB measured, on an unauthenticated GET with no rate limiter. The only ceiling in the process is `MAX_RANKED_ROWS = 5000`, calibrated for process memory rather than egress; at that ceiling one request is ~3.5 MB. The declaring comment says "up to 44 rows per answer" and nothing enforces 44 — a documented bound with no gate, which is the shape V4C-49 forbids and which this agent wrote. **Why it is not fixed here:** truncating silently is exactly the failure this project refuses (D-121's doctrine, D-111's counted-and-disclosed rule), and disclosing the truncation needs a new payload field — a SECOND `/v1` revision, which D-124 does not permit because D-125 already spent the single one. So the honest options are both the owner's: extend D-124 for one more move in M9, or ship a cap with the disclosure and accept a third revision. Fail direction is OPEN, which is correct for an availability control (V3C-33/45) — but there is no control to fail. Owning milestone: **M9.** **RESOLVED 2026-08-20 — bounded at BOOT, never truncated at serve time.** The owner directed that the four escalations be resolved. Truncating the response was refused for the reason the row states, and disclosing a truncation would need the second contract revision D-124 does not grant — so the ARTIFACT is bounded instead of the response. `MAX_PUBLISHED_RANKING_ROWS` (default 500, env-overridable) is checked in `validate_startup_config` via `_largest_surface_row_count`, which calls `category_ranking` itself rather than mirroring its join, so the bound cannot drift from what is published. An artifact that would emit a larger answer refuses to boot and names the surface. Nothing is ever silently shortened, because the state requiring truncation is unreachable. Largest surface today: 58 rows. **DEFERRED TO THE OWNER'S OWN USE, 2026-08-22 (M11-W3), by his ruling.** Re-measured at this tree: one surface at `budget=unlimited` returns **15.3 KB**. The row is not closed and is not escalated-and-waiting either — the owner ruled that this is a decision he will make from the APP, not from a number: *"I will decide once I see it in the UI; some of the tests I do are going to be decided by using the app"* (owner, translated from Turkish). Recorded this way because a size limit is a product judgement about what a reader is shown, and 15.3 KB is a fact that does not by itself say whether it is too much. Blocked on M11-W3's Simulator session, not on engineering. **RE-SCOPED 2026-08-25 (M12-W3).** This row asked whether the unfiltered `ranking` array is too long, and the owner deferred it to his own use of the app. Half the question has now changed shape: **until M12-W3 there was no budget control at all** (`ContentView.swift` hardcoded `unlimited`), so every judgement he could have made about list length was made against the widest possible list. With `Cheaper` and `Mid-priced` selectable the list he sees is the one his budget admits. The row stays open and stays his — but the thing he will be judging is now the thing the product actually offers. |
| W-041 | M8 independent code + security reviews (both) | 2026-08-19 (M8-W5) | `pyproject.toml:105` | FIXED | **`pytest --cov` runs with no `--cov-fail-under`, which is the structural reason a new module shipped at 32% coverage through a green gate.** `epoch_board.py` — the single reader behind six of the nine categories — reached HEAD with zero tests naming it, every one of an independent tester's fourteen mutants surviving, and `make check` exit 0 throughout. `permission-matrix.md` §11 lists "coverage drop on a touched module" as BLOCKING, and nothing mechanically enforces it anywhere. Total coverage is 88% today and the M8 fix wave took `epoch_board.py` from 32% to 91%, so a floor could be set now without turning the build red. **NOT applied by the agent: adding a gate to `make check` is a gate-definition change** (AGENTS.md §3, escalate-now) and it would fail the build for every future contributor, which is the owner's decision to make and not a side effect of a fix wave. This is the single highest-value item in the M8 review, above any individual finding: it is the control that would have caught the finding. Owning milestone: **M9.** **RESOLVED 2026-08-20 — both halves.** Global floor `fail_under = 85` in `pyproject.toml` against a measured 87.8, deliberately close: a floor with ten points of slack never fires. And the half that matters, because 88% total is exactly what concealed a 32% module: `scripts/coverage_floor.py` enforces a 60% per-module floor with NAMED, reasoned exemptions, and fails on a stale exemption whose file no longer exists. Both run inside `make check` — a gate outside the command people type does not run. **`wave-check-all` was wired in at the same time (closing W-032)**, scoped by `process_version: v5.0` rather than by date so the tool cannot retroactively invalidate the twenty pre-migration records, which is this project's own GPF-001 finding applied to itself; omitting the stamp on a newer record is a failure, not an exemption. **RESOLVED 2026-08-22 (M11-W3 ledger audit), in a different form than proposed.** `pyproject.toml` still has no `--cov-fail-under`, and it does not need one: `scripts/coverage_floor.py` runs in `make check` with per-module NAMED exemptions that fail when they go stale, which is strictly stronger than a single global threshold. The structural hole this row named — a new module reaching HEAD at 32% through a green gate — is closed. |
| W-042 | M8 independent security review, MINOR | 2026-08-19 (M8-W5) | `ios/app.sh:58` | ACCEPTED | **The only documented way to run this product launches the engine with `APP_ENV=test`, which is in `RELAXED_ENVS`** — so `validate_startup_config` logs its problems instead of refusing to boot, and V3C-51's fail-closed startup control is never exercised on the path anybody actually runs. Harmless today (nothing is deployed, and the relaxed lane exists deliberately for local work), and it is how a command gets copied into a deploy script. Accepted rather than changed mid-fix-wave because flipping it would make `./ios/app.sh up` refuse to start until `MODEL_RANKING_DB` and `APP_BUILD` are exported, which is a change to the owner's one working command. Owning milestone: **M9**, together with the deploy decision. **RESOLVED 2026-08-20 — fail-closed consequence without relabelling a laptop.** `ios/app.sh` now runs `validate_startup_config` as a PREFLIGHT and refuses to start on any problem it reports. The substantive checks always ran in the relaxed lane; what was missing was the consequence. This gives the strict outcome on the one command anybody runs without demanding production configuration on a development machine. Verified: an unbuilt artifact now stops `up` with the engine's own remedy sentence. |
| W-043 | M8 independent tester, V3C-02 audit | 2026-08-19 (M8-W5) | `docs/prd.md` REQ-API-010 | ACCEPTED | **REQ-API-010 has no citing test and cannot have one in the form V3C-02 describes.** It requires that a contract gap found by the client is recorded as a finding against `/v1` before any client-side workaround — a PROCESS criterion whose evidence is the ledger and the ADR trail, not an assertion about running code. V3C-02 as written admits no exception ("EVERY acceptance criterion has a citing test"), so the honest state is that this criterion is unverifiable by the rule the project applies to all the others. Two ways out, both the owner's: write the criterion so it has a testable consequence, or record an explicit exemption class for process criteria in AGENTS.md §3.3. Recorded rather than quietly excluded, because an unverifiable criterion that nobody has named is how a checklist acquires a row that always passes. Owning milestone: **M9.** **RESOLVED 2026-08-20 — the criterion was made expressible rather than exempted.** A process criterion can have a failable test once you stop testing the process and test its ARTEFACT. `tests/unit/test_contract_change_provenance.py` cross-checks the frozen field set against the decision record in both directions. **It caught a real defect on its first run**: a false 'the window is UNSPENT' claim still live in `docs/closure-report-m8.md` after a correction pass whose own verification grep was broken. REQ-API-010 is marked MET with a declared PROCESS class in `docs/prd.md`; the general form is proposed to the pipeline as P-8. |
| W-044 | M8 independent code review, MAJOR-3 | 2026-08-20 (M8-W6) | `src/app/adapter/main.py:_answer_for`, `ios/ModelRanking/ContentView.swift` | ACCEPTED | **`/v1` gives two accounts of one query and only the client half is fixed.** `ranking` is the FULL ranking by design (D-125 publishes every ranked model beside the three picks); `picks` are budget-filtered. So `budget=low` on `everyday` reports `eligible_count: 25` and then serves **58** rows whose most expensive model is **$36.09/1M**, with no per-row eligibility marker and no notice. Measured 2026-08-20. **Fixed on the SCREEN, which is where a user meets it:** the "See all N" control now reads *"See all 58 — 25 fit your budget"* whenever the two disagree, computed from two values the payload already carries, so nothing moved and REQ-APP-005 is untouched. **NOT fixed in the payload**, and that is the residue: an API consumer that is not this app still receives 58 rows under a low-budget query with nothing saying they are unfiltered. Marking rows, or adding a count, is a payload field — a SECOND contract revision, where D-124 grants one and D-125 spent it. Same blocked shape as W-040, same two owner options: extend the window in M9, or accept a third revision. Owning milestone: **M9**, and it should be settled together with W-040 since both are one field away. **RE-ASSIGNED to M10 on 2026-08-22.** This row named **M9** as its owning milestone and M9 closed without addressing it. Recorded rather than quietly renumbered: the ledger's own rule is that a warning may not survive the close it was raised in, and six rows survived this one — which is the same failure this ledger already recorded in the opposite direction at M8, when five rows stayed open after their work had shipped. The owner is the milestone that will do it, not the milestone that hoped to. **FIXED 2026-08-22 (M11-W3) by owner ruling (D-134).** The residue this row named — an API consumer that is not this app receiving 58 rows under a low-budget query with nothing saying they are unfiltered — is closed by publishing the cap as a sibling resource, `/v1/budgets`, rather than by a third payload revision. Re-measured at the fix: `everyday`/`low` still serves 58 rows and reports `eligible_count: 25`, and filtering the served ranking by the served cap now yields exactly 25. The row's own framing said this was "one field away" from W-040 and blocked on the same window; it turned out not to need the window at all, because a new resource changes no existing response. |
| W-045 | M8 independent code review, MAJOR-4, found while fixing it | 2026-08-20 (M8-W6) | `tests/unit/test_ios_payload_contract.py` | FIXED | **The payload-contract guard covered one Swift struct out of six, and its own docstring, `ios/README.md`, W-038 and the M8 closure report all described it as covering the app's decoding contract.** `Answer`, `Pick`, `RankedModel`, `SourceHealth` and `SourceRow` — every struct that decodes an actual recommendation — were ungated. Widening it exposed two further defects **in the guard's own parser**, both of the same family it exists to prevent: (1) it searched forward from a struct declaration for the next `CodingKeys` block with no upper bound, so a struct declaring none silently inherited the NEXT struct's field set — `SourceHealth` was being checked against `SourceRow`'s keys; (2) its regex could not see Swift's comma-separated `case model, vendor, score` form, so `RankedModel` parsed as 5 of its 11 fields and `Pick` as 10 of 19. **An under-reading parser does not fail — it EXEMPTS**, which is the more dangerous half, and a mutant deleting `harness` from the published field set walked through it. Both fixed, and the parser now proves completeness: every declared property must yield a key, so the next parser gap is loud instead of silent. Four field-rename mutants and one parser regression all die. |
| W-046 | M9-W2 independent review, MAJOR-1 | 2026-08-22 (M9-W2) | `docs/plans/m9-plan.md` §5.2, `src/app/workflows/refresh.py` (the status record) | FIXED | **The plan's second required decision was never made, and D-130 claims to answer it.** §5.2 asks *"what happens on the SECOND consecutive refusal?"*; D-130 is entirely about `launchd` versus `cron` and missed TRIGGERS, which is a different question. `docs/plans/m9-wave-2-close.md` row 8 asserts all three §5 questions were answered — that assertion is wrong and is corrected here rather than in place. **Live consequence:** a legitimate permanent upstream prune of more than a quarter refuses on every cycle forever, with no counter, no escalation and no relaxation; and the record is **depth-1**, so cycle N+1 overwrites cycle N and the file cannot represent "two in a row" — the exact state §5.2 says the product must not sit in quietly. A transient refusal is also erased within twelve hours, so an owner running `runner` thirteen hours later sees `published` and never learns a refusal happened. Remedy named by the reviewer: `consecutive_refusals` and `last_published_at` in the record, with `runner` escalating at two and reporting ARTIFACT age rather than only cycle age. Owning milestone: **M9-W3.** **RESOLVED 2026-08-22 (M9-W3).** The record carries `consecutive_refusals` and `last_published_at`, carried forward from the previous cycle rather than overwritten, so it can now represent the state plan §5.2 is about. `runner` escalates at TWO in a row and reports ARTIFACT age beside cycle age — the two answer different questions and diverge exactly when it matters: a refresh cycling happily every twelve hours while refusing every candidate looks perfectly healthy on the first number and is serving week-old evidence on the second. The reviewer's separate point stands and is fixed with it: a transient refusal used to vanish within twelve hours, so an owner reading `runner` the next day saw `published` and never learned it had happened. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** The second-consecutive-refusal question is answered in code: `refresh.py` carries `consecutive_refusals` in its status record and `runner` escalates at two. The decision this row said was never made was made in M9-W3 and the row was not updated. |
| W-047 | M9-W2 independent review, MAJOR-3 | 2026-08-22 (M9-W2) | `src/app/workflows/refresh.py` — no lock anywhere in the module | FIXED | **Nothing serialises two refreshes, and the decision is made against a baseline another writer may already have replaced.** Two demonstrated failures. (1) The record's scratch path is a fixed shared name, so two cycles collide on it: the reviewer produced an interleave where one cycle raised `FileNotFoundError` on the rename and **the record left on disk carried the other cycle's payload**. The artifact candidate uses `mkstemp` and the record does not — the same lesson applied once. (2) A hand-run `build.py` published a score of 95.0 mid-cycle; the refresh then published its own candidate (80.0) over it, having made its degradation decision against the pre-build artifact rather than the one it actually overwrote. **A refresh can therefore publish something far worse than what it replaces without the guard ever comparing against it.** Remedy: an `O_EXCL` lock file beside the artifact, re-fingerprint the live artifact immediately before `replace` and refuse if it moved, and `mkstemp` for the record's scratch. Owning milestone: **M9-W3**, and it should land before any scheduler makes concurrency ordinary rather than exotic. **RESOLVED 2026-08-22 (M9-W3).** An `O_CREAT | O_EXCL` lock beside the artifact — the filesystem decides the winner atomically, with no check-then-act window — and the loser reports **BUSY (exit 4)** rather than waiting, because waiting turns a scheduler into a queue and an overlapping trigger is ordinary rather than wrong. The record's scratch path uses `mkstemp` now instead of a fixed shared name, which is the same lesson the artifact's candidate already had. And the second half the reviewer demonstrated: the baseline is RE-READ immediately before publishing, so a cycle that decided against an artifact a human has since replaced refuses instead of overwriting it — the lock stops two refreshes and cannot stop a person running `build.py`. A lock whose holder pid is gone is reclaimed at once rather than after two hours, so a SIGKILL costs no cycle. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** M9's Stage 4.0 raised this to BLOCKING and replaced the whole scheme with `fcntl.flock` — an exclusive advisory lock held by the process, not a path with an age rule. Four references in `refresh.py`. |
| W-048 | M9-W2 independent review, MAJOR-6 | 2026-08-22 (M9-W2) | `docs/prd.md` REQ-REF-001 and REQ-REF-006 | ACCEPTED | **Two acceptance-criterion clauses are marked MET with no test.** REQ-REF-001's plan verification reads *"Fault injection at every stage boundary, including SIGKILL, with the live artifact verified byte-identical afterwards"* — a search of `tests/unit/test_refresh.py` for SIGKILL, `os.kill` or `signal` returns nothing. REQ-REF-006 requires *"a request in flight during the swap completes on consistent data"*; the test asserts only that the NEXT request sees new data. Both clauses are plausible-by-construction — POSIX rename is atomic and an open inode survives replacement — which is exactly why they went unwritten, and V3C-02 does not admit "obviously true" as evidence. Accepted rather than fixed at W2 because both need a subprocess harness rather than an assertion, and the wave was already answering three BLOCKING findings. Owning milestone: **M9**, wave 3. **RESOLVED 2026-08-22 (M9-W3).** Both clauses now have tests that can fail. REQ-REF-001's SIGKILL clause kills a REAL subprocess mid-build — a signal cannot be simulated in-process — and asserts the artifact is byte-identical afterwards AND that the next cycle is not wedged behind the dead one's lock; the second property only became visible once the first was written. REQ-REF-006's in-flight clause is tested at the layer that provides it: a reader opened BEFORE the swap keeps seeing a coherent artifact rather than half of each, which is what POSIX gives while something holds the inode, and what publishing by COPY across a filesystem boundary would take away. |
| W-049 | M9 Stage-4.0 security review, MEDIUM-4 | 2026-08-22 (M9 closure) | `src/app/workflows/refresh.py::degradations`, `src/app/workflows/sources.py` | FIXED | **Nothing bounds what an upstream can publish UPWARD, and unattended operation is what makes that matter.** Every automated defence on served content is a SHRINKAGE detector: the `minimum_rows` floors catch a source going short, and D-128's axes catch surfaces going blind, losing a quarter of their models, or pricing a budget out. All of them explicitly exempt gains and score movement, on the reasoning that a model getting worse is news rather than damage. **The inverse has no guard at all.** An upstream that ADDS fabricated models with high ratings, renames existing ones, or drops prices produces a larger, cheaper artifact — every check reports healthy and it ships to users twice a day. Five remote sources are fetched over TLS with no pinning, no signature and no provenance; attended, a human read the build report, and unattended that report goes to a log nothing reads or rotates. The reviewer's proposed remedy: an upward-anomaly axis — refuse when a surface gains more than X% previously-unseen model names, or when the median blended price moves more than Y% — plus a row-count BAND per source rather than a floor. **This is the most important thing M10 inherits**, because it is the failure mode the refresh created rather than one it exposed. Owning milestone: **M10.** **RESOLVED 2026-08-22 (M10-W2) by D-132.** Two axes, both measured before either threshold was written. A surface where more than a quarter of the models are names this artifact has never seen is refused and NAMED; so is a surface whose median published price moves by more than a quarter in either direction. Names rather than counts, because a count cannot see a roster that was REPLACED rather than resized, and because a real board adds models one or two at a time while an injected set arrives together. The median rather than a price, because one provider cutting a price is news and a whole surface's median moving is a feed. **Ordinary movement, measured between two consecutive live builds: 0% new names and 0.0% median price movement on all nine surfaces** — so a quarter is a wide limit ordinary movement does not approach, which is the right shape when the failure to fear is a product that freezes while every check reports healthy. A surface RETURNING from blind is explicitly exempt: when arena came back `assistant` went from nothing to 65 models and every name was new, and refusing that would have frozen the product on the day it improved. Nine mutants, nine killed, including both directions of each threshold. **RESOLVED 2026-08-22 (M11-W3 ledger audit).** M10-W2 added the upward axis: `refresh.py::upward_anomalies`, bounded at 0.25 in either direction, with the thresholds chosen after measuring ordinary movement rather than before. D-132. |
| W-050 | M9 Stage-4.0 security review, MEDIUM-5 | 2026-08-22 (M9 closure) | `src/app/clients/arena.py::_paginate` | FIXED | **Aggregate allocation across pages is unbounded.** Each page is capped at `MAX_RESPONSE_BYTES` (32 MB) and `_MAX_PAGES` is 50, but `_paginate` accumulates every page into one list and then `json.dumps` copies it: a hostile upstream serving fifty full pages of `category:"overall"` rows drives roughly 1.6 GB of raw JSON into several GB of live objects, on the owner's laptop, unattended, every twelve hours. M9 amplified rather than created this — the server-side filter used to keep the real read at ~400 rows, and paging a 10,359-row split is now ordinary. Not exploitable by anything but the upstream itself, and the page cap still bounds the request COUNT. Accepted rather than fixed at closure because the remedy is a total-rows/bytes cap beside the page cap and this milestone was already answering three BLOCKING findings. Owning milestone: **M10**, with W-049, since both are about what an upstream can do to an unattended process. **FIXED 2026-08-22 (M10-W3).** `_MAX_MERGED_ROWS = 2_000`. Recorded because the first attempt was 5,000, which is exactly `_MAX_PAGES * _PAGE` — a bound placed where nothing can reach it, which is this project's recurring shape and was caught only by trying to write a test that fires it. Adding a reachable row bound then made the PAGE cap unreachable for an all-overall payload; the page cap is kept as the backstop for a raised row bound, and its test raises the row bound to reach it rather than pretending it fires in normal operation. |
| W-051 | M9 Stage-4.0 security review, MINORs N1/N2/N4/N5 | 2026-08-22 (M9 closure) | `runner`, `deploy/com.hcs.modelranking.refresh.plist`, `src/app/workflows/refresh.py` | FIXED | Four small findings kept together because they share one shape — **assumptions about the environment that nothing checks.** (N1) `runner`'s staleness fails OPEN on non-finite and future timestamps: `json.loads` accepts `NaN` and `nan > 24` is False. (N2) `reason` is printed to the operator's console unbounded and unescaped; the reviewer found no fully attacker-controlled path today, but ANSI in the record would render in the console the owner uses to decide. (N4) The plist's absolute paths and log files are never created or verified, `RunAtLoad` is deliberately absent so an install error is invisible for twelve hours, and the err log accumulates a full build report every cycle with no rotation. (N5) The lock, the record, the candidate and the artifact share one directory; today that is the owner-owned repo root and an exclusive-create open does not follow symlinks, so the classic attacks do not apply — but **nothing enforces the assumption**, and V3C-51 asks for exactly that kind of check at startup. Accepted with the reviewer's own remedies named: `math.isfinite` plus a negative-age reject, bound and sanitize `reason` at write time, and one `stat` on the artifact's parent refusing a group- or world-writable directory. Owning milestone: **M10.** **FIXED 2026-08-22 (M10-W3).** `refresh.py::environment_problems` refuses a group/world-writable target directory and a non-finite or non-positive clock, and `write_status` substitutes the wall clock when the one it was handed is not finite — found because the refusal record for a NaN clock crashed while serialising the NaN it was reporting. Six mutants over the delta, six killed. |
| W-052 | M10 Stage-4.0 security review, BLOCKING-1 | 2026-08-22 (M10 closure) | `src/app/workflows/schema.py::open_readonly` | FIXED | **`f"file:{path}?mode=ro"` returns a WRITABLE connection.** Four path shapes were measured and all four wrote into the artifact they were supposed to be reading. It is recorded even though it is fixed, because the fix already existed: `adapter.main.open_readonly` carried the correct construction and a docstring naming this exact defect from M6, and `workflows.refresh` rebuilt the bug three milestones later under a comment arguing the copy was "not a second definition of any project behaviour". **A fix that lives in one module is not a fix.** Now one definition in `app.workflows.schema`, three callers, and an `ast` gate in `tests/unit/test_readonly_uri.py` that refuses any `file:`-prefixed f-string in `src/` or `scripts/`. |
| W-053 | M10 Stage-4.0, MINOR N1/N2 | 2026-08-22 (M10 closure) | `scripts/arena_calibration.py`, `src/app/workflows/schema.py::open_readonly` | FIXED | Two small ones kept together. (N1) the calibration script parses `--db`/`--category` by hand instead of with `argparse`; it is a developer tool and the failure mode is a confusing message, not a wrong figure. (N2) `open_readonly` resolves symlinks, which is correct where an operator names the artifact and is a property the next caller must read rather than discover. Accepted rather than fixed to keep the closure delta to the blocking finding. Owning milestone: **M11.** **FIXED 2026-08-22 (M11-W3).** (N1) `scripts/arena_calibration.py` now parses with `argparse`. The hand-rolled version worked and was replaced anyway: it accepted `--db` twice, silently ignored an unknown flag, and would have read `--category` as a positional given a missing value. None of those produces a wrong FIGURE — but a calibration tool that quietly does something other than what was typed is the wrong tool to be relaxed about, in the file whose own history is W-037. Verified: an unknown flag is now an error rather than silence. (N2) `open_readonly`'s symlink resolution is stated in its docstring as a property with the context it is correct in and the context it would not be, so the first caller that arrives with an untrusted path makes a decision instead of a discovery. |
| W-054 | M10 Stage-4.0, MINOR N3 | 2026-08-22 (M10 closure) | `deploy/com.hcs.modelranking.refresh.plist` | ACCEPTED | **Every verdict in the M10 security review describes CODE, not operation.** The 12-hour refresh has still never run unattended, so no refusal, no lock and no anomaly threshold has been exercised by anything except a test. `launchctl load` is one owner command and is deliberately deferred until the coding is done (owner ruling, 2026-08-22). Same class as W-030/W-031: a control that has never run in the shape it ships in. Owning milestone: **M11.** **NARROWED 2026-08-24: the refresh has now RUN, and published.** `launchctl kickstart` executed it through launchd with the plist's own environment and redirection — the production path, only the trigger was the owner's. It published: `outcome: published`, exit 0, reason *"the served content changed"*, nine surfaces answering, and the artifact was replaced beneath a running engine without breaking it (`make check` green afterwards on the new artifact, 714 passed). **Every refusal, lock and threshold this project built at M9 and M10 has therefore executed for real at least once.** What remains is narrower than what this row opened with: two firings ON THE 12-HOUR SCHEDULE have not been observed. Owner ruling 2026-08-24, translated from Turkish: *"let it trigger itself, why wait 12 hours — trigger it whenever you need it now, we'll test the 12-hour schedule later, it's not that important."* Owning milestone: **M12.** |
| W-055 | M10 closure, V4C-13 control-bypass telemetry | 2026-08-22 (M10 closure) | `docs/plans/m10-wave-4-close.md` row 3, K.7 | FIXED | **K.7 (fresh eyes: the reviewer never authored the code) has now been bypassed a fourth time.** Three bypasses at M8 fired `C2b`, whose whole purpose is that the third bypass sends the CONTROL for review rather than the seat — and the review was assigned to M9, and M9 closed without it, and M10 has now bypassed it again and found a BLOCKING defect by self-review (W-052). The telemetry worked exactly as designed and nothing consumed it. This is ESCALATED rather than accepted because the remedy is not the lead agent's to choose: either K.7 gets an environment where a second seat exists, or the rule is amended to say what a single-agent session actually does. **The one thing that must not happen again is a fifth silent bypass.** Owning milestone: **M11**, and it is an owner decision under AGENTS.md §3 (gate-definition change). *(the ESCALATED text above is superseded by this note — it records why the row was raised, not where it landed.)* **RESOLVED 2026-08-22 (M11-W1) by owner ruling:** K.7 is amended to match a single-agent lane rather than left to accumulate a fifth bypass — the reviewing seat is a separate session and the review is a file (D-133), enforced by `scripts/wave_check.py::review_seat_problems`. |
| W-056 | M11-W1, first run of the REQ-REV-001 gate | 2026-08-22 (M11-W1) | `docs/plans/m8-wave-5-close.md` row 3 | FIXED | **The one wave record that claimed K.7 was satisfied cited two review records that have never existed.** `docs/reviews/m8-code-review.md` and `docs/reviews/m8-security-review-independent.md` return nothing from `git log --diff-filter=A`. The three seats did review and their findings were acted on; the reports came back in conversation and nobody wrote them down, and the row's word for that was "equivalents". **A record asserting a control which is not there — on the single row certifying the control every other row defers to.** The row is amended in place with a dated note and its verdict corrected from ✅ to WAIVED, rather than deleted. The gate that found it checks broken citations in EVERY era for this reason. |
| W-057 | M11-W1 independent review, MINOR N1 | 2026-08-22 (M11-W1) | `scripts/wave_check.py::review_seat_problems` | ACCEPTED | The `milestone=None` branch cannot be reached through `main()`, which rejects any filename the milestone regex would not match, so `test_a_citation_to_a_file_that_does_not_exist_fails_in_every_era[None]` pins a state the entry point cannot produce. Accepted rather than removed: the direction it pins (unknown era means the strictest rules apply) is what the code's own comment claims, and deleting the case would make the function's contract depend on its only caller — which is how a helper acquires an invariant nobody wrote down. Owning milestone: **M12.** |
| W-058 | M11-W3, measured while discharging W-039 | 2026-08-22 (M11-W3) | `src/app/adapter/main.py::health` | ESCALATED | **`/health` answered `{"status": "ok"}` while every `/v1` query answered 503.** Stage 4.3 verifies a deploy by curling `/health`, so the deploy check was measuring the PROCESS and not the product — the W-023 shape this project has already paid for once, where a thing looks healthy to every existence check and answers nothing. A new additive field `evidence: servable\|unavailable` now reports it, which the endpoint's own docstring permits. **`status` is deliberately NOT changed**, because re-defining what a liveness probe reports is a contract change, and because the deploy gate reading the new field is a gate-definition change: both are the owner's under AGENTS.md §3. The question for him is narrow: should Stage 4.3 fail a deploy when `evidence` is `unavailable`? Owning milestone: **M11**, and it must not survive this close the way W-037 survived M9's. **OWNER'S ANSWER RECORDED 2026-08-22:** Stage 4.3 is to FAIL a deploy when `evidence` is `unavailable`, with `status` left as liveness. The wiring is deferred to the end of the milestone at his instruction, alongside the deploy question itself — there is nothing to gate until something deploys. |
| W-059 | M11-W2 independent review | 2026-08-22 (M11-W2) | `runner` swift-tests section, `ios/Package.swift`, `docs/prd.md` REQ-IOS-002 | FIXED | **Three findings from one seat, and the first is the worst.** (1) The `runner` swift-tests section called `pass` and `fail`, which are not commands in that script — only `record <name> <exit-code>` is. With `set -u` and no `set -e` both branches died as "command not found", `$FAILED` stayed empty, and **runner reported ALL PASS with the Swift tests red**. The other half of the same change, the Makefile recipe, HAD been proven by breaking an assertion; this half was only read. *Proving one half of a change proves nothing about the other half.* (2) `Package.swift`'s comment claimed the macOS-26 floor was what kept the FoundationModels tier compiled and that the iOS pair "cannot silently drift" — the seat disproved both by lowering the floor (still green, tier still compiled) and by observing that nothing compared the two declarations. The rationale is corrected and `tests/unit/test_ios_platform_drift.py` makes the drift claim TRUE rather than merely retracted. (3) REQ-IOS-002 was marked DONE while its `unmeasured` clause had no test that could fail. |
| W-060 | M11-W2 independent review, scope | 2026-08-22 (M11-W2) | `ios/ModelRanking/ContentView.swift`, `.github/workflows/` | ACCEPTED | **The Swift gate covers the Engine layer and nothing else, and it runs on no CI machine.** `ContentView.swift` and `ModelRankingApp.swift` are SwiftUI and remain unexecuted — a deliberate scoping decision (the Engine is where a defect changes what a reader is TOLD), stated in `ios/Package.swift` and in every record rather than implied away. Separately, every CI job is `ubuntu-latest`, so `make swift-test` reports SKIPPED NO-ENVIRONMENT there and the 33 Swift tests run only on the owner's Mac. That makes them a local gate, which is what they are and not what a green CI badge would suggest. Owning milestone: **M12.** **NARROWED 2026-08-22:** the review's other five minors are now CLOSED and only the two stated above remain. Closed: the `make swift-test` recipe passed on ZERO tests (measured — an empty target prints `Executed 0 tests, with 0 failures` and exits 0, the THIRD gate-cannot-fail in this one wave), now a `SWIFT_TEST_FLOOR` in the coverage-floor shape; `swift-test` missing from `.PHONY`; the Engine compiled in Swift 6 mode while the app ships Swift 5, now pinned and compared by `tests/unit/test_ios_platform_drift.py` along with a check that no `#if DEBUG` / `#if os()` makes the two builds different programs; the D-104 membership guard on the model's output was unreachable behind an `@available(iOS 26)` call and its deletion survived the suite — extracted to `ModelOutputBoundary` OUTSIDE the availability block and covered by six tests; and a dead member on a test double. |
| W-061 | M11-W3, found by running the simulator session before handing it over | 2026-08-22 (M11-W3) | `Makefile` `run` target | FIXED | **`make run` did not start the engine from a clean environment.** It is the command `note.txt` tells a developer to use to reach the engine from the iOS app, and `validate_startup_config` fails closed on an unset `MODEL_RANKING_DB` and an unset `APP_BUILD`. Both refusals are correct; what was wrong is that the documented command supplied neither, so the only way to run the engine was to already know something no document said. Found because `scripts/simulator_session.sh` was RUN before being handed to the owner rather than read — the first execution died on import with the error in a log nobody would have opened. `run` now supplies developer defaults with `:-` so an operator who sets either keeps it. Verified from an environment with both variables unset: `/health` answers `{"status":"ok",...,"build":"dev-a35bb3a","evidence":"servable"}`. |
| W-062 | M11-W3, the simulator session's own first run | 2026-08-22 (M11-W3) | `scripts/simulator_session.sh` | FIXED | Two defects in the hand-over script, both found by running it. (1) It reported an engine with **no `evidence` field at all** — every build before M11-W3 — as "NOT servable", conflating an absent measurement with a failed one, which is how somebody rebuilds a healthy artifact. Three states now: servable, unavailable, and unknown-because-the-engine-predates-the-field. (2) It reused an engine already listening on 8080 without checking what that engine WAS: on the first run the listener reported `build: dev-3f2de92` against a HEAD of `a35bb3a`. A session that walks the product against yesterday's engine and writes down what it saw is worse than no session, because the notes look like evidence (L.7). It now warns and names both stamps. |
| W-063 | M11-W3 owner session, defect 1 of 3 | 2026-08-22 (M11-W3) | `ios/ModelRanking/Engine/Router.swift::ModelRouter` | FIXED | **The on-device tier could not say "nothing here measures this".** The owner typed *"Profile picture polishing"* and got Agentic coding under the sentence *"Matched your question to this surface on this device."*; *"Food kalori calculation"* routed to coding. Neither was a bad match — `GenerationSchema(anyOf: known)` constrained the model to the nine ids the engine serves, so tier 1 had **no expressible way to decline**, and every question in the world came back as a measured surface with a confident sentence. REQ-RTR-005's disclosure existed only in the SIMILARITY tier's floor, which runs second and therefore almost never runs on a device that carries the model: **the control lived in the path that does not execute.** Fixed by adding `ModelOutputBoundary.declineSentinel` to the schema's choices and to the instructions, mapped to `assistant` with `unmeasured: true`. A test asserts the sentinel is never a real surface id, because a collision would turn a refusal into a recommendation. Six mutants over the three fixes, six killed. |
| W-064 | M11-W3 owner session, defect 2 of 3 | 2026-08-22 (M11-W3) | `ios/ModelRanking/ContentView.swift`, `src/app/adapter/main.py::CODING_INTENT` | FIXED | **The surface the reader chose was not the one that answered first.** He typed "Coding", the app selected Coding, and the first block on screen read "Agentic coding" — three times, which he read as the router ignoring him and reported as *"is the AI not working behind it?"*. `task=coding` expands server-side to `CODING_INTENT = ("agentic-coding", "coding")` and `/v1` states in its own payload that answer order carries no meaning, so the alphabetically-first surface always spoke first. The client now puts the SELECTED surface first. This is not the re-sorting the M11 plan's Trap 1 forbids: that rule is about reordering models inside a ranking, which is the engine's answer; which of two answers appears first is explicitly meaningless to the engine and entirely meaningful to the reader who just asked. **Two independent defects produced one symptom** — the router genuinely mis-routed two of three questions (W-063) AND the right routing was invisible — which is why it read as one broken feature. |
| W-065 | M11-W3 owner session, defect 3 of 3 | 2026-08-22 (M11-W3) | `ios/ModelRanking/ContentView.swift` — `rankingPreview`, `categoryStrip` | FIXED | Two screen defects. (1) **The preview repeated the picks.** Claude Opus 5 appeared as Best Quality in large type and again four lines below in small type, because the preview was the top of the FULL ranking. Now five distinct models visible in total, split by however many distinct picks there were — a surface can produce fewer than three, and the owner's own screenshot shows DeepSeek V4 Flash holding two pick labels at once. (2) **The category strip snapped back to the start on every selection.** He scrolled right to Mathematics, tapped it, and the strip returned to Coding with no chip visible as selected — selecting rebuilds the view and an unbound `ScrollView` is recreated at offset zero. `scrollPosition(id:)` persists it. It does not scroll on his behalf: the strip moves when he moves it and at no other time. |
| W-066 | M11-W3.5, the design pass | 2026-08-23 (M11-W3.5) | `ios/ModelRanking/ContentView.swift` | FIXED | **Half of this design was verified by looking at it and half was not, and the record says which half.** Screenshotted from the running app on a booted simulator: the card surfaces, the pick badges, the section titles, the spacing, the selected-surface-first ordering, and the whole screen again in DARK mode — semantic colours adapt correctly. NOT seen: everything below the fold, which is the grouped ranking card, the "See all N" row and the disclosure block. macOS refused `osascript` permission to send scroll events to the Simulator, so there was no way to scroll from the command line and no attempt was made to fake one. `ContentView` is the half of the product no test executes (W-060), so "it builds" is not evidence about a layout. The owner's next session is what closes this row. Owning milestone: **M11**, and it closes in W4 or not at all — the owner's next simulator session either sees the rest of the screen or the milestone closes saying it did not. **CLOSED 2026-08-23, same session: the rest of the screen has now been SEEN.** The owner granted the accessibility permission, and a press-move-release drag posted with CoreGraphics scrolls the Simulator where scroll-wheel events and arrow keys do not (`scripts/simtools/`). Verified below the fold: the remaining models render as ONE card with dividers and a `See all 44` row; **no pick is repeated in it** — Claude Opus 4.7, DeepSeek V4 Pro and DeepSeek V3.2 appear only as cards, and the small list holds exactly two rows, which is the 3+2 shape the owner asked for; and both disclosures survived the design pass, rendering in orange with warning glyphs (the 177-day stale-evidence notice and the effort-mix notice). |
| W-067 | M11-W3.5 verification, the half that could not be reached | 2026-08-23 (M11-W3.5) | `ios/ModelRanking/Engine/Router.swift::ModelRouter` | FIXED | **Whether the on-device model actually EMITS the decline sentinel is unverified.** W-063's plumbing is tested — the sentinel is in the schema's choices, and `ModelOutputBoundary` maps it to `assistant` with `unmeasured: true`, both killed by mutants. What no test can answer is whether `FoundationModels`, given "write me a poem", chooses `__none__` over the nearest measured surface. That is a question about a model's behaviour and only a real run answers it. Synthetic keystrokes reach the Simulator but do not land in a SwiftUI `TextField` even with Connect Hardware Keyboard on, so it could not be typed from here; clicks DO land, which is how the layout was walked. **The owner typing one unmeasured question is the whole test.** Owning milestone: **M11**, and it closes in W4 by his answer or the milestone closes saying it is open. **CLOSED 2026-08-23 by the owner's own run: it warned.** He typed an unmeasured question into the running app and the app disclosed that it does not measure it. That is the only evidence this row could ever have taken — the plumbing was already unit-tested and mutant-killed, and what was open was whether `FoundationModels` would actually CHOOSE the decline sentinel over the nearest measured surface. It does. The screenshot was lost and it does not matter: the claim is about behaviour observed by the person who asked for it, and a picture would have been a record of the same observation rather than a better one. **CORRECTED 2026-09-18 (M13 sign-off session) — this row closed on evidence that cannot have come from the tier it was closed about.** The question it asked was whether `FoundationModels` CHOOSES `__none__` over the nearest measured surface. It was closed because the owner typed an unmeasured question and the app disclosed that it does not measure it. But the M13 handover §4 measured `SystemLanguageModel.default.availability` as `.unavailable(.appleIntelligenceNotEnabled)` on the owner's Mac and concluded that `ModelRouter.route()` returned `nil` at its first guard on every question asked up to that point. If that reading holds, tier 1 was not running on 2026-08-23 and the disclosure the owner saw came from the SIMILARITY tier's floor — the same sentence, from the other path. **The two tiers were indistinguishable on screen at the time**, because the wording split that names which one answered was not written until M13-W3 (review MINOR-5). The observation was real; the attribution was not, and nothing on screen could have told the owner apart. **The question this row asked is now answered, for the first time, on 2026-09-18:** with Apple Intelligence enabled, the owner typed `Image enchantment` into the app on the booted simulator and the screen returned the notice at `FrontDoor.swift::routingNotice` case `(_, true, .english)`. `.manual` and `.similarity` are matched in earlier cases, so that case is reachable only with `tier == .model`, and the only construction of `.model` with `unmeasured: true` is `ModelOutputBoundary.outcome` on the decline sentinel (`Router.swift:369`). No `OnDeviceState.help` line rendered beneath it, which `ContentView.swift:280` shows only when the state is not `.available`. **So tier 1 runs inside the simulator, it emitted `__none__`, and it did so on a misspelling (`enchantment`) that the wording tier had no term to match.** That settles the open question the handover left as *probable, not measured*. |
| W-068 | M11-W3.5, the simulator helper's own defect | 2026-08-23 (M11-W3.5) | `scripts/simtools/keyboards_on.scpt` | FIXED | **A helper that ensured a setting it could not read, and so only flipped it.** The owner asked for both keyboards — the Mac's, and the app's own on tap. The first version clicked both menu items and checked neither, because it assumed both carried a state. Measured: `Connect Hardware Keyboard` reports a check mark and `Toggle Software Keyboard` reports `missing value` — it is a pure toggle with no readable state. So run twice, the script turned the software keyboard back OFF, leaving the owner's Simulator in a state nobody chose. Now it ensures only the half it can read and prints ⌘K for the half it cannot, and running it twice is proven to change nothing. **A control that cannot observe what it is setting is not a control, it is a coin flip** — the same shape as a gate that cannot fail, in the opposite direction. |
| W-069 | M11-W3.5, second owner session | 2026-08-23 | `ios/ModelRanking/ContentView.swift::RankingList` | FIXED | **Reported as "pressing C filters by category, not by model name". It never did.** Measured against the real artifact: on `coding`, `c` matches **12 of 44** models and the first is `Claude Opus 4.7`. What he was looking at were the three LOWEST-scoring matches — GPT-5.2 Codex, Qwen3 Coder, GPT-5.1 Codex — because he was scrolled down a 44-row list, typing shrank it to 12, and the scroll offset clamped to the end. Three coding-named models in a row read exactly like a category filter. **The predicate was never the defect; a list that changes underneath the reader and leaves them at an arbitrary slice of it is.** Fixed by scrolling to the top on every filter change. The predicate is also extracted to `filterRanking` in the Engine — it had been written out twice in the view layer with no test on either copy — and six tests now pin that it matches name and vendor and never the surface, including the exact `c` case that was reported. |
| W-070 | M11-W3.5, second owner session | 2026-08-23 | `ios/ModelRanking/ContentView.swift::categoryStrip` | FIXED | **The chip fix from the first session was the wrong fix, and it also could not work.** He asked for the selected category to be CENTRED; the first attempt bound the scroll offset with `.scrollPosition(id:)` to stop the strip snapping to the start. That solved a different problem — he then tapped `Web development` and found the chip he had just chosen sitting half-cut against the right edge. Worse, the second attempt added a `ScrollViewReader` centring the selection while LEAVING the binding in place: the two control the same thing, the binding wins, and the programmatic scroll silently did nothing. Measured, not reasoned — tapping a clipped chip selected it and the strip did not move a pixel. The binding is removed and centring subsumes it: the strip cannot snap to the start because every selection scrolls to the chosen chip. **Not visually confirmed** — synthetic clicks stopped landing on the chips reliably, so the owner's next tap is the check. |
| W-071 | M11 closing audit, found by counting rather than remembering | 2026-08-23 | `docs/prd.md` REQ-RUN-002, `docs/plans/m11-plan.md` §W3 | ESCALATED | **A signed plan promise was never written into the PRD, and W3 closed without it.** The plan assigns REQ-RUN-002 — two unattended refresh cycles observed — to W3. No such row existed in `docs/prd.md`: it was never added at its wave, so nothing traced it, no gate could see it, and W3's close record listed the four items it DID deliver without noticing the fifth. Found only because the owner asked what was left and the answer was counted instead of recalled. **This is the exact failure the wave-time REQ-ID rule exists to prevent (F-1 drift), and it is the second time this project has had a plan promise quietly not delivered — the first was W-027 at M7.** The row is now in the PRD marked NOT MET rather than backfilled as done. ESCALATED because the remedy is the owner's: `launchctl load` is his command, and until it runs the milestone cannot honestly claim the requirement. Owning milestone: **M11**. |
| W-072 | M11 Stage-4.0 security review, BLOCKING-1 | 2026-08-23 (M11-W4) | `scripts/wave_check.py::review_seat_problems` | FIXED | **The K.7 gate could be switched off by renaming one table cell.** It found its subject by grepping the row's NAME cell for `review|K.7`, so a close whose row 3 read "Fresh eyes per tier" was skipped entirely — the seat reproduced it against a copy of a real wave record: baseline PASS, rename, still PASS, **exit 0, no review cited and none in existence.** `AGENTS.md` §4 and the function's own comment both claimed it "cannot pass silently"; that was measurably false. **The gate failed OPEN on a label it did not know**, which is the worst direction available. Worse, the test written to catch exactly this used the label `Fresh-eyes code REVIEW` — which still contains "review" — so it proved case-insensitivity and not label-independence: fixture blindness in the test guarding the gate guarding the reviews. Fixed by removing labels from the question entirely. The rule is now record-level: **a v5.0 close must cite a review declaring `seat: independent`, or name a ledger row for the bypass.** Neither can be renamed away. Verified against four labels including ones the gate has never seen, and against all 19 real v5.0 records. |
| W-073 | M11 Stage-4.0 security review, MAJOR-1 | 2026-08-23 (M11-W4) | `scripts/simulator_session.sh` | FIXED | **"Ctrl-C … that stops the engine" did not stop the engine.** `( cd "$REPO" && … uvicorn … ) &` is two commands, so bash forks a real subshell and `$!` is the SUBSHELL's pid; killing it left uvicorn orphaned on port 8080. That is also where this script's own stale-engine warning kept coming from — it was detecting its own leftovers. Fixed with `exec`, and the teardown now VERIFIES the port is free rather than assuming it. **Worth recording twice over:** the first attempt to reproduce it used a single-command subshell, which bash optimises into no fork at all, so the engine died and the finding looked wrong. A simplified repro that does not reproduce is worse than no repro — it argues the reviewer was mistaken. |
| W-074 | M11 Stage-4.0 security review, MAJOR-2 | 2026-08-23 (M11-W4) | `scripts/*.sh`, `runner` | ESCALATED | **A script that installs a background job on the owner's machine reached the tree through no wave, no review record and no linter, in a Stage 4.2 CAPTURE commit.** `scripts/enable_refresh.sh` writes a launchd plist into `~/Library/LaunchAgents` and bootstraps it. The security seat reviewed it and found nothing dangerous — no root, no other-user-writable path, fails closed on a wrong CWD — but that is luck rather than process. Two things are true and separable: (1) the shell in this repository is **unlinted**, `shellcheck` is not installed and no gate runs it, across `runner` and every script under `scripts/`; and this milestone shipped three shell defects that a linter may or may not have caught but nothing even looked for. (2) Work arriving outside a wave has no review seat by construction. ESCALATED: adding `shellcheck` to `make check` is a gate-definition change and therefore the owner's under AGENTS.md §3. Owning milestone: **M12.** |
| W-075 | M11-W4, found by watching the install instead of trusting it | 2026-08-24 | `scripts/enable_refresh.sh` | FIXED | **The installer printed a claim the plist it had just installed contradicts.** It said *"The first cycle runs now; the second in 12 hours."* `RunAtLoad` is DELIBERATELY absent from `deploy/com.hcs.modelranking.refresh.plist`, and the plist's own comment gives the reason: installing the job must not immediately rebuild the artifact somebody is serving. Measured 20 seconds after a real load: `runs = 0`, no log written, and `advisor.db.refresh.json` still carrying the previous day's cycle. **A record stating the opposite of the code — this project's most-recorded defect — in a script written to reduce operator error.** Corrected to say the first scheduled cycle is twelve hours away and to name `launchctl kickstart` for anyone who wants one now. Consequence for the milestone, stated rather than absorbed: REQ-RUN-002 asks for two cycles **on the schedule**, so it is 24 hours away and not two commands away. |
| W-076 | M12-W1, from the M11 council | 2026-08-25 (M12-W1) | `docs/decisions.md`, `tests/unit/test_adr_citations.py` | FIXED | **D-119 and D-120 were cited in 30 files and had never been written.** `build.py:25` calls D-120 "`schema.py`'s frozen D-120 contract"; `test_roster_window.py:416` calls it "a K.8 frozen contract, so the SET is pinned"; the M10 and M11 plans both list it among the surfaces this project promises not to move. `decisions.md` went D-118 straight to D-121. Both are now written, **dated today and describing contracts in force since M6** — not backdated, because a false date would commit the defect inside its own repair. Writing D-120 surfaced a second fact nobody had recorded: **exit code `3` means two different things** — "built but not servable" in `build.py`/`schema.py`, `EXIT_REFUSED` in `refresh.py`. Recorded as a KNOWN divergence rather than resolved, because both consumers already depend on their number and changing one to tidy a document would break a caller. |
| W-077 | M12-W1, correcting a council finding | 2026-08-25 (M12-W1) | `docs/reviews/m11-council-senior-swe.md` | FIXED | **The council seat also reported P-002 and P-003 as phantoms, citing 7 and 5 files. They are neither.** `docs/decisions.md:151` explains them as reserved mirrors of D-006 and D-007 under P-001, and every one of those "citations" was the substring `P-002` inside **`REQ-APP-002`**. Caught by re-measuring with word boundaries before acting — two ADRs were about to be written that already had a documented status. Recorded because the seat's other findings were excellent and this one was not, and a council whose findings are adopted unverified is worth less than no council. `tests/unit/test_adr_citations.py` matches on word boundaries for exactly this reason and says so. |
| W-078 | M12-W1, from the M11 council | 2026-08-25 (M12-W1) | `Makefile` `gate` target, `scripts/check_records.py` C2b | FIXED | Two controls that could not do their job. (1) **`make gate` exited 2 on a clean tree** — it depended on the RAW `conformance` leg while `check` runs `conformance-gate`, which applies the per-finding exemptions. It is the only thing the post-edit hook runs, its own log had never been written, and CI never calls it: a gate red for months, unread by everybody including the hook that ran it. One line. (2) **`C2b` grouped on the ledger's free-text provenance column**, unique on every row by construction — 22 ACCEPTED rows, 22 distinct keys, **zero triggers ever**, while K.7 had been bypassed ten times and two records asserted it had fired. Now keyed on CONTROL identifiers and **dischargeable**: naming the ADR that reviewed the control satisfies it, which is what V4C-13 asks for. A finding that cannot be discharged is an alarm somebody eventually silences. |
| W-079 | M12-W1, from the M11 council's mobile seat | 2026-08-25 (M12-W1) | `ios/ModelRanking/Engine/Router.swift::matchesFilter` | FIXED | **The model filter would have broken on the day Turkish shipped.** `localizedCaseInsensitiveContains` folds case by the READER's locale, and Turkish folds the capital I to a dotless lower-case letter — so under `tr_TR`, searching the ordinary lower-case i stops matching "GPT-5.1 Instruct". Measured in three locales. Now folded through a pinned `filterLocale`, because a model name is an IDENTIFIER and is spelled the same everywhere. **The part worth keeping:** the six tests pinning this predicate were written a day earlier and all inherit the process locale, so none could see it — and after the fix, a mutant restoring `Locale.current` still survived every behavioural test, because this machine's locale folds like English. Only asserting the CHOICE — `filterLocale.identifier == "en_US_POSIX"` — kills it. **Fixture blindness whose blind spot is the ENVIRONMENT rather than the data.** |
| W-080 | M12-W2, found on the screen by its own author | 2026-08-25 (M12-W2) | `src/app/adapter/main.py::ORDERING_NOTE` | FIXED | **The ordering note had become false, and by our own hand.** It said *"Answers are ordered alphabetically by surface id"* — true when written, and untrue since M11 fixed W-064, where the client began putting the SELECTED surface first. The note asserted an ordering the reader could see was not happening, and it shipped that way for a whole milestone. Rewritten to state what is INVARIANT — no position means anything — which survives a client reordering; a disclosure pinned to an implementation detail has an expiry date. "plans" also became "models": the engine ranks models, plans are the subscription CLI's subject (council finding). The test that guarded it pinned the literal phrase "carries no meaning" and failed on a rewrite saying exactly that in other words, so it now pins the CLAIM — a phrase pin makes any rewording look like a regression and a real regression look like a rewording. |
| W-081 | M12-W2, from the M11 council | 2026-08-25 (M12-W2) | `src/app/workflows/recommend.py::cheaper_phrase` | FIXED | **`"{ratio:.0f}x cheaper"` reads "1x cheaper" for every ratio under 1.5** — telling a reader they give up quality and save nothing, the opposite of the trade-off the sentence exists to describe. NOT visible on today's data, where every ratio is 3x or more, which is why it has nine tests rather than a fix: a defect today's data hides is one that waits for a price change, and the artifact is republished every twelve hours. Below 2x the wording is now a percentage, because "1.4x cheaper" is arithmetic and "29% cheaper" is a sentence. Two models at the same price report no saving at all rather than `0% cheaper`. **CORRECTED 2026-08-25 (M12-W5, Stage 4.0 MAJOR-5) — this row named the wrong symbol AND gave a false reason.** (1) The fix was recorded at `src/app/workflows/recommend.py::cheaper_phrase`, which **nothing ever called**: W2 wrote it, W4 wired a second implementation of the same rule (`trade_off_facts` + `trade_off_sentence`) into the answer, and the ledger went on citing the dead one. `cheaper_phrase` is now deleted and its nine tests run through the shipping composer, so a drift in the live rule fails them. (2) The row said the defect was *"NOT visible on today's data, where every ratio is 3x or more, which is why it has nine tests rather than a fix"* — measured false: the M12 starting tree served `task=computer-use&budget=low` a `budget_pick` reading `2.7 points below the leader, but 1x cheaper.` (`0.98` vs `0.69`, ratio `1.4203`) on the day this was written. **The unmeasured assumption was load-bearing**: it is the entire argument for why nine tests were an acceptable substitute for shipping the fix. |
| W-082 | M12-W2, from the M11 council | 2026-08-25 (M12-W2) | `ios/ModelRanking/Engine/EngineClient.swift` | FIXED | **The app told end users to run build commands.** `recovery` said *"Start it with `make run` in the engine repository"* and lectured the reader about `NSAllowsArbitraryLoads` and the `/v1` contract. Correct while the only reader was the person who built it — which was true for eleven milestones and stopped being true the week a 60-year-old CFO opened the app. The developer detail is MOVED to a new `diagnostic`, not deleted: the transport detail is what tells whoever is debugging that this was a refused connection rather than a DNS failure, and the cleartext warning exists to head off a one-line "fix" that would ship this product's first network call in the clear. Both halves are tested. |
| W-083 | M12-W3, from the M11 council's product seat | 2026-08-25 (M12-W3) | `ios/ModelRanking/ContentView.swift` | FIXED | **The product called itself budget-aware and had no budget control.** `private let budget = "unlimited"` — a constant. Meanwhile `/v1/budgets` published the caps (built at M11-W3, with tests proving the served cap reproduces the served `eligible_count`), `eligible_count` arrived on every answer, the "N fit your budget" line was written and rendered, and `scripts/simulator_session.sh` instructed the owner to "switch the budget". **A milestone of work served a control that did not exist**, and the defect was found by a council seat reading one line rather than by any of it. Fixed: three choices under the category strip, captions from the engine so the app does not hardcode what `low` means. The half worth testing separately is that the CHOSEN budget reaches the engine — a picker that updates state and sends the old value looks identical on screen. |
| W-084 | M12-W4, caught by its own derivation test on the first run | 2026-08-25 (M12-W4) | `src/app/workflows/recommend.py` | FIXED | **The prose and the fact were computed in parallel and disagreed immediately.** D-136 says the sentence is DERIVED from the fact; the first implementation computed both from the same inputs in two places, and the sentence said `3x cheaper` beside a fact carrying `3.1` — so a client composing from the fact would have written a different sentence than the engine shipped. Two more of the same followed: `84 points` beside a floor of `84.4`, and `6` beside a window of `6.0`. Fixed by computing the FACT once and composing the sentence from it, and by not rounding a threshold in the claim — the bar really is 84.4, and printing `84` states a bar the engine does not apply. **Two parallel computations that agree today are two sources of truth**, and this one did not even manage today. |
| W-085 | M12-W4, seen on the first Turkish screenshot | 2026-08-25 (M12-W4) | `ios/ModelRanking/Engine/Language.swift` | FIXED | **English units inside Turkish sentences**: `En iyisinin 6 points yakınında`. The unit arrives from the engine as a display label and was interpolated raw. A half-translated sentence reads worse than an untranslated one, because the reader cannot tell which half to trust. Fixed for the units, and then for the rest of the screen the same argument reaches: the score scale, the price, the chrome, the surface names, the pick badges and the budget captions. **`elo` and `ECI` are deliberately NOT translated** — they are the names of scales, and translating a proper noun would leave two readers unable to compare notes, which is the same reasoning that keeps the digits identical in both languages. |
| W-086 | M12-W4, third occurrence of one shape | 2026-08-25 (M12-W4) | `tests/unit/test_ios_client_contract.py` | FIXED | **A test that pins WHERE logic lives fails when it moves and passes when it is deleted from the place it moved to.** This one grepped `ContentView.swift` for `eligibleCount < ranking.count`; the comparison moved into `UIText.seeAll`, where it gained direct Swift tests in two languages, and the guard failed on a change that kept every part of the behaviour. **Third time in two waves** — the ordering note's literal phrase, the ATS warning's 600-character window, and now this. Each cost a wave a red gate and taught the same lesson: pin the claim and the audience, never the offset. A test that fails when text moves teaches people to leave text where it is, which is how a user-facing string ends up explaining App Transport Security to a CFO. |
| W-087 | `scripts/wave_check.py::review_seat_problems`, the date rule added at M12-W5 (D-137); the bypass itself was found by the M12 Stage 4.0 independent seat as BLOCKING-3 | 2026-08-25 (M12-W5, Stage 4.0) | `K.7` / `V3C-78` — `docs/plans/m12-wave-1-close.md`, `docs/plans/m12-wave-2-close.md`, `docs/plans/m12-wave-3-close.md`, `docs/plans/m12-wave-4-close.md` | ACCEPTED | **All four M12 waves closed the K.7 row GREEN citing council reviews dated 2026-08-24, while every M12 code commit is dated 2026-08-25.** The rows said the timing themselves — *"the review preceded the code"* — written as the defence and standing as the proof. Those seats reviewed M11's product and named the findings M12 implemented; that is what shaped the milestone, and it is genuinely valuable. It is not a reading of M12's code. **Nobody read M12's code independently until the Stage 4.0 pass, which found two BLOCKING defects in it** — an `Int` conversion trapping on non-finite input, and an L1 scope hole where an unbalanced fence silently exempted 26 lines from the English-only rule. Both are FIXED at M12-W5; the read that was owed was performed, late, and it was not free. **Accepted rather than escalated because the remedy is already in force and nothing is left outstanding:** the four wave rows now read WAIVED naming this row instead of a false green (a bypass the owner can see is worth more than a green he cannot check, V4C-13); D-137 records that a review has a DATE and that a review dated before the code did not read the code; and `review_seat_problems` now refuses to let a stale review satisfy the seat requirement, measured to fire on exactly these four records plus `m7-wave-1-close.md`, which predates the seat rule. **This is K.7's fourth acceptance and V3C-78's fourth**, after D-133 reviewed K.7 at three — D-133 settled who reviews and never touched when, which is precisely how the same control was bypassed again under a rule written to stop it. **C2b-reviewed: D-137 @4** — both controls reviewed together at this fourth acceptance, because they failed together on the same missing axis. Owning milestone: **M12, closed at W5.** |
| W-088 | M13 Stage 4.0 independent seat, MAJOR-3 | 2026-09-06 (M13-W1) | `V3C-78` — `docs/plans/m13-wave-1-close.md:19` | ACCEPTED | **The pulled-forward security pass of a HIGH wave was waived, and this is the wave it would have caught.** The signed plan tagged M13-W1 HIGH. Row 4 read WAIVED under the Security-Reviewer profile's narrower trigger (no auth, PII, payment, crypto or migration surface) rather than the tier definition the plan used. The waiver was honest and named a local ledger row, but it never reached THIS ledger, so the counter that exists to notice repetition could not count it. **The waiver had a measured cost:** W1 memoised the `/health` probe on `(path, mtime, size)`, a key that cannot see a permission change or a same-size rewrite, and `/health` read `servable` over a 503. The Stage 4.0 seat found it (security review MAJOR-1); it is fixed at M13 closure with two negative tests through `/health`. A security read of this slice at W1 is where a memo on a readiness probe was most likely to be questioned. Owning milestone: **M13, closed at W5**; the control review is D-141. |
| W-089 | M13 Stage 4.0 independent seat, MAJOR-3 | 2026-09-15 (M13-W2) | `V3C-78` — `docs/plans/m13-wave-2-close.md:27` | ACCEPTED | **Second waiver of the same pass in one milestone, with the same reason.** W2 added fields to an unauthenticated discovery route and a per-request read of the artifact behind it. The Stage 4.0 seat measured that read at 1.36 ms against 4.68 ms for the answer route, and found one defect there: a warning written per request against a bad artifact, a log volume any caller could choose (security review MINOR-1, fixed at M13 closure: once per artifact identity). Nothing escaped here that only a pulled-forward pass would have caught. Owning milestone: **M13, closed at W5**; the control review is D-141. |
| W-090 | M13 Stage 4.0 independent seat, MAJOR-3 | 2026-09-15 (M13-W3) | `V3C-78` — `docs/plans/m13-wave-3-close.md:32` | ACCEPTED | **Third waiver in one milestone.** A client-only wave with no server change. It rewrote the path that the privacy criterion REQ-RTR-004 guards, and the Stage 4.0 seat found the criterion still held in the code while its citing test could no longer fail: a mutant sending the reader's words to the engine passed (security review MAJOR-2; the test now asserts data flow at every engine call and every assignment to `task`, and kills that mutant and two more). **This is the third acceptance in one milestone and the seventh of this control overall**, so the CONTROL goes under review rather than the waves. D-141 reconciles the two texts that disagree about when a HIGH wave owes a security pass. **C2b-reviewed: D-141 @7.** D-141 is PROPOSED: the review is done and its recommendation is written, and ratifying it is the owner's at M13 sign-off. **If the owner refuses D-141, this marker must be removed**, because C2b reads the `@7` and never the ADR's status (Stage 4.0 re-verification MINOR-5). Making C2b accept only an accepted ADR is a gate change, queued to the owner for M14. Owning milestone: **M13, closed at W5.** |
| W-091 | M13 Stage 4.0 independent seat, MINOR-3 | 2026-09-15 (M13 closure) | `Makefile:132` `SWIFT_TEST_FLOOR` | FIXED | **The Swift test floor is 121 and the suite runs 215**, so up to 94 tests can stop being discovered with `make check` still green: this milestone's deadline, request-gate, unmeasured-question, figures-line and hostile-score tests among them. The recipe's own comment says to raise the floor when tests are added. **Not raised by the agent:** the floor decides what the gate accepts, so changing it is a gate-definition change and the owner's, the reasoning W-026 applied to widening `make lint`. Owner action, one line: set `SWIFT_TEST_FLOOR` in `Makefile` to the count `make check` prints at the closing tree (215 at the Stage 4.0 fix round). Owning milestone: **M13 sign-off.****RESOLVED 2026-09-20 — owner ruled RAISE.** `SWIFT_TEST_FLOOR := 215`, the count `make check` printed on the owner's machine the same day. |
| W-092 | M14-W1, found while measuring the image-editing board's ranked population | 2026-09-18 (M14-W1) | `src/app/workflows/registry.py::canonicalize` | FIXED | **An image model's price was published as a text model's price, in the artifact that is serving right now.** `canonicalize` is first-match-wins over an ORDERED rule table, and the module's own docstring states the invariant that ordering exists for: a variant's price or score may never leak into its parent family (REQ-CAN-002, written after GPT-5-nano's price surfaced as GPT-5's). The ordering defends the SIZE axis. **Nothing defended the MODALITY axis**, because there is no `gpt-5-image` rule for `openai/gpt-5-image` to match first — so it fell through to the `gpt-5` family rule. Measured on `advisor.db` built 2026-08-27: of 2,661 distinct pricing aliases, **19 carrying five modality tokens reconciled to six text models**, and **four published an inflated price** — `gpt-5` input `1.562` against `1.094` text-only (**+43%**), `gpt-5.4` `2.5/15.0` against `2.188/13.125`, `gemini-2.5-flash` `0.3/2.5` against `0.262/2.188`, `deepseek-v4-flash` `0.105/0.21` against `0.1/0.2`. **72 price cells moved across eight of the nine surfaces, and the comparative sentences moved with them**: `trade_off` and its D-136 `trade_off_fact` differ on 16 of 27 (surface, budget) answers, `computer-use` Best Value going from `cheaper_by_percent: 20` to `9`. What did NOT move, verified across all 27 answers: model order, the identity of every pick, `eligible_count`, `frontier_size`. **Nobody was recommended the wrong model; everybody was told the wrong price, and some the wrong saving.** Fixed by a modality guard that refuses a rule match when the alias carries a modality the matched rule does not DECLARE — a field on `ModelRule`, not a substring of its id, so an image rule keeps its own aliases whatever it is named. Five tests, red-first; five mutants, five killed, including the reviewing seat's own survivor. Found not by a gate but by asking a question about a different surface — the ranked population of the image-editing board — which is the second time this milestone that a coverage question has surfaced a correctness defect. |
| W-093 | M14-W1 independent Code-Reviewer seat, MAJOR-1 and the C1/C2/C3 refutations | 2026-09-18 (M14-W1) | `docs/warnings.ledger.md` W-092 as first drafted, `src/app/workflows/registry.py` comment block | FIXED | **The author's own measurement of W-092 was wrong, and the wrong numbers were written into the file the docstring calls the project's core IP.** Three claims did not survive the seat's re-measurement: six model families were affected and the record said four; four price medians moved and the record said three (`deepseek-v4-flash` was missed in every count because the author's first probe filtered on `image` alone and the figure was then carried into a paragraph describing the full token set); and the author reported "picks, eligible_count and frontier_size unchanged" from **a comparison that could not fail** — the fields were read with `getattr(recommendation, "best_value")`, which is `None` on that dataclass because the picks live under `.picks`, so the check compared `None` to `None` for all 27 answers and proved nothing. Re-measured correctly afterwards, the conclusion held (no pick identity changes) but the scope did not: 72 price cells and 50 sentence/fact fields, not 16 price cells. **A verification that cannot fail is this project's most-recorded defect shape, and it was committed here inside the verification of a fix for another instance of it.** Also refuted: "three new tests red against baseline" — five tests, of which four go red and only ONE is behaviourally red; the other three raised `ImportError` on a symbol that did not exist yet, which is evidence that a symbol is new and not evidence that behaviour changed. All figures in W-092 and in the code comment are now the seat's. **The control earned its place in the wave it was first run in:** without it the milestone would have shipped a corrected price under a record that misdescribed it. |
| W-094 | M14-W2, found by a calibration script refusing to trust itself | 2026-09-18 (M14-W2) | `src/app/workflows/categories.py` header comment vs the nine shipped `min_quality` values | ACCEPTED | **The comment above the thresholds states the opposite of the thresholds.** `categories.py` says: *"Every threshold below is sized on the RANKED population -- models that reconcile to the registry AND carry a price median -- not on the full board. Calibrating against the board has now produced wrong thresholds three times (W-037)."* Measured against `advisor.db`, the shipped floors are the top third of the **full board**, not of the ranked population: `expert` 83.6 against a board third of **83.3** and a ranked third of 91.0; `web-dev` 1478.9 against **1480.0** and 1522.9; `assistant` 1400.0 against **1393.4** and 1450.9; `everyday` 149.9 against **144.6** and 153.2. The board column matches on every surface and the ranked column is 8-15 points high on every percentage scale and ~50 Elo high on both Elo scales. **The calibration record settles it beyond inference:** `docs/reviews/m8-category-calibration.md`'s own table carries a `Models` column reading 521 / 263 / 238 / 59 / 168 / 102 for ECI, GPQA, AIME, TerminalBench, ARC-AGI and WebDev -- the full board counts, identical to the per-source row counts in a freshly built artifact. Its 2026-08-19 correction was about CSV ROWS versus MODELS (dedup), and was read afterwards as though it had also moved the population from the board to the priced subset. It did not. **So W-037's lesson was written into a comment and never applied to a number**, and the comment has since been cited as evidence that it was -- the record-contradicts-the-code shape this project has recorded more often than any other, this time inside the comment written to prevent it. **Consequence, stated at the right size:** `min_quality` is the Budget Pick floor, and a floor sized on the whole board is LOWER than one sized on the models the engine can actually sell, so the product admits budget picks its own stated rule would reject. No evidence yet that a pick actually changes -- that is measurable and is not measured here. ESCALATED rather than fixed: every threshold is a gate definition and therefore the owner's (the reasoning W-026 and W-091 applied), and re-deriving nine surfaces' floors is a milestone's work, not a wave's. **It also blocks M14-W2's own two surfaces**: calibrating them on the ranked population makes them stricter than the nine beside them, and calibrating them on the board perpetuates something this repository calls a defect. Owning milestone: **M14**, decision needed before W2 sets a threshold. **M14 DISPOSITION 2026-09-20 (D-145):** the owner ruled that new surfaces follow the rule the product ships (board third, distinct models), so the two M14 surfaces are consistent with the nine. Owning milestone for the remainder: **M15**. The underlying question -- which rule is RIGHT -- stays escalated, as one decision for all eleven surfaces. **RULED 2026-09-22 by the owner (D-148): the board-third rule, on the WHOLE board, for every surface**, and the header comment in `categories.py` now says so. **Corrected the same day by the W1 review (M-1):** the M8 floors are the top third of the board's ROWS, not of distinct models, so the product ships two counts. **The owner ruled the same day: board ROWS** (D-148 clause 1), the count the M8 floors already follow. Still to re-derive under it: `document`, `factuality`, `vision`, `search`, `search_factuality` and `agentic-coding` (50.0, which fits neither). Re-deriving any floor changes what the product recommends, so it is a calibration wave with its own review, not a closure edit. **Owning milestone: M16.** Control: V4C-77 -- this row has now been carried across three closes, and M16 is its last. |
| W-095 | M14, the owner's runner session | 2026-09-18 (M14) | `~/Desktop/terminal_output/model_ranking/refresh_bisect.sh` §2 | FIXED | **A diagnostic published to the artifact it was diagnosing, and the agent had told the owner it would not.** The script written to find why the launchd refresh exits 78 ran the real command in the foreground to see what it does — against `advisor.db`, the live artifact. It did exactly what the refresh is built to do: built a candidate, found the served content had changed, and published. The agent had described the runner to the owner as "nothing here writes to advisor.db", which was true of every other step and false of this one, and the owner ran it on that description. **The outcome was benign and that is luck, not design:** the publish was correct, it carried the W-092 price fix into the served artifact, and it ended a three-week staleness that was itself an open M13 item. It could as easily have published a candidate built from a half-finished tree. **A diagnostic that changes its subject is not a diagnostic**, and a claim about what a script touches has to be checked against the script rather than against its intent. Fixed: §2 now copies `advisor.db` and runs against the copy, and says so in its own output. The same section's launchd probes were also reading their subject after 2 s for a job that takes 7 s, so the two probes that mattered reported "never exited" where the answer should have been — a probe that measures before its subject finishes measures nothing; it now waits for the job to stop. |
| W-096 | M13 carry-over, closed in M14 by the owner's runner sessions | 2026-09-20 (M14) | `deploy/com.hcs.modelranking.refresh.plist`, `scripts/refresh_job.sh` (new) | FIXED | **The 12-hour refresh exited 78 (EX_CONFIG) on every trigger from 2026-08-27 and wrote nothing.** The standing hypothesis was macOS privacy protection on `~/Desktop` (grant Full Disk Access). **It was wrong, and so were three more the agent produced**: nine probes cleared, each by measurement, the Desktop log paths, the Desktop working directory, the venv interpreter, the two combined, the plist's location in `~/Library/LaunchAgents`, the symlink chain, the installed plist's contents (byte-identical to the repo's, `plutil`-valid), the job's registration (a reload reset `runs` 13 -> 1 and it still exited 78 at once), and the command itself (runs and publishes by hand). **The shape that worked:** `/bin/bash` as the program, running a wrapper that sets the directory and `PYTHONPATH` itself. Under launchd that wrapper ran the real refresh to completion in 39 s; the refresh exited 3, a correct D-128 refusal of a candidate that an upstream TLS timeout had left without Arena evidence. **The exact mechanism of the 78 was not isolated, and the hunt stopped on purpose** once a working shape existed: the probes that remained would have cost more than the answer is worth. One real side-observation, not the cause: a launchd process cannot `getcwd()` inside `~/Desktop` (`git rev-parse` fails with `Operation not permitted`), and the refresh never calls git. **Also recorded because it recurred three times in one hunt:** probes that read their subject before it had finished and reported a blank where the answer was; the fix is to wait on a sentinel the subject writes as its own last line, never on `launchctl print`'s state, which says "not running" both before a job starts and after it ends. **Remedy:** `scripts/refresh_job.sh` is the job's program; the repo plist is rewritten to call it by `install_refresh_wrapper.sh`, which the owner runs, because `deploy/` is his surface. **CORRECTION 2026-09-21 (W-100): that script did not exist in the repository when this row was written, and `enable_refresh.sh` installed the plist alone.** Owning milestone: **M14**; closes when the installed job reports an exit code other than 78. **CLOSED 2026-09-20, and the diagnosis above was incomplete.** Installing the wrapper alone still returned 78. Moving ONLY the log paths to `~/Library/Logs` turned it into exit 126 with the first real message of the hunt: `/bin/bash: .../Desktop/.../scripts/refresh_job.sh: Operation not permitted`. Moving the wrapper to `~/Library/Application Support/model-ranking/` made the job run: exit 3, a correct D-128 refusal (Arena timed out upstream). **So the cause was macOS privacy protection on `~/Desktop` after all** -- for files launchd opens on the job's behalf (logs) and files the job's program reads before it can do anything (the script). The earlier probes that 'cleared' the Desktop paths used `/bin/echo`, which is Apple-signed and let through; the protection is decided per program. The job's plist now points both outside `~/Desktop`; `scripts/refresh_job.sh` stays the source and is copied there on install. |
| W-097 | M14-W3/W4 independent Code-Reviewer seat, C5 refuted | 2026-09-20 (M14-W3) | `tests/unit/test_ios_client_contract.py::SORTING_PERMITTED` | FIXED | **The sorting exemption the author called narrow was a line substring.** Any line in `FrontDoor.swift` containing `entries.` passed, and the seat wrote two mutants that sorted the engine's ranking on such a line; both passed. Now every sort call on a line is checked on its own and must be applied DIRECTLY to a permitted receiver (`entries`, `entries.indices`). |
| W-098 | M14-W4, scope measured against the plan at close | 2026-09-20 (M14-W4) | `docs/plans/m14-plan.md` §2 W4 last two bullets | ESCALATED | **Two W4 bullets were not delivered:** price as an attribute on the detail screen (§0 ruling 2), and the metric's name kept available on the detail screen. The card and rows changed; the detail screen did not. Escalated to the owner for M15 scope rather than done at the end of a night wave with no review left. |
| W-099 | M14 Stage-4.0 closure seat, BLOCKING-1 | 2026-09-21 (M14 closure) | `tests/unit/test_router_hints.py::test_the_gap_register_stays_on_the_device` | FIXED | **The milestone's headline privacy invariant had a negative test that could not fail.** The three network assertions searched the REGISTER's section of `FrontDoor.swift`; the reader's words are recorded from `ContentView.ask`. The seat POSTed `typed` to a remote host in four lines immediately after `gaps.record(typed)` and the mutant survived every gate — `swift test` included, because the Engine target does not compile `ContentView.swift`. No egress existed, so this was a hole in the proof, not in the product. **Third occurrence of one shape** (M13 security MAJOR-2 / W-090: a mutant sending the reader's words to the engine passed; M13 Stage 4.0 MINOR-4: four more doors into `task`). **Remedy:** the ban is now on the whole view and the whole front door — `URLSession`, `URLRequest`, `URL(string:`, `NWConnection`, `CFStream` — leaving `EngineClient` as the one sanctioned egress, whose every argument is already pinned. Verified red on the seat's own mutant, green on the tree. |
| W-100 | M14 Stage-4.0 closure seat, MAJOR-1 | 2026-09-21 (M14 closure) | `scripts/enable_refresh.sh`, `scripts/install_refresh_wrapper.sh` (new), `tests/unit/test_refresh_job_install.py` (new) | FIXED | **The refresh job this milestone repaired could not be installed from this repository, and W-096 cited a script that did not exist.** W-096 moved the job's program to a wrapper under Application Support; `enable_refresh.sh` — the only installer — still copied the plist alone, so the documented install path produced a launchd job whose program was absent: the exact 24-day silent outage W-096 was raised for, reinstated by the fix for it, on any machine but the one it was hand-repaired on. `install_refresh_wrapper.sh`, named in W-096 as the remedy, existed only in the owner's scratch folder. **Remedy:** `enable_refresh.sh` installs the wrapper and the plist together and refuses if the two disagree; `install_refresh_wrapper.sh` ships for the wrapper-only case; three structural tests pin the plist's program, both installers' destinations and the log paths against each other. W-096's text is corrected below. |
| W-101 | M14 Stage-4.0 closure seat, MAJOR-2 | 2026-09-21 (M14 closure) | `src/app/workflows/categories.py::CategorySpec.primary_source` | FIXED | **One field, documented as "informational" in one file and as "load-bearing" in another, and nothing pinned it.** It decides DISCLOSURE: `build.py` maps an unavailable source to the surfaces that must go silent (D-121). The seat repointed `document` at `arena`; 912 tests stayed green, and the surface would have kept answering from a board nobody fetched while the missing-source action said "no surface names it as primary". **Remedy:** the comment says what the field does and what it must never be used for; `test_every_surface_names_the_source_its_board_arrives_on` pins all eleven pairs AND drives each one through `_surfaces_left_without_evidence`. |
| W-102 | M14 Stage-4.0 closure seat, MAJOR-3 | 2026-09-21 (M14 closure) | `docs/plans/m14-wave-2-close.md` row 6, `tests/unit/test_categories.py` | FIXED | **A wave-close row claimed a criterion was tested through the live entry point; the cited test read four dataclass fields.** REQ-SUR-001 ("each surface ranks only its own board") is a property of the ranking QUERY. **Remedy:** `test_a_board_only_reaches_its_own_surface_through_the_ranking_query` ingests three boards carrying one shared model at three ratings plus a model on one board only, then reads each surface through `category_ranking`. The old test is kept — it kills a different mutant — and the W2 row is corrected in the closure report rather than rewritten in place. |
| W-103 | M14 Stage-4.0 closure seat, MAJOR-4 | 2026-09-21 (M14 closure) | `ios/EngineTests/FrontDoorTests.swift` | FIXED | **Owner's `make check` is the measurement.** Nothing asserted that a reader can REACH the two surfaces M14 added.** All four Swift references to the new ids were membership in an eleven-id list, and the real-embedding calibration probe carried only the seven M10 questions: every test was a negative. The seat reworded `factuality`'s hint into a near-duplicate of `assistant`'s and the agent lane could not see it. **Remedy:** `testTheTwoNewSurfacesAreReachableByAsking` runs the real `SimilarityRouter` on two plain questions. If it fails on the owner's machine the hint is the defect, and that is the point of it. |
| W-104 | M14 Stage-4.0 closure seat, MAJOR-5 | 2026-09-21 (M14 closure) | `docs/prd.md` REQ-CMP-004 | FIXED | **The prd still stated the display rule D-143 amends, while the shipped card contradicts it.** REQ-CMP-004 said an unbounded scale is shown with its NAME (`Score 1504.2 Elo`); after W4 every anchored Elo surface prints `Score 65.0 / 100`. The prd is the criteria source for the closure coverage trace. **Remedy:** the M13 row is kept and marked superseded, and an amended row states the M14 rule with its citing test. |
| W-105 | M14 Stage-4.0 closure seat, MAJOR-6 | 2026-09-21 (M14 closure) | `docs/plans/m14-plan.md` §1 (REQ-IMG-002/003), `docs/closure-report-m13.md` (REQ-DTL-001/002) | FIXED | **Four carried requirements left the milestone with no disposition anywhere.** REQ-IMG-002/003 (the tenth surface and its routing) died with the image surface when W1 measured its ranked population at zero — a correct stop, recorded in the W1 close, but the §1 criteria table was never amended and neither ID appears in the prd, a wave-close scope row or this ledger. REQ-DTL-001/002 (the detail screen) were moved into M14 by the M13 closure report and are the plan's stated mitigation for taking the unit off the card — **and there is no detail screen in the app at all**, which W-098 understated. Consequence: the product removed the metric name from every card and row with nowhere for a reader to find it. Owning milestone: **M15**, where the image pricing basis is already scheduled and where the detail screen is the first thing the owner is asked to rule on. **HALF CLOSED 2026-09-21 (M15-W2): the detail screen is built and REQ-DTL-001/002 are criteria with citing tests (`docs/prd.md` §M15).** What stays open is REQ-IMG-002/003, which died with the image surface in M14-W1 and still have no disposition beyond this row; they belong with the image pricing ruling. **FIXED 2026-09-22 by M15-W2 (commit 93686df):** the owner ruled to build the screen (M15 plan §0.1) and it exists, opened from any pick and any ranking row; REQ-DTL-001/002 carry criteria and citing tests (`docs/prd.md`, `DetailTests.swift`, `test_ios_client_contract.py::test_the_detail_screen_is_reachable_and_composes_nothing_itself`). REQ-IMG-002/003 remain with the image-pricing question (W-113). |
| W-106 | M14 Stage-4.0 closure seat, MAJOR-7 | 2026-09-21 (M14 closure) | `docs/plans/m14-wave-1-close.md` row 4, `docs/plans/m14-wave-2-close.md` row 4, D-141 | FIXED | **The pulled-forward security pass was waived on two more HIGH waves and neither waiver reached THIS ledger, so the counter that exists to notice repetition could not count it** — W-088's own defect, recurring in the milestone that inherited it. Both waves named a LOCAL ledger row only. D-141, the ADR that would make row 4 name a row here, is still `proposed`: the owner signed M13 with "D-141 left proposed: explained, not ruled on", and W-090's closing line ("making C2b accept only an accepted ADR is a gate change, queued to the owner for M14") is also unaddressed. **This row is the missing count: five waivers now (W-088, W-089, W-090, M14-W1, M14-W2).** Owning milestone: **M15**, and it needs the owner's ruling on D-141 first. **FIXED 2026-09-22 (M15-W4) by the owner's ruling: D-141 accepted** ("Yes, make it mandatory"). A HIGH wave now owes its pulled-forward security pass, and a waiver must name a row in THIS ledger. The count stops at five: M15's one HIGH wave (W3) had its pass, though only at closure, after the commit (`docs/reviews/m15-closure-security-review.md`, MINOR-3), which the M15 closure report states as such. |
| W-107 | M14 Stage-4.0 closure seat, MAJOR-8 | 2026-09-21 (M14 closure) | `src/app/adapter/main.py`, `tests/unit/test_uncertainty_contract.py` | FIXED | **D-146's whole point was defended only by a numeric coincidence.** Every pinned `score_anchor` equals its surface's `min_quality` today, so serving the floor instead of the anchor passed every test; the seat ran that mutant and it survived. The board-max mutant REQ-SCR-003 names does die — it is a different mutant. **Remedy:** `test_the_served_anchor_does_not_follow_a_moved_floor` moves one surface's floor and not its anchor, through `/v1/categories`. Verified: the mutant now fails it. |
| W-108 | M14 Stage-4.0 closure seat, MINORs | 2026-09-21 (M14 closure) | `Makefile:81` lint scope, `.github/workflows/ci.yml:49`, `advisor.db` (gitignored) | FIXED | **Two gates cannot be reproduced outside the owner's machine.** (1) `make lint` runs `ruff check src tests scripts` and CI runs `ruff check src tests`; on ruff 0.15.11 the Makefile form fails on `S310` in `scripts/slopsquat_check.py:80` while CI's passes, and `pyproject.toml` pins `ruff>=0.7` with no upper bound — so `make check` is not reproducible across ruff versions and `scripts/` has never been lint-gated by anything that runs on a push (the M7/W-026 widening never reached CI). (2) 45 tests fail or error on a fresh clone solely because `advisor.db` is gitignored, while two other tests SKIP gracefully on the identical precondition — so no independent seat can reproduce a wave record's "912 passed", and the CI test jobs are almost certainly red with nothing requiring them. Not fixed here: both are toolchain/CI decisions that touch `.github/`, the owner's surface. Owning milestone: **M15**. **FIXED 2026-09-22 (M15-W4, owner authorised the `.github/` and toolchain change in session):** ruff pinned to `==0.16.2` in `pyproject.toml`; CI's lint step now runs `ruff check src tests scripts`, the scope `make lint` checks; the 45 tests that read `advisor.db` carry the `artifact` marker and SKIP by name where it is absent (`tests/conftest.py`), while `make test` sets `MODEL_RANKING_REQUIRE_ARTIFACT=1` so the owner's run exits 4 rather than skipping. **Not a seeded artifact, as recommended:** the repository is PUBLIC and the artifact holds licensed upstream data, and a synthetic one rich enough for these 45 real-data invariants would make them pass on invented numbers. Measured on a clean copy without the artifact: 904 passed, 59 skipped, coverage 88.70% (floor 85%). |
| W-109 | M14 Stage-4.0 closure seat, MINOR | 2026-09-21 (M14 closure) | `.governed-records`, `docs/research/question-coverage-2026-09-18.md` | FIXED | **`docs/research/` was ungoverned and M14 was the milestone that put a record there.** The seat gave that file a bogus `record_type`, an `id` duplicating the real plan's and a bogus `status`: `check_records` still reported PASS. W-012's shape on a directory nobody named. **Remedy:** `docs/research/*.md` is named in `.governed-records`; the record's frontmatter now uses a declared type (`brief`) and is ratified. Verified: the seat's mutant now fails the gate. |
| W-110 | M15-W2 independent Code-Reviewer seat, BLOCKING-1 | 2026-09-21 (M15-W2) | `tests/unit/test_router_hints.py::test_the_gap_register_stays_on_the_device` | FIXED | **The egress ban closed at the M14 closure named two FILES, so the file written the next day was not covered.** `Detail.swift` renders to the reader; the seat put a `URLSession` POST inside `detailFacts` and it survived every gate. **Fourth occurrence of one shape** (M13 security MAJOR-2 / W-090; M13 Stage 4.0 MINOR-4; W-099; this) and the second in two days, which is the lesson: a ban written as a list of paths is not a ban on the invariant. **Remedy:** every file under `ios/ModelRanking` is scanned, `EngineClient.swift` is the one named exemption with its reason, and a staleness assertion fails if that exemption stops being needed. Verified red on the seat's own mutant. |
| W-111 | M15-W2 independent Code-Reviewer seat, BLOCKING-2 | 2026-09-21 (M15-W2) | `Makefile:137` `SWIFT_TEST_FLOOR` | FIXED | **The Swift floor was raised by one for eleven new tests, so the wave's only content test is deletable with `make check` green** — W-091's defect, twelve days after the owner ruled on it, and the third time this integer has been wrong. It is the owner's line (W-091, W-026: a floor decides what the gate accepts, so changing it is a gate-definition change), so it is NOT guessed at here: `grep -c 'func test'` counts 257 in this tree while his last run printed 242, and only the run is authoritative. **Owner action:** the runner of 2026-09-21 23:29 printed **257** and the floor is set to it here, which is the number his own ruling on W-091 names ("raise it to the count it prints") — he reverts it if he reads that ruling as one-time rather than standing. **Proposal attached:** derive the floor from the declarations at check time instead of typing it, which ends the class rather than correcting the third instance. Owning milestone: **M15 closure.** **Ruled 2026-09-22 (M15-W4, owner in session): derive the floor, do not type it.** The owner chose the attached proposal: `SWIFT_TEST_FLOOR` is computed at check time from the `func test` declarations under `ios/EngineTests`, so deleting a test turns `make check` red without anyone editing an integer. Until then the floor stays at the count the runner prints (258). **Owning milestone: M16**, first build wave (a gate-definition change, now authorised). Control: V3C-02 -- a floor that lets a criterion's only citing test be deleted is a hole in that gate. C2b-reviewed: D-150 @3 (clause 1, owner-accepted). **CORRECTED 2026-09-22 (M15-W4 independent review, MAJOR-2): the claim above is false, and the owner accepted clause 1 on it.** A floor derived from the `func test` declarations cannot see a deleted test: deleting a test removes its declaration, so both numbers drop together and `make check` stays green. The lead agent wrote the claim and put it to the owner in those words. Put to the owner again, correctly, the same day: **ruled a committed list of test names** -- the check fails when a listed test is not run, so a deletion needs a visible edit to the list. D-150 clause 1 amended to match; owning milestone unchanged, M16-W1. **FIXED 2026-09-22 (M16-W1):** `ios/EngineTests/test-manifest.txt` names every test (262 at the M16-W1 close; this row first said 258, which the M16-W1 review measured wrong); `make swift-test` diffs it against `swift test --list-tests` and takes the floor from its line count, and `tests/unit/test_swift_test_manifest.py` makes the same comparison against the source declarations for a lane with no Swift toolchain. Verified red four ways: a test deleted from the code, its line deleted from the manifest, a renamed test, and the executed count falling short. **After the M16-W1 review (M-3, M-4):** a skipped test (`XCTSkip`) fails `make swift-test` as well, and the static half strips comments and refuses a test declaration inside `#if`, so a test commented out or guarded off fails in a lane with no toolchain. **Re-review (R-M3):** that last claim was false for the ordinary case -- the rule looked for `#if` in the first four columns and a class member's `#if false` sits at column five, and a `private func test` still read as declared although XCTest never runs it. Now the rule is about content (no `func test...()` between any `#if` and its `#endif`, at any indentation, including an `#if` left open) and a private test reads as missing; both mutants, and one with no `#endif`, re-run red. An emptied test body still passes both halves; a name list cannot see inside a test (N5). |
| W-112 | M15-W2 independent Code-Reviewer seat, MAJOR-4 | 2026-09-21 (M15-W2) | `docs/plans/m15-plan.md` §2 W2, `src/app/adapter/main.py` `/v1/categories` | FIXED | **The plan said the detail screen holds "the surface's floor" and it does not — because `/v1` does not publish `min_quality` at all.** Plan §3 is explicit that a fact `/v1` does not carry is an ADR rather than a quiet addition, and `docs/prd.md` REQ-DTL-001 was written without the clause, which is W-104's shape (a criterion narrowed to what was built). The plan line now records the drop. **Owner ruling needed:** publish the floor on `/v1/categories` under a new ADR — it is the number behind "we would not recommend below this", and the screen is where a reader would check it — or amend the plan's W2 line for good. Owning milestone: **M15-W3**, with the surfaces. **Ruled 2026-09-22 (M15-W4, owner in session): publish it.** `/v1/categories` gains the surface's floor (`min_quality`) under its own ADR, written before the field ships (plan §3, D-146 the precedent), and the detail screen shows it as the line below which the product does not recommend. **Owning milestone: M16**, beside the `/v1` ADR D-149 already owes. Control: K.8 -- a field `/v1` does not carry is an ADR, not a quiet addition. C2b-reviewed: D-150 @3 (clause 2, **accepted by the owner 2026-09-22** at the M15 sign-off). **FIXED 2026-09-23 (M16-W2):** the detail screen shows the surface's floor as "Recommendation floor", in both languages, from `/v1/categories` `min_quality` (D-152), printed through the card's own `scoreText` so it is on the same scale as the score above it, and in the board's own unit on a rank-only board. `ios/EngineTests/DetailTests.swift` `testTheFloorIsShownOnTheCardsScale` and two more; the door pins in `tests/unit/test_ios_client_contract.py` hold both detail doors to passing it. |
| W-113 | M15-W3, two runs of one measurement disagreeing | 2026-09-22 (M15-W3) | `scripts/survey_boards.py` vs `scripts/calibrate_board.py`, the `vision` board | FIXED | **The rankable count for `vision` is 41 by one script and 64 by the other, a day apart.** The two smaller boards moved by one model each, which a day of price and registry movement explains; 41→64 is not that. Both scripts ingest into a throwaway copy, reconcile, and read `ranked_population` — so one of them is doing something the other is not, and neither has been shown right. **No shipped threshold depends on it:** the floors are computed over the whole board and agree to the decimal across both runs. What depends on it is any sentence of the form "N models we could recommend", so no such sentence is published for `vision` until this is settled. Owning milestone: **M15 closure**, by re-running both on one artifact minutes apart and diffing the model lists. **FIXED 2026-09-22 (M15-W4).** Both scripts run on one artifact minutes apart: 41 and 64 again, so the counting differed, not the data. **41 is right:** `calibrate_board.py` counted board NAMES that map to a ranked model, and one model sits on a board under two or three names (`GPT-4o` three times). The same defect explains the two smaller boards' "moved by one" (25/24 models, 26/25 names). It also reached two SHIPPED numbers: `close_call` paired every name with every other, so a model was paired with its own snapshot. Over one name per model, `vision` is 7.8 / 31.2 (was 8.1 / 32.3) and `search_factuality` 4.9 / 19.5 (was 4.2 / 17.0); `search` does not move, and no floor or anchor moves. The owner ruled the corrected margins in; `categories.py` carries them. **Remedy:** `one_name_per_model` in the script, which now prints `ranked_population` (models) beside `rankable_board_names`; `tests/unit/test_calibrate_board.py` fails on the old pairing (verified red). Record: `docs/reviews/m15-category-calibration.md`, correction of 2026-09-22. **CORRECTED the same day (M15-W4 independent review, MAJOR-3):** "verified red" was an `AttributeError` -- the helper did not exist before the fix -- and putting the old counting back into `main()` passed every test. `test_main_counts_models_and_pairs_models_on_a_board_with_two_names_for_one` now runs `main()` on a patched fetch; each of the two old behaviours, restored alone, turns it red. |
| W-114 | M15-W3, found by an independent seat reading the tree after a red `swift test` | 2026-09-22 (M15-W3) | `ios/ModelRanking/Engine/Language.swift`, `tests/unit/test_ios_client_contract.py` | FIXED | **The agent deleted eleven of the app's screen strings by writing one file from a stale copy, and no gate in the project could see it.** The 2026-09-22 UI refresh added `loading`, `heroTitle`, `heroSubtitle`, `questionEyebrow`, `privateQuestion`, `noPicks`, `noAnswer`, `retry`, `noMatches`, `noMatchesHint`, `openEvidence`; the M15-W3 sync wrote `Language.swift` from a copy that predated them. `ContentView.swift` kept calling all eleven, so **the app would not build** — while the Python suite stayed green (it greps the caller) and `swift test` stayed blind (`ios/Package.swift` never compiles the view). Only the Xcode build, on one machine, could fail. **The original wording was never committed and is lost;** the replacements are the agent's and the owner may correct them. **Remedy:** `test_every_screen_string_the_client_calls_exists` resolves every `UIText.x` the client calls against `Language.swift`, verified red by deleting one. **The process defect behind it:** a file was synced from a clone that had not re-read the device's copy. Re-read before writing, always — checksums after a sync prove the write, not the merge. |
| W-115 | M15-W3, the same seat, reading the router | 2026-09-22 (M15-W3) | `ios/ModelRanking/Engine/Router.swift` `CategoryHints.byID`, `unmeasuredHints[0]` | FIXED | **Wording fixed; the probe measurement is still owed (see the end of this row).** **Two defects in one change, both in hints, both invisible to every gate that runs without Xcode.** (1) `search` and `search_factuality` were written CROSSED: each hint opened with the phrasing of the other's probe question ("look it up on the web" on `search`, "search the web" on `search_factuality`). (2) **The wave made images measurable and left the hint that declares them unmeasured in place** — `vision` measures how well a model reads an image, while `unmeasuredHints[0]` still claimed every image question was unmeasured, so the decline kept winning questions the new surface exists to answer. **Remedy:** the discriminating phrase moved to the hint whose reader would say it, and the decline hint narrowed to MAKING and CHANGING images, which nothing here ranks. **Not yet measured:** the M10 calibration probe has not been re-run with fourteen hints, and the router centres its scores on the mean over the hints it is given — so the existing probe questions' margins have moved by an unknown amount (`mathematics` sat at 0.180 against a floor of 0.15 with nine). The owner's `swift test` is that measurement. **A wave that ships router hints owes the probe a run, and this is the second wave in two milestones that did not give it one.** **Probe run 2026-09-22, and it was red:** 11 of 18 pre-M15 questions against 18 of 18 at 855b44a, because the search hints had become hubs, and four rewordings did not converge. Discharged by D-147: the wording tier routes on six example questions per surface. 21 of 21 on the probe, 18 of 22 on a held-out set never tuned against, `swift test` 257 of 257 with the tests unchanged (`docs/reviews/m15-router-recalibration.md`). |
| W-116 | M15-W4 closure, reading the plan against the tree | 2026-09-22 (M15-W4) | `docs/plans/m15-plan.md` §2 W3, D-144 | FIXED | **D-144's per-source carry-forward was planned for M15-W3 and not built.** Nothing in `src/` implements it (checked 2026-09-22), and no W3 record says it was dropped -- the plan simply stopped mentioning it. Consequence unchanged from M14: one upstream outage still blanks the surfaces that source feeds for a whole refresh cycle, and D-128 refuses to publish that rather than serving it, so the cost is staleness, not a wrong answer. **The owner deferred it on 2026-09-22 (in session). Owning milestone: M16**, first build wave. Control: V4C-13 -- planned scope that was dropped without a record is a bypass, and this row is the record it should have had at W3. **FIXED 2026-09-23 (M16-W3, D-156):** every source carries its last good data for 30 days from its last served arrival; an expired source's surfaces drop and the refresh publishes the rest; `/health` and the refresh record name what is carried. REQ-REF-009. |
| W-117 | M15-W3 independent review (retroactive), BLOCKING-1 and BLOCKING-2 | 2026-09-22 (M15-W4) | `tests/unit/test_categories.py`, `tests/unit/test_arena_client.py` | FIXED | **Two tests that protect the three M15 surfaces did not reach them.** (1) REQ-SUR-002's board-isolation test ingested three boards and checked a hand-written tuple of three surfaces, so pointing `vision` at the chat board (`primary_benchmark="Arena text"`) passed every test -- D-105's forbidden comparison, live. The ingest-label test and the client table stopped at the same three. (2) The truncation-floor assertion was loosened from `>= 25` to `>= 20` to admit the new boards while its comment claimed a proportion rule; cutting `vision`'s floor to 20 (13% of its board) passed. **Remedy:** the isolation test now covers every surface an Arena board feeds, derived from `CATEGORIES`, with the one-board-only model asserted to reach exactly one surface; the ingest test and the client table cover every registered board; each board's floor is pinned beside the board size it was set against. Both seat mutants re-run and die. |
| W-118 | M15-W3 independent review (retroactive), MAJOR-1 | 2026-09-22 (M15-W4) | `ios/ModelRanking/Engine/Router.swift` `defaultFloor = 0.15`, `ios/EngineTests/RouterBoundaryTests.swift` | FIXED | **D-147 changed how the router scores a question and the 0.15 floor was never re-measured under it.** On the wave's own probe every nonsense input scores 0.196-0.235, above the floor, and is declined only because a decline group happens to score higher; off-topic questions route with no disclosure ("what time is it" to `vision` at 0.548). REQ-RTR-005 is exactly the promise this breaks. A test pins the old literal, so the gate enforces a number whose measurement no longer applies. Nothing is installed on any reader's phone. **The owner ruled 2026-09-22 (in session): fix it in M16's first wave** -- re-measure the floor on a nonsense set and an off-topic set, add a general-question decline group if the floor alone cannot separate them, and add real-router tests for a nonsense string and for `describe this photo` reaching `vision` (the same review's m-3; m-4, held-out questions in a Swift test, travels with it). Owning milestone: **M16-W1**. Control: V4C-50 -- W-115's fix changed the router's arithmetic and inherited no re-measurement of the number that arithmetic feeds. **FIXED 2026-09-22 (M16-W1), and the measurement moved the remedy** (`docs/reviews/m16-router-floor-measurement.md`): re-measured under D-147's scoring across 86 questions in five sets -- two of them (nonsense, off-topic) written before any change and one of those never read until the end -- the floor turns out NOT to be the lever. No correct route scores below 0.232 and nonsense scores 0.161-0.395, so the ranges overlap; 0.15 and 0.20 give identical answers on all 86 questions. What was wrong was the EXAMPLES: `assistant` had no general question among its six, and `vision`'s six were question-shaped sentences any short question resembled ("what time is it" -> `vision` 0.548). Three general examples for `assistant`, the image named in `vision`'s: off-topic 6/16 -> 11/16 on the tuning set and **3/16 -> 8/16 on the held-out set**, with probe 21/21 and held-out 18/22 unchanged. Three Swift tests added on the real router (reading an image reaches `vision` while making one is declined -- the m-3 regression; five held-out questions; the floor exercised on both sides beside the fact that nonsense is declined by the groups, not the floor). The residual is W-123. |
| W-119 | M15-W3 independent review (retroactive), MAJOR-2 | 2026-09-22 (M15-W4) | `src/app/workflows/categories.py` `search`, `search_factuality`; `src/app/workflows/registry.py` | FIXED | **The two search surfaces rank on per-token price and the search call itself is not priced**, which is the pricing-basis gap the image boards were refused for; W1 named it and W3 shipped without a ruling or a disclosure. A model whose per-search fee dominates what a reader pays looks cheap in Budget Pick and Best Value. **The owner ruled 2026-09-22 (in session): state it.** Both surfaces say that the search call is not in the price, under an ADR written before it ships. Owning milestone: **M16**, with W-112's `/v1` ADR. Control: INV-24 -- what an answer does not cover is said in the payload, not left for the reader to discover. **FIXED 2026-09-23 (M16-W2):** the code D-153 publishes is worded by one Engine function, `priceExclusion` (`Detail.swift`), and shown on the pick card under its price line, on the detail screen as "Not in the price" in both languages, and once under the full ranking, whose every row prints a price. An unknown code prints nothing. `DetailTests` `testTheSearchSurfacesSayTheSearchCallIsNotInThePrice`, `testNoCodeOrAnUnknownCodeSaysNothing`; decoding pinned on a payload copied from the live route (`EngineClientTests` `testTheFloorAndThePriceExclusionDecode`). |
| W-120 | M15-W3 independent review (retroactive), MAJOR-3 | 2026-09-22 (M15-W4) | `docs/plans/m15-wave-3-close.md`, `docs/reviews/m15-wave-3-review.md` | FIXED | **A HIGH wave was committed with no code review, no Tester pass and no close record.** W-114 and W-115 were found by a seat that read the tree after a red run but left no file, so for K.7 no review existed. D-141's security pass also landed at closure, after the commit. **Remedy, retroactive and stated as such:** the independent Code-Reviewer + Tester seat ran on `d8cd650` and returned BLOCKING (2/3/6/2, 16 mutants, 5 survived); every finding is dispositioned here (W-117..W-119) and in the W3 close, written at W4. The M16 plan's definition of done now requires each close at the wave's own close. |
| W-121 | M15-W4 independent review, MAJOR-1 | 2026-09-22 (M15-W4) | `tests/unit/test_router_hints.py` `_assert_the_client_has_no_way_off_the_device` | FIXED | **The rewritten D-126 gate was still bypassable, eleven ways, sixth recurrence of the shape.** Its hand-written comment stripper did not model string interpolation, raw strings or regex literals, so each could make it eat a literal `URLSession` upload `swift test` compiled; `contentsOf:` was permitted for all of `FrontDoor.swift`, so a second read inside `save()` sent the register itself away; the import scan was line-anchored and blind through `/**/`; `ShareLink`, `AsyncImage`, iCloud Drive, `UIActivityViewController` and a second `@AppStorage("language")` were unguarded. **Remedy (owner chose to fix it in M15):** nothing is stripped -- the gate reads raw source, comments and strings included, so there is no parser to fool; every `import` anywhere is matched against the allowlist; FrontDoor's file access is permitted as five EXACT expressions, each exactly once; URL construction of any spelling (`URL(`, `URL.`, `.init(`, `NSURL`) is refused outside `EngineClient`; exactly one `@AppStorage`, typed `Language`; `isExcludedFromBackup` may only be set to `true`; and the Xcode project may reference no source outside the synchronized folder. **All 23 mutants from both seats (M01-M11, M15, N01-N11) re-run and die, plus two new ones.** **RE-REVIEWED the same day (`docs/reviews/m15-wave-4-rereview.md`, BLOCKING): 27 more ways past it** -- a markdown link in `Text`, Handoff (`.userActivity`), `.fileExporter`, `.draggable`, printing and document pickers, crash messages carrying the question, and spellings split by backticks, inline comments or a `typealias`. **Second fix:** every pattern is matched on the raw text AND on a view with backticks and inline comments removed (an extra view, never the only one); the ban list covers those surfaces, URL literals, URL-typed values, `NSMutableURLRequest`-style names, `typealias`, and interpolation into `fatalError`/`precondition`/`assert`; the backup exclusion must be a plain `= true`. 54 of 54 attempts die (the re-review's 27 as replicated by the author). **What this row does NOT claim:** that the invariant is enforced. A gate over spellings cannot follow a value through a binding, and a determined author can reach the network in ways no word list names; that is W-122. |
| W-122 | M15-W4 independent re-review, MAJOR-1 and MINOR-1 | 2026-09-22 (M15-W4) | `tests/unit/test_router_hints.py` | FIXED | **D-126 is enforced by a gate over spellings, and six rounds have shown what that can and cannot do.** Each round closed the ways a reader-facing feature would plausibly write and each seat found more; the re-review's remedy is to check the DECLARATIONS the code references (`swiftc -dump-ast` or the index store) against an allowlist, which no spelling can dodge. Open until then: deliberate indirection (`dlopen`, `@_silgen_name`, runtime class lookup by a computed string), a value laundered through a binding, and any API not yet named. Nothing of the kind ships today (checked by the three seats' greps). **Owning milestone: M16-W1**, before the "update now" button adds the app's second route to the engine. Control: V4C-49 -- a rule that bans a shape ships its gate in the same change, and this gate bans spellings where the rule bans a data flow. **FIXED 2026-09-22 (M16-W1):** `scripts/client_decl_gate.py` type-checks every file under `ios/ModelRanking` against the iOS SDK and reads the DECLARATIONS the compiler bound each reference to (`swiftc -typecheck -dump-ast`), where `` `URL`(string:) ``, `Address(string:)` after a `typealias` and `URL(string:)` are one entry. Two rules: a module allowlist that also reads `import` lines (so `Network`, `WebKit`, `CloudKit` and anything nobody has thought of are refused by absence, and `UIKit` is allowed symbol by symbol), and capability rules inside the allowed modules -- the network is `EngineClient.swift`'s alone, the file system is `FrontDoor.swift`'s alone, and sharing, hand-off, pasteboard, printing, logging and shared storage belong to nobody. Wired into `make check` as `client-decls`, SKIPPED loudly where there is no Xcode. **Measured on 20 bypasses from the two M15-W4 seats: 18 die** (backticked `` `URL` ``, `typealias`, a markdown link, `.userActivity`, `.draggable`, `NetService`, `NSMutableURLRequest`, `write(toFile:)`, `UIPrintInteractionController`, `UIDocumentPickerViewController`, `UIPasteboard`, `fputs(stderr)`, `print`, `UserDefaults`, `ShareLink`, a second read in `save()`, `URLSession` in `Detail.swift`). **Two survive and are covered by the text gate, which stays:** a second `@AppStorage` and a `typealias` of it resolve to the declaration the one legitimate use also resolves to, so it is the COUNT that catches them (`test_router_hints.py`). Neither gate is the whole check, and the record says so rather than claiming the class is closed. **Correction and fix round (M16-W1 independent review, `docs/reviews/m16-wave-1-review.md`, BLOCKING):** the markdown link in the list above does NOT die here -- `Text("[report](https://…)")` is a `LocalizedStringKey` literal, not an `AttributedString`, and it dies in the text gate only (B10); this row credited the wrong gate. The seat got past this gate three ways, all compiling for a shipped target: a type error stopped the dump and the gate judged 8 of 11 files (B-1), one configuration was checked so `#if DEBUG` and device-only branches shipped unseen (B-2), and `Darwin` was allowlisted whole, handing every file sockets, `getaddrinfo` and `dlopen` (B-3). Fixed: a client that does not type-check FAILS, every file must appear in every dump, four configurations (simulator/device x release/debug) are each checked, and `Darwin` is off the allowlist. From the minors, `NSString`/`NSData.write`, `OutputStream`, an App Group container, `@SceneStorage` and crash messages (`fatalError`/`precondition`/`assert`) are now refused, each verified red. **Still open, and written into the gate's docstring:** it scopes by FILE, not by data, so a relay declared in `EngineClient.swift` and called from elsewhere passes (B19), and `FrontDoor.swift` may write anywhere the file system reaches (B31); raw `@_silgen_name` and a runtime class lookup by computed string are unchanged from above. **Re-review of the fix round (`docs/reviews/m16-wave-1-rereview.md`, BLOCKING, 1/3/7/0):** B-3's lesson recurred one module over -- `CoreFoundation` was allowlisted whole, and `CFSocketCreate`/`CFSocketConnectToAddress`/`CFSocketSendData` from `Router.swift` delivered the typed text to a listener on the seat's Mac while both gates passed (R-B1); the same door gave Darwin notifications (B11) and `CFShow`. Also past both gates: a markdown link built at run time through `LocalizedStringKey(_:)` (R-M1), `URLCredentialStorage` with `.synchronizable` and `HTTPCookieStorage` (R-M2), `NSException.raise`, `Stream.getStreamsToHost` inside the register's file, `StringProtocol.write(to:)`, `FileWrapper` and the iCloud Drive container. **Fixed:** `CoreFoundation` is allowlisted symbol by symbol (`CGFloat` only, which is all the client resolves), and each of the others is a rule; 13 mutants re-run and all die, and `InputStream(data:)`, which the round-2 `Stream` rule had refused, passes again. `try!` on an error carrying the reader's words reaches the crash report and has no declaration to refuse; that is written into the gate's docstring. |
| W-123 | M16-W1, the router floor re-measurement | 2026-09-22 (M16-W1) | `ios/ModelRanking/Engine/Router.swift` `CategoryHints.examples`, `docs/reviews/m16-router-floor-measurement.md` | ACCEPTED | **Ordinary questions still reach a measured surface without saying so: 5 of 16 on a held-out set, 5 of 16 on the tuning set.** `what does this word mean in english` -> `vision` 0.452, `is coffee bad for you` -> `search` 0.423. Four nonsense strings also reach a surface (`test test` -> `mathematics` 0.242); the decline groups catch 12 of 16 and the floor catches none, because nothing observed scores below it. Not fixed by more example-tuning: the two ends were moved once already and the honest next step is evidence about what people actually ask, which **M16-W4** measures on public prompt corpora -- decline groups and examples are then written against real questions rather than the author's guesses. Consequence meanwhile: a reader asking an ordinary question gets a ranking for a surface their question does not belong to, with `unmeasured` false. Owning milestone: **M16-W4**, and the corpus record decides whether this needs a general-question surface, a wider decline group, or a different tier. Control: V4C-50 -- the fix for W-118 is a new artifact of the same kind (a set of example sentences), so it inherits the risk class and owes the same measurement rather than a second round of guessing. **Owner moved (2026-09-23):** M16-W4 was replaced by the updater wave on the owner's ruling, so this measurement moves to M17 with it (`docs/plans/m16-plan.md` §2 W4). Owning milestone: M17. **Owner moved again (2026-09-23, M17 plan):** the owner's §0.4 ruling put the combination on the phone, which filled M17's cap; owning milestone M18. |
| W-124 | M16-W2, fault injection on the nightly refresh | 2026-09-23 (M16-W2) | `src/app/workflows/refresh.py` `_cycle` (the `mkstemp` candidate), `src/app/adapter/nightly.py` `TIMEOUT_SECONDS` | FIXED | **A cycle killed from outside leaves its candidate file behind.** Measured by `tests/unit/test_nightly_refresh.py::test_a_cycle_killed_mid_publish_leaves_the_live_artifact_and_the_lock_usable`: the live artifact is byte-identical, the `flock` is released with the process and the next cycle publishes, but the killed cycle's `advisor.db.<random>.candidate` stays. The name is unique on purpose (two cycles must never share scratch), so nothing reuses it and nothing removes it. Only the engine's 30-minute timeout kills a cycle, and a real one takes 6-9 seconds, so this needs a stuck cycle to happen at all. **Not fixed in W2** because the fix belongs in `refresh.py` (sweep candidates older than a day before building), which is the one definition of a safe cycle and deserves its own change and test. Owning milestone: M16-W3 or the closure, whichever touches `refresh.py` first. Control: V3C-72 -- found by the fault-injection protocol, which is what a HIGH wave owes. **Also (M16-W3 review NIT-1):** since D-156 the cycle writes two more uniquely named scratch files beside it, `<artifact>.*.last-ok` and `<artifact>.*.sources`; a killed cycle leaves those too, removed only by the `finally`. Same remedy, same owner. **And (M16-W3 re-review NIT-3):** the `.sources` file is created before the `try` whose `finally` removes it, so an error writing `.last-ok` (ENOSPC) leaks it. Same remedy, same owner. **FIXED in M16-W4 (2026-09-23):** each cycle, under the lock, sweeps a candidate, build report, arrival record or unpacked bundle a day old or more (`refresh._sweep_stale_scratch`), and `.gitignore` covers them; cited by `tests/unit/test_epoch_bundle_fetch.py::test_scratch_a_killed_cycle_left_is_swept_by_the_next`. |
| W-125 | M16-W2 security pass, MAJOR-1 | 2026-09-23 (M16-W2) | `src/app/workflows/subscribe.py:25` -> `plans.py:27` -> `ingest.py`; `src/app/workflows/rank.py:19` | ACCEPTED | **The serving process loads the source parsers and their HTTP client.** Importing `app.adapter.main` puts `app.workflows.ingest`, every `app.clients` module and `httpx` in `sys.modules` (measured), because `subscribe` imports `stale_plans` from `plans`, which imports `RunContext`/`SourceReport` from `ingest`, and `rank` imports one attribution constant from `clients.epoch`. It predates M16-W2; W2's first docstring and D-154 clause 1 claimed the opposite and are corrected. **Nothing in the server calls them** and the modules that run a cycle or fetch (`refresh`, `build`, `sources`, `epoch`, `rosters`) are proven absent by `tests/unit/test_nightly_refresh.py::test_the_serving_process_never_loads_the_refresh_the_build_or_the_fetchers`. D-116 clause 2 is about ingestion RUNNING on a serving host, which it does not; it is weaker than it could be while the code sits there. **Remedy:** move the shared record types and the attribution constant into a module with no client imports, then extend that test to `ingest` and `app.clients`. Owning milestone: M16 closure. Control: V4C-49 -- a boundary asserted in prose shipped without the gate that would have caught its own claim. **Owner moved (2026-09-23, M16 Stage 4.0 security review MINOR-6):** not reached at the M16 closure, which carried a MAJOR of its own; owning milestone M17. |
| W-126 | M16-W2 code review, MINOR-3 | 2026-09-23 (M16-W2) | `src/app/adapter/nightly.py` `TIMEOUT_SECONDS` | ACCEPTED | **The 30-minute limit lives in the engine, so an engine killed hard leaves a stuck child running, and holding `refresh.py`'s lock.** A graceful stop (SIGTERM, SIGINT, `app.sh down`) cancels the schedule and kills the child -- measured by the seat on a real uvicorn. A SIGKILL or a crash of the engine does not, and macOS has no parent-death signal to hand the child; a child that is also STUCK then holds the `flock` and every later cycle exits `busy` until someone kills it. Needs two faults at once, and the launchd job this replaces had no time limit at all. **Not fixed in W2:** a limit inside the child means code in the cycle that is not `refresh.py`'s entry point (D-149 clause 2). Remedy when it matters: a wall-clock limit inside `refresh.main`. Owning milestone: M16 closure, with W-124 (both are `refresh.py` changes). Control: V3C-72. **Owner moved (2026-09-23, M16 Stage 4.0 security review MINOR-6):** not reached at the M16 closure, which carried a MAJOR of its own; owning milestone M17. |
| W-127 | M16-W3, D-148 clause 2 checked against the tree | 2026-09-23 (M16-W3) | `src/app/workflows/categories.py` `value_window`/`close_call` of `assistant`, `document`, `factuality`, `vision`, `search`, `search_factuality` | FIXED | **Six surfaces' tie margins and windows do not follow the rule D-148 clause 2 names.** Clause 2 keeps M8's sizing by candidate count on the ranked population, which the eight M8 surfaces follow. `assistant` was sized at M3 by live 95%-interval overlap, and the five M14/M15 Elo surfaces by the median gap of pairs whose PUBLISHED intervals overlap, window four times it (`scripts/calibrate_board.py`). Both are measurements of what the board cannot tell apart, which is arguably the better rule for an Elo board with published intervals; clause 2 simply does not name it. **Not re-sized in W3:** a margin change moves every `too close to call` line on those surfaces and belongs in a calibration with its own before/after, like the floors. Owner's choice for the M16 closure: amend clause 2 to name the interval rule for Elo boards, or re-size the six. Control: V4C-13 -- a rule the tree does not follow is recorded, not left for the next reader to find. **FIXED 2026-09-23 (owner, M17 plan §0.2):** D-148 amended to name the interval rule for Elo boards with published intervals; no number changed. |
| W-128 | M16-W3 re-review MINOR-3 (first review MINOR-4, last bullet) | 2026-09-23 (M16-W3) | `tests/unit/test_floor_rule.py`, `docs/research/m16-w3-floor-table-2026-09-23.md`, `scripts/survey_boards.py --floors` | FIXED | **The floor gate holds code == record, never record == measurement.** `test_floor_rule.py` compares `categories.py` with the research record's D-148 column; nothing compares that column with `survey_boards.py --floors` on the served artifact, because the artifact is not in the repository and CI cannot read it. Today they agree: the re-review regenerated the measurement on a copy of the served artifact and 14 of 14 surfaces match. **Not gated in W3:** a test pinned to one night's artifact goes red on the next refresh, which is noise, not a control. The honest gate is an operator check at each re-derivation (`survey_boards.py --floors` diffed against the record), Owning milestone: the M16 closure, or whichever wave next moves a floor. Control: V4C-13 (AGENTS.md §3.5: a hand-kept comparison is a named entry with a written reason). **FIXED 2026-09-23 (M17-W1, D-159):** there is no hand-kept floor left to go stale: `src/app/workflows/floors.py` derives every floor from the served board where it is read (`recommend`, `subscribe`, `/v1/categories`), and `scripts/survey_boards.py` measures with the same function. `tests/unit/test_floor_rule.py` holds the served floors to a second reading of D-148 clause 1 on the owner's artifact -- SQLite ordering with OFFSET, sharing no code with `floors.py` (after the M17-W1 review, MINOR-4: the first version copied it). A floor moved by unranked rows is a served change (review BLOCKING-1). |
| W-129 | M16-W4 source research (`docs/research/source-expansion-2026-09-23.md` §1.2) | 2026-09-23 (M16-W4) | `src/app/clients/swebench.py` (the SWE-bench leaderboard JSON), the `epoch_arc_agi` board, every Epoch `_external` board | ACCEPTED | **Three data sources may not permit a commercial product.** The SWE-bench site repository's LICENSE is CC BY-NC 4.0 (checked 2026-09-23), and the `coding` surface reads its leaderboard JSON. ARC Prize's terms forbid commercial reuse and database compilation, and ARC-AGI feeds `abstract`. Epoch's hub says its `_external` files "retain their original licensing", so Epoch's CC-BY covers only the boards Epoch runs itself. This is not a legal opinion. **Owner ruling 2026-09-23 (in session, translated from Turkish): keep them for now and record it.** To be resolved before any commercial launch; the research record lists a clean replacement for `coding` (Epoch's own SWE-bench Verified board, CC-BY). Owning milestone: M17, or the commercial-launch gate if that comes first. Control: V4C-13 (a known risk is recorded, not left for the next reader). |
| W-130 | M16 Stage 4.0 security review, MINOR-3 | 2026-09-23 (M16 closure) | `src/app/adapter/nightly.py` (the child's output pipe at the timeout kill) | ACCEPTED | **The timeout kill waits on any grandchild that still holds the child's output pipe:** 30.1 s against a 2 s timeout, measured on a child that starts one. Not reachable today, because `refresh.py` starts no subprocess; it becomes reachable the day it does. Remedy: kill the process group, or stop reading the pipe after the kill. Owning milestone: M17. Control: V3C-72 -- the same timeout-kill path as W-126, found by a seat breaking it on purpose. |
| W-131 | M16 Stage 4.0 security review, MINOR-5 | 2026-09-23 (M16 closure) | `docs/security-baseline.md`, the INV-n rows spread across ADRs and reviews | ACCEPTED | **There is no single, current list of the security invariants with the negative test each cites.** The Stage 4.0 seat rebuilt one as a 25-row table (`docs/reviews/m16-closure-security-review.md`), which is the current list until a maintained one exists; the closure checklist asks for it every milestone. Remedy: adopt that table as `docs/security-invariants.md` with a gate that each row's test exists. Owning milestone: M17. Control: V4C-49 -- invariants asserted in prose across ADRs and reviews, with no gate holding them together, as W-125 was. |
| W-132 | M17-W1 re-review MINOR-R4 (first review MINOR-3, second half) | 2026-09-23 (M17-W1) | `tests/unit/test_categories.py` (CAT-10, `value_window < floor`) | ESCALATED | **The check that a value window stays below its floor runs only where the served artifact exists.** Since D-159 the floor is derived from the served board, so a fixture board would test the fixture, not the product; the check runs in the owner's `make test` (`MODEL_RANKING_REQUIRE_ARTIFACT=1`) and is skipped by name in CI. A board whose floor FALLS to or below a surface's value window turns the owner's run red, which is the signal wanted. Escalated in PR #12's description, where the owner decides. Owning milestone: M17 (W2 adds boards, and re-reads this). Control: V3C-02 -- a criterion whose only citing test cannot run in CI is half a gate. **Escalated, not accepted:** this would be V3C-02's third acceptance (W-043, W-048), and at three C2b puts the CONTROL under review, which is the owner's call. |
| W-133 | M17-W1 review MINOR-5, re-review MINOR-R6 | 2026-09-23 (M17-W1) | `src/app/adapter/main.py` (`score_anchor`), `ios/ModelRanking/Engine/Language.swift`, `Uncertainty.swift` | FIXED | **Ruled, not yet built.** **The app's "50 is at the bar" uses the pinned score anchor, which since D-159 no longer sits at the floor.** Put to the owner in PR #12; **ruled 2026-09-23: the anchor moves with the floor** (translated from Turkish, "move the reference with the floor"). That reverses D-146 clause 2 and changes `/v1`'s `score_anchor`, so it is its own ADR and change, after PR #12 merges. Owning milestone: M17 (next change after W1). **FIXED 2026-09-24 (#15, D-162):** `score_anchor` is served from the derived floor on every Elo surface, so the app's "50 is at the bar" is true; the pinned field and table are gone. |
