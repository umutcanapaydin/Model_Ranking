# Wave-Close Checklist — M4 Wave 4 (rounding, equivalence, the $4.99 row; v4.1 template)

> Wave scope: m4-plan.md §3 W4 (REQ-REC-009, REQ-REC-010, REQ-SUB-006).

| # | Check | Evidence (fresh referent) | ✅/WAIVED |
|---|---|---|---|
| 1 | Risk tier: **LOW** (output formatting + one curated data row; no authz/secrets/crypto/egress; no new input surface) | m4-plan.md:6 | ✅ |
| 2 | Dev-test loop ran; full `make check` green: ruff, black, mypy (23 source files), **191 passed + 5 gated**, check-records, selftest, install-check, pin-check | run log 2026-08-15, /tmp/check.log | ✅ |
| 3 | LOW → ONE combined fresh-eyes reviewer, then a **second** fresh-eyes pass over the fix delta (the reviewer never authored any of it). Round 1: 2 BLOCKING + 5 MINOR. Round 2 on the fixes: verdict **FAIL** — all 6 round-1 findings confirmed fixed and defended (6/6 mutants RED), but the round-1 MINOR-3/-4 display-delta fix was itself **undefended** (both engines could be reverted with the suite staying green), plus 3 further findings. All closed in-wave; 3 ledgered with reasons (record §5) | review transcripts; docs/reviews/m4-w4-equivalence.md | ✅ |
| 4 | *(plan-tag)* HIGH slice | N/A — LOW wave | ✅ |
| 5 | Fault-injection (in-place, md5-verified byte-identical restore, no `git checkout`): **10 mutants, 10 RED.** (a) equivalence only vs the quality pick; (b) the note's "cheapest" sentence removed; (c) rounding moved into `plan_ranking`; (d) `round_optional_score` returns 0.0 for None; (e) same helper stops rounding; (f) seed price 4.99→7.99; (g) `lead_phrase` zero-guard removed; (h) `shown_gap` rounds after subtracting; (i) equivalence group reads the uncapped ranking; (j) group membership re-resolved by plan NAME. Round-2 reviewer independently reproduced (a)-(f) plus 2 probes that STAYED GREEN — both now covered by (g)-(j) | /tmp/fi.py, /tmp/fi2.py, /tmp/fi3.py run logs | ✅ |
| 6 | Criteria → citing tests: REQ-REC-010 (test_scores_are_rounded_at_the_output_boundary_not_in_the_math, test_elo_scores_are_rounded_in_the_output **through the CLI**, test_rounding_never_reaches_the_pareto_comparison, test_secondary_score_rounds_and_absence_stays_absent, test_a_sub_rounding_gap_never_prints_as_a_zero_delta, test_model_engine_trade_off_never_claims_a_gap_the_fields_deny); REQ-REC-009 (test_equivalent_plans_are_named_when_the_three_labels_collapse, test_equivalence_is_computed_for_every_label_not_only_the_quality_pick, test_equivalence_never_names_a_plan_the_budget_excluded, test_equivalence_group_membership_is_resolved_by_plan_id_not_name); REQ-SUB-006 (test_sub_dollar_price_survives_the_seed_exactly, test_seed_dataset_meets_req_sub_002, test_seed_dataset_ingests_and_reconciles_end_to_end) | tests/unit/test_subscribe.py, test_recommend.py, test_recommend_assistant.py, test_plans_ingest.py | ✅ |
| 7 | Security invariants: no new input, network, or authz surface; the only new data is a curated row with provenance; every new string is derived from DB values, none is user-supplied. No secret-adjacent literal added (gitleaks clean in `make check`) | git diff; review §2 | ✅ |
| 8 | No `git checkout`/`restore` on uncommitted work — every revert was a byte-comparison restore | row 5 | ✅ |
| 9c | Invariant hardening: the equivalence group is built from the **cap-filtered** rows and keyed on `plan_id`, so it can neither advertise a plan the budget excluded nor merge two same-named plans scoring different models. Both are pinned by tests, both were STAYING-GREEN probes before this close | subscribe.py group loop; 2 citing tests | ✅ |
| 9b | **Scope row — one planned criterion CHANGED, in the open.** PLANNED = rounding rule + Google AI Plus re-probe + "live proof that `--subscription` offers ≥3 **distinct** plans". DELIVERED = rounding rule (both engines, CLI-tested) + the $4.99 row with dated evidence + **the honest form of the third item**: on live data 4 of the 5 scoreable plans rank on the SAME model, so ≥3 distinct plans cannot be produced without manufacturing variety. The engine now NAMES the equivalence instead (`equivalent_plans` + `equivalence_note`, live: "$4.99 — $99.99, same model"). Rationale and measurement in docs/reviews/m4-w4-equivalence.md §1. **Ledgered, not fixed (record §5):** L-1 roster-vs-plan-page provenance wording (M4 closure) · L-2 `equivalent_plans` loses group structure with 2+ groups (M5, with the API contract) · L-3 the `dusuk` case says nothing about the 5 plans the cap priced out (M4 closure) | m4-plan.md:92-95; docs/reviews/m4-w4-equivalence.md | ✅ |
| 9a | Economy: 7 code/test files + 2 records, ~456 net lines. Token spend above the plan §5 W4 ≈ 60k line — two review rounds on the same delta, the second of which found a defect class the first fix introduced. Recorded, not hidden (V4C-13) | git diff --stat | ✅ |
| 9 | RUN LINE — gates run: ruff · black · mypy · pytest(191+5skip) · check-records · selftest · install-check · pin-check · **live end-to-end re-verification** (LiteLLM + SWE-bench + Aider live, Arena from the owner's fetch, 10 plans, 18 roster links; all three budgets × assistant + coding through the real CLI) · outcome: **shipped, with one criterion honestly restated and 3 findings ledgered**. SKIPPED: live contract tests (standing rule). Pre-existing, out of scope: `scripts/` fails repo-wide ruff/black (gate is scoped to `src tests` — GP-upstream note, unchanged from W3) | this row | ✅ |

**Escaped-blocker tripwire:** one. The round-1 review's own MINOR-3/-4 fix shipped with no citing test and was caught only because the fix delta got a second fresh-eyes pass. **Lesson for EXPERIENCE: a fix authored in response to a review is new code and inherits the review obligation — "it was written to close a finding" is not evidence that it works.** This is the V4C-49 shape (a rule without its gate) applied to fixes rather than rules.

Filled by: Claude (Cowork lead agent, D-106) · Date: 2026-08-15 · Wave commit range: `ee5a582..HEAD` (this commit)
