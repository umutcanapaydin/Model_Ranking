# Wave-Close Checklist — M4 Wave 2 (provider model rosters; v4.1 template)

> Wave scope: m4-plan.md §3 W2 (REQ-ING-009).

| # | Check | Evidence (fresh referent) | ✅/WAIVED |
|---|---|---|---|
| 1 | Risk tier: **LOW** (curated data + one new table column; no authz/secrets/crypto/egress). CI file touched = K.10 owner-review surface, flagged | m4-plan.md:6 | ✅ |
| 2 | Dev-test loop: probe provider pages → curate → implement → test → review-fix → re-run. Full `make check` green: ruff, black, mypy (47 files), **171 passed + 5 gated**, check-records, selftest, install-check, pin-check | run log 2026-08-15 | ✅ |
| 3 | LOW → ONE combined fresh-eyes reviewer. Verdict **CHANGES REQUESTED: 1 BLOCKING + 7 MINOR** — all closed in-wave. **B1** the tie-break and the output provenance had NO citing test (both mutations stayed green — the escalate-now stay-green class) → three tests added, and the fixture rebuilt so it actually discriminates (the first version passed by alphabetical luck; the reviewer's "removed" mutant is now RED too). M1 roster scope was prose-only → `scope:` is now REQUIRED DATA the parser validates, with the page's self-contradiction recorded; M2 the note undercounted drops (Kimi K3 too) → corrected; **M3 the new columns had no migration, so a persisted pre-wave DB broke with "no such column"** → idempotent `migrate()` + citing test; M4 weak version guard → table convention; M5 `INSERT OR IGNORE` too broad → scoped `ON CONFLICT ... DO NOTHING`; M6 CI printed the roster report without asserting → assert added; M7 misleading test comment → corrected | review transcript; tests/unit/test_rosters.py (16 tests) | ✅ |
| 4 | *(plan-tag)* HIGH slice | N/A — LOW wave | ✅ |
| 5 | Fault-injection (in-place, md5-verified): reviewer ran (a) tie-break inverted → GREEN (finding), (b) tie-break deleted → GREEN (finding), (c) unknown-plan guard removed → RED. After the fix, (a) and (b) re-run by the author: **both RED**, restored byte-identical. Reviewer also proved atomicity by injecting a mid-transaction failure: plan-page links byte-identical before/after | reviewer log; author re-run this session | ✅ |
| 6 | Criteria → citing tests: REQ-ING-009 "separate source with provenance + last_verified" (test_roster_links_a_plan_whose_page_names_nothing, test_shipped_roster_file_is_valid_and_fresh_on_entry_day), "links only through the registry, never guessed" (test_roster_links_reconcile_through_the_registry_and_count_drops), "own verification clock" (test_cli_exit_codes_through_real_entrypoint — the exact CI command), plus the review-mandated tie-break/provenance/migration tests | tests/unit/test_rosters.py | ✅ |
| 7 | Security invariants: no new network path (curated file, same class as plans.yaml); conflict suppression narrowed so NOT NULL violations stay loud; migration is additive-only (no data loss path). Roster staleness is its own unconditional CI leg | rosters.py; contract-tests.yml | ✅ |
| 8 | No `git checkout`/`restore` on uncommitted work | rows 3/5 | ✅ |
| 9c | Invariant hardening: every plan→model link now carries WHERE it came from (`link_source`), and the product output states it — provenance is enforced by schema, not by convention | schema.py plan_models; subscribe.py PlanPick | ✅ |
| 9b | Scope row: PLANNED = roster client/parser, roster→registry linkage with its own clock, Anthropic + Perplexity + OpenAI/Google rosters where documented. DELIVERED = the mechanism + **Perplexity Pro/Max rosters (the only provider that publishes one)**; one registry rule (claude-5-sonnet) the roster required. **NOT DELIVERED, by evidence:** Anthropic and OpenAI publish NO per-plan model list — support.claude.com Pro/Max pages and help.openai.com's Plus page name no models (both re-verified by the reviewer). Recorded in the file's probe log, not papered over. **Measured: assistant plan coverage 3/9 → 5/9; coding unchanged at 1/9** (reviewer-recomputed) | data/rosters.yaml header; review §6 | ✅ |
| 9a | Economy: 7 files, ~600 net lines (module + curated data + 16 tests). Token spend near the plan §5 W2 ≈ 90k line; the review ran long again (curated-data honesty is the wave's whole risk) | git diff --stat | ✅ |
| 9 | RUN LINE — gates run: ruff · black · mypy · pytest(171+5skip) · check-records · selftest · install-check · pin-check · live provider-page probes (4 URLs, all re-verified independently by the reviewer) · outcome: **shipped**. SKIPPED: live contract tests (standing rule → CI/owner). Ledger: **Claude Fable 5 is deliberately NOT in the Perplexity rosters** — the page states it only in its "Computer Orchestrator" section, which contradicts the Search table we transcribe; including it would have changed the top pick (Perplexity Pro 1493.3 instead of Google AI Pro 1479.6), so the choice is recorded as `scope: search-models` in data and named here. **Pre-existing defect queued to W3:** `claude-sonnet-4-6` canonicalizes to `claude-4-sonnet` (no 4.6-sonnet rule) — predates this wave, reviewer-confirmed | this row | ✅ |

**Escaped-blocker tripwire:** none.

Filled by: Claude (Cowork lead agent, D-106) · Date: 2026-08-15 · Wave commit range: `17eec69..HEAD` (this commit)
