# Written by export_project.py: per-directory file counts at the moment of export.
# `M1` reads this. A directory with fewer files than it shipped with is an incomplete
# install, and unlike a byte threshold a count cannot be satisfied by a placeholder.
.claude/agents	4
.claude/skills	17
conformance	42
docs/plans	0
docs/reviews	0
src	6
tests	4
