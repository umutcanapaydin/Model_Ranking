Project: model_ranking
RESUME HERE (2026-09-25, M17-W3 closed agent-side): the project runs on DevFlow v6.6 (D-161). Run `/start-session` first.
  - Merged since M17-W2 (#23): #29 (#25 download bounds), #30 (#16 CI step), #31 (#13 branch
    protection, applied), #34 (#28 PRD statuses), #36 (#32 the engine as a launchd service running a
    DEPLOYED copy in ~/Library/Application Support/model-ranking/engine). The owner installed it;
    the served artifact lives there now, not in this directory.
  - M17-W3 (#37, and #24) is CLOSED agent-side: draft PR on `wave/m17-w3`. Eleven boards (five
    Epoch-run, six Agent Arena on their own IPS metric), each model's accessibility from Epoch's
    model_metadata.csv, D-164 over every board no surface ranks on, and D-166 (no derived model from
    a moving alias). Close record: docs/plans/m17-wave-3-close.md. The first night was measured
    against the served artifact and publishes: docs/research/m17-w3-boards-and-first-night-2026-09-25.md.
  - OWNER after the W3 PR merges: rerun scripts/install_engine_service.sh, so the service deploys
    the merged code. Twelve moving-alias models then leave five surfaces, as D-166 intends.
  - Filed by W3 and triaged: #38 effort in parentheses (fix-issue), #39 guards compare display
    names (work-issue), #40 moving-alias spellings (fix-issue), #41 a third copy of the carried
    tables (fix-issue), #42 no loss guard on accessibility (work-issue, with W4).
  - Open: #26 (go-live), #33 (docs drift), #35 (no lock file), #38-#42. Next wave: M17-W4.
  - Open, owned: W-123 (M18), W-125, W-126, W-129 (kept and recorded), W-130, W-131.

State: 2026-09-25 -- main at c04ec4f (#36 merged); `make check-fast` PASS on the owner's Mac,
6/6 legs in 46 s. `make check-fast` is the working gate; `make check` the merge gate.

READ IN THIS ORDER
  1. this file
  2. docs/closure-report-m16.md               -- what M16 shipped, what needs you
  3. docs/plans/m17-plan.md                   -- M17: lists nobody publishes (W1 done, W2 next)
  4. docs/handovers/handover_project-2026-09-21.md -- the whole project, for someone who knows nothing
  5. docs/warnings.ledger.md W-123..W-129     -- everything open, newest first

M16 IN ONE LINE: the engine keeps itself current -- nightly refresh (D-151, D-154), every board
fetched by the refresh (D-158), a failed source carried 30 days (D-156), floors on one rule (D-148),
new models registered from the data under "the list wins" (D-157).

THE THING A NEW AGENT MUST NOT UNDO
  - Uncertainty.swift is the ONE file allowed arithmetic on a served score (D-138).
  - Nothing typed leaves the device (D-126). The gate reads RAW source under ios/ModelRanking; a
    comment may not name a banned API either. EngineClient.swift is the one door (W-121).
  - No commit as the owner (D-117); English-only repository; deploy/ and .github/ are the owner's.
  - Before writing a file, RE-READ the copy on disk (W-114). Before recommending a mechanism to the
    owner, test that it does what you say (D-150's amendment).
