Project: model_ranking
State: **M1-M5 CLOSED + SIGNED.** The owner ran the gate on his own machine (271 passed / 12
  skipped; 278 / 5 with the Epoch bundle) and ruled on all three §0 items on 2026-08-16: D-112 and
  D-111 RATIFIED, migration REVIEWED AND APPROVED, W-011 ruled RE-AUTHOR. Repo is green: 271 tests,
  8 gates, clean tree. **Next milestone is M6 — and M6 is M%3==0, so its closure MUST generate
  `docs/handovers/handover_q2.txt`.**
Last session date: 2026-08-16
Last work agent: Claude (Cowork lead agent, D-106) — inherited M5 mid-milestone from another agent

FIRST THREE THINGS A NEW AGENT DOES
1. Read `docs/closure-report-m5.md`, starting at §0 — it records the three rulings that closed M5.
2. Read `docs/handovers/handover_m5-start.md` for the project's doctrine and footguns (still
   accurate; its "M5 is unsigned" framing is the only stale part — M5 was signed, built and closed).
3. Read `docs/retrospectives/m5-retrospective.md` §"Carried question" — M6's plan must answer it:
   two coding surfaces now exist (`coding`, `agentic-coding`); does the product present both or
   choose one? The HTTP API freezes whatever is decided into a contract.

WHAT'S DONE
- M1 data layer · M2 assistant category · M3 subscription table · M4 plan answers made real ·
  **M5 coding-category rescue**.
- M5 shipped: Epoch AI as a first-class source (CC-BY, local bundle, own acquisition clock) ·
  DeepSWE as a SEPARATE `agentic-coding` category (D-105 intact — no board mixed inside a category) ·
  effort became stored data, so a score is now a (model, harness, effort) triple · budget exclusion
  counted and disclosed (D-111) · an explicit `schema migrate` command · Epoch cadence in CI.
- **The number M5 existed to move: coding coverage 1/10 -> 5/10, plus a new agentic-coding category
  at 6/10; unique plans rankable on either surface 1/10 -> 6/10.** The two numerators must NOT be
  added — the categories overlap on five plans.
- 271 unit + 12 gated tests. `make check` = 7 targets, all green (lint/ruff, typecheck, test,
  check-records, selftest, install-check, pin-check — black is installed but NOT invoked by the gate).
- Security close PASS (conditional on the owner's migration review). 22 fault injections, all RED.

SETTLED AT THE M5 GATE (2026-08-16) — do not re-litigate
- **D-112 RATIFIED.** A category with no `ranking_effort` policy keeps the best evidence each board
  published and DISCLOSES an unequal-effort comparison instead of equalising it by shrinking. The
  owner was shown the cost of the alternative (28 rankable models today; 19 / 4 / 3 at
  `unspecified` / `high` / `max`) and ruled that Q1's single-effort rule binds a category that HAS
  an effort policy, not every category. **D-111 ratified in the same session.**
- **Migration REVIEWED AND APPROVED** by the owner — permission-matrix §11 satisfied; the Stage-4.0
  security PASS is now unconditional.
- **W-011 ruled RE-AUTHOR.** The twelve placeholder-email commits are rewritten to
  `Codex <noreply@openai.com>` by the OWNER before the first push, scoped so the closing agent's own
  commits keep their identity:
      git rebase --exec 'if [ "$(git log -1 --format=%ae)" = "the address you want on commits" ]; then git commit --amend --no-edit --author="Codex <noreply@openai.com>"; fi' 57de98e^

WHAT'S OPEN
- W-001 gitleaks false positive — has now survived THREE closes; it fires at two paths while the
  ledger row records one. Owner action.
- **M6 is not planned yet.** Its plan must answer M5's carried question (two coding surfaces:
  present both, or choose?) and its closure MUST generate `handover_q2.txt`.
- Carried to M6 (docs/warnings.ledger.md is the truth): W-002 equivalent_plans group structure ·
  W-005 YAML expansion guard · W-008 roster staleness window borrowed from the plan table ·
  W-009 two migration entry points + a read-time invariant · W-010 the effort counter under-reports
  rows it cannot classify. Plus two security deferrals: the CSV export half carries no attribution;
  the agentic-coding payload does not say its evidence is undated (the coverage report does).
- Push: this container CANNOT push (proxy 403). Commits ship to the owner as a git bundle.

THE DATA ON THE OWNER'S DISK
`/Users/umutcanapaydin/Desktop/terminal_output/model_ranking/`
- `epoch_data/` — the Epoch bundle (CC-BY). Point `EPOCH_DATA_DIR` at it to un-skip 7 tests that
  carry the strongest evidence for REQ-ING-011b, REQ-SUB-007 and REQ-REC-012 (277 passed with it,
  270 without). CI cannot see them.
- `arena_overall_*.json` — the Arena snapshot (389 rows, 2026-08-12); the live client is flaky (W-007
  is FIXED — the self-inflicted rate-limit fallback is gone — but the upstream 500s remain).

ANCHOR
- AGENTS.md: 145 lines (cap 150)
- Active ADRs: universal set + D-100..D-112. D-111 and D-112 are `ratified` (owner-signed 2026-08-16).
- Governed records: `.governed-records` globs match 8 files (decisions, closure-report-m1..m5,
  fixpack-1, EXPERIENCE). The review records carry frontmatter but match no glob — widening the
  globs is a gate-definition change and therefore the owner's call.
- Most recent retrospective: docs/retrospectives/m5-retrospective.md — answers M4's carried question
  and poses the next one: two coding surfaces now exist; should the product present both, or choose?
- Most recent quarterly handover: docs/handovers/handover_q1.txt (M3). **M6 is M%3==0 — the next
  closure MUST generate handover_q2.txt.**

HANDOFF NOTES
Sandbox network: GitHub raw reachable; HF datasets-server / openrouter / epoch.ai are proxy-403 —
live halves run in CI or on the owner's machine. Turkish product strings are deliberate
(.language-allow, each with a reason); everything else committed from M3 on is English (V4C-79).
The lessons this project has paid for, in one line each:
- A fixture that encodes a remote VALUE is an untested assumption wearing a test's clothing (FP-M2-2).
- A fix written to close a review finding is NEW CODE and inherits the review obligation (M4-W4).
- A guard that cannot fail is worse than no guard, because it reads like coverage — write the mutant
  that should kill it in the same sitting (M5, third instance of this class).
- A new SOURCE is a new corpus: extend the registry's live-name expectations in the same commit that
  adds the board (M5, kimi-k2.5/k2.6 swallow).
