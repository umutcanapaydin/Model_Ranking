Project: model_ranking
State: **M1-M11 CLOSED.** Ratified by the owner 2026-08-24 on a runner with `failed: none`
  across eight sections (docs/closure-report-m11.md). Repo green: 714 Python + 59 Swift, `make check` exit 0.
  Nothing on Fly.io; nothing is billing. All NINE surfaces answer.
Last session date: 2026-08-23
Last work agent: Claude (Claude Code CLI, local lane under D-117)

ONE COMMAND
  ./runner     gate + secrets + build + surfaces + engine + refresh record + Swift tests + iOS build.
               Writes nothing into the repo and NEVER touches advisor.db. If it is not
               all-PASS, stop and read latest.log before doing anything else.

TWO MORE, AND THEY ARE THE ONES THAT FIND THINGS
  ./scripts/simulator_session.sh   starts the engine, builds, installs and launches the app on a
                                   booted simulator. This is what found six defects that eleven
                                   milestones of gates did not.
  ./scripts/enable_refresh.sh      turns the 12-hour refresh on. STILL NOT RUN (REQ-RUN-002).

WHAT M11 SHIPPED
- **The reviewer is somebody else (D-133).** In the local single-agent lane K.7 means a SEPARATE
  SESSION that reads policy from the protected base ref and leaves a FILE. A close that cannot cite
  a review with `seat: independent` must name a ledger row for the bypass. Three seats ran under it
  this milestone and all three returned BLOCKING — and every blocking finding was in the MACHINERY,
  never in the served product.
- **1,093 lines of Swift stopped being unexecuted.** `ios/Package.swift` compiles the sources the
  app ships; 59 tests in `make check` and `runner` behind a count floor. Scope: the Engine layer.
  SwiftUI is still unexecuted (W-060) and the records say so everywhere.
- **`/v1/budgets`** (D-134) — the caps `eligible_count` is computed against are published, so a
  consumer can tell which ranked rows fit. A sibling resource is not a payload revision.
- **`/health` reports `evidence`** — it used to say `{"status":"ok"}` while every query answered 503.
- **`contract-tests.yml` is green for the first time in its life.** It had run twice and failed
  both, and not for source drift: it enforced the full suite's coverage floor on a step running 14
  network tests, so it was unpassable from the day it was written.

THE THING A NEW AGENT SHOULD READ FIRST
`docs/plans/m12-inputs.md`. The owner gave the app to other people; one is a 60-year-old CFO. Five
requirements came back and four are one finding: **the product explains what it measured in the
language of the measurement.** `161.7 ECI`. `$1.03/1M`. `Abstract reasoning`. All correct, none
usable. Eleven milestones were specified by people who already knew what a benchmark was.

WHAT NEEDS THE OWNER
- **REQ-RUN-002 is PARTLY MET.** The refresh HAS now run through launchd and it **published** —
  `runs = 1`, exit 0, the artifact replaced beneath a running engine without breaking it. What is
  missing is two firings on the 12-HOUR SCHEDULE; the owner's ruling is to trigger on demand and
  verify the schedule later (W-054). Trigger one with:
  `launchctl kickstart gui/$(id -u)/com.hcs.modelranking.refresh`
- **W-074** — `shellcheck` is not installed and NO gate lints any shell here, across `runner` and
  six scripts, while M11 shipped three shell defects nothing looked for. Gate-definition change.
- **W-058** — Stage 4.3 failing a deploy on `evidence: unavailable`. Waits on a deploy.
- **D-123 / W-030 / W-031** — nothing deployed, fourth milestone. His sequencing: local, then his
  phone, then the App Store.

SETTLED — do not re-litigate
- **D-104** no LLM in the scoring path · **D-115/D-125** `/v1` payload frozen, window SPENT ·
  **D-133** K.7 = a separate session, and the review is a file · **D-134** a sibling resource is not
  a payload revision · **D-121, D-122, D-126..D-132** as before · **INV-23** read-only URIs derived,
  never concatenated.

THE LESSON M11 PAID FOR
**A control in the path that does not execute is not a control.** The router put its "we do not
measure this" disclosure in the similarity tier's floor; on any device carrying the on-device model
that tier runs second and almost never runs, and the tier that DOES run was schema-constrained to
the nine ids with no way to decline. Every question in the world came back as a confident measured
match. The requirement was satisfied in a path nobody reaches.

Corollary, paid three times in one milestone: **proving one half of a change proves nothing about
the other half.** A Make recipe was proven by breaking a test; four lines below, the `runner` half
shipped the same class of defect by different mechanics; a third instance followed in the same wave.

And one about gates: **a gate whose scope is set by free text a filler chooses is a gate the filler
can switch off without meaning to.** The K.7 gate found its subject by grepping a row's name cell.

M12 OPENS WITH
The carried question: eleven milestones specified this product and every requirement predates any
user. The fix for each of the CFO's five items is obvious once stated; the question is why none was
ever stated. **A correctness culture measures whether a number is right and has no instrument for
whether it is understood.** What would this project have to measure to catch the next one before a
user does?

ANCHOR
- Active ADRs: universal set + D-100..D-134.
- Governed records: 61. v5.0 wave records: 20.
- Most recent retrospective: docs/retrospectives/m11-retrospective.md
- Most recent quarterly handover: docs/handovers/handover_q3.txt (M9). 11 % 3 != 0 — none due.

HANDOFF NOTES
You are running ON the owner's Mac. Agent commits carry a DISTINCT identity and `GP-Agent:` /
`GP-Task:` trailers under the owner's standing permission of 2026-08-22 — never commit as him.
Green gates only. The Epoch bundle at
/Users/umutcanapaydin/Desktop/terminal_output/model_ranking/epoch_data is required by `--epoch-dir`
for seven of the nine surfaces to have any evidence at all.
