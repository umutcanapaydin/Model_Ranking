Project: model_ranking
State: 2026-09-21 — M14 W1–W4 committed and pushed (fc7fe5b). The closure wave (W5) is written
and uncommitted, and needs the owner: one runner pass, four rulings, one commit.

WHAT NEEDS THE OWNER  (detail: docs/closure-report-m14.md §0)
  1. Run runner.sh. One new Swift test may legitimately fail — testTheTwoNewSurfacesAreReachable
     ByAsking runs the real embedding router; if it is red, the router HINT is wrong, not the test.
  2. Rule on the detail screen (W-105). M14 took the unit off every card; the screen the plan said
     would hold it does not exist.
  3. Rule on D-141 (W-106). The HIGH-wave security pass has now been waived five times.
  4. Rule on the two gates that only run on his machine (W-108): CI lints less than `make lint`,
     and 45 tests need a gitignored database.
  5. Then: bash ~/Desktop/terminal_output/model_ranking/commit_w5.sh, and push.

WHAT M14 SHIPPED
  - Two surfaces (document, factuality); 11 answer on the live artifact.
  - The image/text price defect fixed and live (W-092); the 12-hour refresh running again (W-096)
    and now installable from the repo (W-100).
  - The gap register: declined questions kept on the phone, most-asked first, nothing leaves.
  - One score out of 100 per surface (D-143, D-146). Elo leaders read 57–79, percentages 73–100,
    accepted knowingly.

THE THING A NEW AGENT MUST NOT UNDO
  - Uncertainty.swift is the ONE file allowed arithmetic on a served score (D-138).
  - Nothing typed leaves the device (D-126). The egress ban now covers ContentView.swift too —
    it was a proof with a hole in it for a whole milestone (W-099).
  - No commit as the owner (D-117); English-only repository; deploy/ and .github/ are his.

ANCHOR
  ADRs D-100..D-146 (D-141 proposed). Reviews docs/reviews/m14-*. Retro m14-retrospective.md.
  M15 plan is written: docs/plans/m15-plan.md — coverage becomes a measurement, and §0 lists the
  five rulings it waits on. 15 % 3 == 0: a quarterly handover is due at M15 closure.
