Project: model_ranking
RESUME HERE (2026-09-23): M16-W1 CLOSED and pushed (acef579). M16-W2 CLOSED agent-side, UNCOMMITTED
(docs/plans/m16-wave-2-close.md; review + security + re-review, all fixed). Waiting for the owner's commit word.
  - Engine: src/app/adapter/nightly.py (D-154, proposed) runs refresh.py as a CHILD PROCESS once a
    night in 23:00-01:00 and once at startup on a stale record; off unless MODEL_RANKING_REFRESH=
    nightly (ios/app.sh sets it); refused outside dev envs (D-116). /health gains refresh fields.
    Proven end to end on a COPY of advisor.db (port 8099): a real nightly cycle published at 00:41.
  - App: detail screen shows the floor (min_quality, W-112 FIXED); search surfaces say the search
    call is not in the price (price_excludes, W-119 FIXED). make check green: pytest 1000/15, Swift 268.
  - W-124 ACCEPTED: a cycle killed at the timeout leaves its .candidate file (fix in refresh.py later).
  - W-125 (server loads the source parsers), W-126 (a stuck child outlives a hard-killed engine)
    ACCEPTED to the M16 closure with W-124. Next wave: M16-W3 (floors under D-148; owner rules
    each surface whose pick changes, plan §0.3).
  - OWNER, after the commit: ./ios/app.sh restart, check `curl -s localhost:8080/health`, then run
    scripts/retire_refresh.sh to remove the old launchd job.

State: 2026-09-22 -- M15 is CLOSED and the owner signed off (D-150 clause 2 ratified). make check
green on the owner's Mac: pytest 962/15, Swift 262 (each named in the manifest), client-decls PASS
in 4 configurations; check-records / wave-check-all / conformance-gate PASS.

READ IN THIS ORDER
  1. this file
  2. docs/plans/m16-plan.md                  -- the milestone, W1 closing, W2 next
  3. docs/plans/m16-wave-1-close.md          -- what W1 delivered and what it deferred
  4. docs/handovers/handover_project-2026-09-21.md -- the whole project, for someone who knows nothing
  5. docs/warnings.ledger.md W-111..W-123    -- everything open, newest first

M16-W1 IN SHORT
  - D-151 (owner): the refresh runs ONCE A NIGHT between 23:00 and 01:00, in the background, plus a
    catch-up when the engine starts on an artifact older than a day. NO "update now" button and no
    refresh state in the app -- amends D-149 clauses 1 and 3 and shrinks W2.
  - D-152: /v1/categories publishes min_quality. D-153: the two search surfaces publish
    price_excludes="search_call" (null elsewhere). The SCREENS that show them are W2.
  - W-111 FIXED: ios/EngineTests/test-manifest.txt names every test; deleting, renaming, skipping,
    commenting out, making private or #if-guarding one turns make check red (the last three also
    in a lane with no toolchain, tests/unit/test_swift_test_manifest.py).
  - W-118 FIXED: the router floor did NOT move (no floor separates), the EXAMPLES did.
    docs/reviews/m16-router-floor-measurement.md. Held-out off-topic 3/16 -> 8/16 (within-distribution).
  - W-122 FIXED: scripts/client_decl_gate.py reads the declarations the COMPILER resolved, in 4
    build configurations; Darwin off, UIKit and CoreFoundation allowed symbol by symbol. Its
    docstring says what it does NOT do (file-scoped, not data-scoped: B19/B31; try!; FP3).
  - W-123 OPEN (M16-W4): ordinary questions still reach a measured surface 5 times in 16.

M16 IN ONE LINE: one application (the engine refreshes itself nightly, D-151; no app button), floors
re-derived under D-148, carry-forward (D-144), the router floor re-measured first (done, W1).

THE THING A NEW AGENT MUST NOT UNDO
  - Uncertainty.swift is the ONE file allowed arithmetic on a served score (D-138).
  - Nothing typed leaves the device (D-126). The gate reads RAW source under ios/ModelRanking; a
    comment may not name a banned API either. EngineClient.swift is the one door (W-121).
  - No commit as the owner (D-117); English-only repository; deploy/ and .github/ are the owner's.
  - Before writing a file, RE-READ the copy on disk (W-114). Before recommending a mechanism to the
    owner, test that it does what you say (D-150's amendment).
