Project: model_ranking
State: **M1-M6 CLOSED agent-side. M6 awaits TWO owner items** (docs/closure-report-m6.md §0) and the
  Stage-4.0 security verdict. Repo green: 354 tests / 12 skipped (361 / 5 with the Epoch bundle),
  `make smoke-deps` PASS, gitleaks clean for the first time in the project's history.
Last session date: 2026-08-17
Last work agent: Claude (Claude Code CLI, local lane under D-117)

FIRST THREE THINGS A NEW AGENT DOES
0. **Read `docs/handovers/handover_q2.txt` first** — the quarterly handover generated at this
   closure. State, doctrine, the owner, environment, and what M7 opens with. Everything below is
   its summary.
1. Read `docs/closure-report-m6.md` §0 — the five items that need the owner.
2. Verify the state yourself: `make check` (354/12) and, with the owner's bundle mounted,
   `EPOCH_DATA_DIR=... .venv/bin/python -m pytest -q` (361/5). If that is not what you see, STOP.

WHAT M6 SHIPPED
- **The product answers over HTTP.** `/v1/recommendations?task=coding` returns BOTH coding answers
  with neither leading — the owner's Ruling A, frozen as a contract by **D-115**, enforced by a
  frozen payload key set after a denylist and then a regex were both walked past.
- One serializer behind three renderings (JSON payload, CSV export, CLI) — `serialize.py` imports no
  engine module and enumerates nothing.
- **D-118: the product speaks English**, at the source rather than the API boundary. Query values
  are `low`/`medium`/`unlimited`. The CLI became English as a consequence, stated in the ADR.
- A persisted roster staleness window with a migration (W-008), an alias-expansion guard on all four
  YAML inputs including the remote-fed one (W-005), one migration entry point (W-009), the effort
  counter fixed (W-010), `equivalent_plans` as labelled groups (W-002/D-119).
- CORS allowlist with no wildcard in any environment, startup validation failing closed in
  production, read-only database handle.
- **D-116 closes OQ-3**: Fly.io, evidence database as a shipped artifact, no ingestion on the
  serving host. Deploy proposals only — nothing is deployed.

SETTLED THIS MILESTONE — do not re-litigate
- **Ruling A** (both coding answers, neither leading) · **D-113** GP v5.0 baseline · **D-114 then
  D-117** on git authority (the agent commits at wave boundaries under five conditions; the
  milestone commit stays the owner's) · **D-118** English · **D-120** `schema migrate` exit 3 ·
  **W-001's gitleaks waiver**, scoped to a label shape rather than a path · the **REQ-API-005
  criterion amendment** (an unhealthy source is DISCLOSED in a 200, never refused).

WHAT'S OPEN
- **Stage-4.0 security verdict** — running at the time of writing. BLOCKING.
- **W-017** — the serving snapshot copies the whole database into memory per unauthenticated GET.
  **A CONDITION of go-live under D-116, not a follow-up.** Stage 4.0 must re-derive the number
  independently rather than cite W1 or W3.
- **W-021** — `.github/CODEOWNERS` owns every path to a placeholder that assigns nobody. Owner call.
- **W-019** — `L1` detects an alphabet, not a language. No mechanical fix proposed on purpose.
- **GPF-001..005** in `docs/gp-field-findings.md` — findings about GP v5.0 itself, for the owner to
  carry to the pipeline repo. Three are the same records-versus-instructions blind spot.
- The coverage and roster-staleness CI legs have still never RUN (`contract-tests.yml` is a Monday
  cron). Carried to M7; better said than claimed green.

THE LESSON M6 PAID FOR
**An enumeration that is typed out is a denylist wearing better clothes** — four instances in one
milestone (the precedence guard, the YAML wiring predicate, the list of YAML inputs, the smoke-test
endpoints), each missing exactly the member with real exposure. And **ten of ten BLOCKING findings
were reachability failures**: a control that existed, was cited, and did not run.

ANCHOR
- AGENTS.md: 146 lines (cap 150)
- Active ADRs: universal set + D-100..D-120. D-113..D-120 ratified this milestone.
- Governed records: 32 scanned by `check_records`, up from 8 before the W-012 glob fix.
- Most recent retrospective: docs/retrospectives/m6-retrospective.md — answers Ruling A's carried
  question and poses the next: is "does this control actually execute" worth automating, or is a
  control about controls the same bet at one remove?
- Most recent quarterly handover: **docs/handovers/handover_q2.txt (M6)**. Previous: handover_q1.txt.
- Handovers newest first: handover_q2.txt · handover_rocky.txt (M5->M6 doctrine, still accurate) ·
  handover_m5-start.md · handover_q1.txt.

HANDOFF NOTES
You are probably running ON the owner's Mac, not in a sandbox — the git-bundle protocol in older
handovers is obsolete. Push under D-117, green gates only, never as the owner. The Epoch bundle at
/Users/umutcanapaydin/Desktop/terminal_output/model_ranking/epoch_data un-skips 7 tests CI cannot
see, and M6 proved that matters: a Turkish assertion survived the English migration inside one of
them because the migration followed what the gate could see.
